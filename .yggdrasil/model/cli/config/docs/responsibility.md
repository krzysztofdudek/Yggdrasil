# CLI Package Docs — Responsibility

Package-level documentation and build helper scripts. README for npm consumers, agent rules reference for CLI-scoped work, post-build script for template distribution.
