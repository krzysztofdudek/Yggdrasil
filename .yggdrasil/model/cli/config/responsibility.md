# CLI Config Responsibility

Build tooling, quality, and package configuration for the CLI. Defines compilation, testing, linting, formatting, and publishing standards.

Child nodes cover specific areas: `build` (compilation and bundling), `quality` (linting, testing, formatting), `docs` (documentation and helper scripts).
