## [2026-07-31T23:14:47.904Z]
A wildcard or package-level import resolves to a single representative file for its whole directory, picked by asking which node owns the files in that directory. That pick used to ask about every file in the directory regardless of whether the graph excludes it, so an adopter's own coverage.excluded entry could count toward whether a package looked single-owned or split — the excluded file's owner still had a vote it should never have had. The candidate list a package's ownership decision is drawn from now drops any excluded file first, so the split-or-single-owner call is made only from the files this graph still enforces.
## [2026-07-31T23:14:51.954Z]
The previous fix for a split package's ownership decision went too far the other direction: it discarded an owner's OTHER files entirely once one of that owner's files was excluded, so a component still mapping several files in a package — only one of them excluded — could be dropped from the ownership tally altogether and lose a real, still-enforced dependency. Ownership is now decided over exactly the files that remain after the excluded one is dropped: no more, because the excluded file's own vote is still gone, and no less, because every one of its owner's other files still counts on equal footing with everyone else's.
## [2026-07-31T23:14:56.722Z]
Two remaining gaps in the same package-ownership decision both depended on where an excluded file happened to sort among its siblings, or on nothing having a component owner at all. The nodeless-package fallback — used both by the live type-relation gate and by any wildcard import into a package no component owns — still picked a plain first-in-sort-order file with no exclusion awareness, so a forbidden cross-type dependency could disappear purely because the excluded file's name happened to sort first. And a wildcard import into an unowned package was not gated by the type-relation check at all, unconditionally, regardless of exclusion — the identical dependency written as a precise single-type import was already gated correctly. Both fallbacks now prefer a live, non-excluded file the same way the component-owned path already does, and the wildcard path is now gated exactly like a precise import.
## [2026-07-31T23:15:01.375Z]
Two of this package's own language resolvers — Python's absolute-import search and PHP's PSR-4 base-directory search — built their candidate set from a bare on-disk existence check with no exclusion awareness, unlike every other resolver in this package. A module or class that genuinely resolves to two or more files under different roots correctly stays silent as ambiguous, but an excluded file among those candidates still counted toward that ambiguity, so excluding it could never restore a real, now-unambiguous dependency the way excluding a Go or Java package member already could. Both resolvers now drop an excluded candidate from the ambiguity count before deciding whether resolution is ambiguous, the same drop-then-decide rule the rest of this package's resolvers already apply.
## [2026-08-01T00:40:20.080Z]
Java's ancestor-source-root search and Python's per-root candidate list both previously stopped at the first candidate that merely existed on disk, regardless of exclusion — so an excluded nearer Java source root, or an excluded module file shadowing a live same-named package at the same Python root, could still end the search and silence a real, still-enforced dependency instead of yielding to the candidate right behind it. Java's precise-type and wildcard-package searches, and Python's absolute and relative resolvers alike, now treat an excluded candidate as though it were absent and keep trying the next one — the same drop-then-decide discipline this package's other resolvers already apply, extended to a nearest-wins search instead of only an ambiguity count.
## [2026-08-01T02:33:04.357Z]
Fixed the Python import resolver's per-root candidate order: it tried a same-named module file before a package, the opposite of CPython's real precedence. Verified directly against the interpreter (both a package and a module file present with the same name; python3 loads the package's __init__.py, not the module), so the resolver could name the wrong source file for a real import violation. Both the absolute and relative resolvers now try the package first, matching CPython, and the existing exclusion-fallback tests (which previously encoded the reversed order as their own expected behavior) were corrected to match.
## [2026-09-24T06:30:45.963Z]
Rust resolution now follows the module tree the compiler builds rather than an approximation of it. Grouped use trees lost every dependency whose items were paths, or pinned them on the prefix module, which is the shape rustfmt and rust-analyzer write by default, so each item is now resolved on its own under the accumulated prefix. super and self inside an inline module such as the canonical mod tests block were climbed from the file and produced a false dependency on the parent module, so relative specifiers are rebased across the enclosing inline modules before they reach the resolver. Items defined in the crate root were unreachable, so the crate root file is now the module file of crate, bound for a named item only when that file declares or imports the name, which keeps macro_export macros defined elsewhere silent. Binary, test, example and bench targets are crate roots of their own, so crate:: there no longer binds same-named library modules. In-repo Cargo path dependencies resolve by the name code uses, because in a Rust monorepo the inter-crate imports are the architecture to enforce, and registry crates can never be path dependencies. Raw identifiers are stripped because rustc loads gen.rs for mod r#gen. For Go, an import now binds through the longest in-repo module path that prefixes it, drawn from the importing file's own and ancestor go.mod files and the members of the nearest go.work, because workspaces and nested modules importing their parent are how large Go codebases are split and module paths are unique; a directory a deeper go.mod claims for a different module is silenced so the wider search cannot bind the wrong module.
## [2026-09-24T06:38:58.165Z]
The C# extractor missed dependencies real .NET code relies on and produced false edges in multi-project solutions. The base name of a generic (Repository<T>, IHandler<,>, Result<T>) is now a reference like a bare name, and a qualified generic is matched by its plain dotted name, because declaration keys carry no arity and external containers such as List still resolve to nothing. A member accessed through a type name (Guard.NotNull, OrderStatus.Paid, fully qualified chains) is now a reference, emitted only when the file declares no local, parameter or member of that name so a value that shadows a type never becomes an edge. Extension-method calls are bound statically in C#, so extension methods are now indexed by namespace and name in a key space no type can reach, and a call on a value receiver resolves through the enclosing and imported namespaces, skipping System.Object members and names the file itself declares as methods, which an instance method would win. C# 14 extension blocks are read in both the shipped grammar's misparse and the upstream receiver_parameter shape so the edge survives a grammar upgrade, and the shipped grammar's recovery of a null-conditional assignment is read as the object creation it is. Global usings are scoped per project in a new module that finds the nearest .csproj and reads its Using items, Directory.Build files and SDK implicit usings, because a global using only applies inside its own compilation; an alias with two targets in one scope is silenced instead of picking one.
## [2026-09-24T06:39:33.724Z]
The TypeScript/JavaScript extractor and resolver change for three reasons. First, the type-only rule was inconsistent: `import type` and `export type` were silent while `typeof import('x')`, an annotation `import('x').T` and `import type X = require('x')` produced edges, so the everyday vitest `importOriginal<typeof import('x')>()` idiom refused components over a dependency that does not exist at runtime. One rule now holds for every spelling: a relation edge records a module the file loads, and every construct TypeScript erases is silent. Making type-only references edges instead was weighed and not taken in this change: on this repository it adds 289 edges across 127 components, one of them into a pair the architecture forbids, so it is an owner decision about what a relation means, not a bug fix. Second, the dominant cross-folder import styles of real TypeScript monorepos never resolved: tsconfig paths and baseUrl, workspace packages imported by name, package.json imports, root-absolute Vite specifiers, a directory's own package.json entry, index.mjs/.cjs, explicit .json/.css/.svg/.vue targets, bundler query suffixes, new URL with import.meta.url, and imports inside Vue and Svelte components. Each is now resolved the way the compiler or the bundler resolves it, and every ambiguous answer (two paths targets naming different files, two packages with one name, an unreadable tsconfig) stays silent, because a wrong edge is worse than a missed one. A root-absolute specifier used to be joined onto the importer's own directory, which could name the wrong component. Third, the shipped grammar swallows valid statements into parse errors (export-from with attributes, anything after for-await-using, a .tsx import type with type arguments); the lines an error region touches are rescanned for line-anchored import and re-export statements so the swallowed dependency is not lost, while a clean parse is never rescanned and the type-only rule applies to recovered statements too. The grammar itself is not changed here.
## [2026-09-24T06:40:27.202Z]
Python absolute imports probed every ancestor directory as a source root, including directories inside a regular package, so a package with its own logging.py or celery.py turned every import of the real standard or installed library from its sub-packages into a false dependency. A directory holding an __init__.py, or whose parent does, can never be on sys.path, so it is no longer probed. The same resolver missed every import across source roots that are not ancestors of the importer, which is how the PyPA src layout and uv or Poetry workspaces are built: tests outside src, and one workspace member importing another. Source roots are now also discovered repo-wide from pyproject.toml, setup.cfg and setup.py (the src child when it exists, the project directory otherwise); a discovered root never matches a standard-library top-level name, because installed projects come after the standard library on sys.path, and discovered roots join the same two-distinct-matches-means-silence rule.
## [2026-09-24T06:40:32.480Z]
Ruby treated every qualified constant as absolute and suppressed every bare constant inside a class or module body. The first bound an unrooted Billing::Invoice written inside module Shop to the top-level Billing::Invoice although Ruby resolves it to Shop::Billing::Invoice, a wrong-node edge; the second hid nearly every dependency a Rails app has, because those are bare constants in class bodies. References are now resolved the way Ruby resolves them: only a ::-rooted reference is absolute, and anything written inside a namespace gets the Module.nesting candidates nearest first, then the top level. A nesting candidate whose first segment exists but whose member does not silences the reference, because Ruby stops there, and the top-level fallback is silenced when some in-repo namespace nests a constant of the same name, because the enclosing class could inherit it. Compact declarations whose file path matches the Zeitwerk convention for their full name now anchor their implicit outer namespaces, so compact-style Rails code is no longer silenced wholesale, while a stub whose file name does not match anchors nothing.
## [2026-09-24T06:42:55.346Z]
Removed guards that no input can reach from the new Rust and Go resolution code, so the branch coverage gate measures real paths. The inline-module rebase is only ever called for a self or super specifier inside at least one inline module, a nameless inline module is folded into the same silence as a path-relocated one, and every module path the Go candidate list holds is non-empty by construction. Behaviour is unchanged.
## [2026-09-24T07:02:00.466Z]
The Rust and Go module-tree fixes and the Python and Ruby resolution fixes met in this component at merge. Both keep the one-row-per-line-and-node rule through the shared resolveDetectedEdges helper, and project-root discovery now builds the Rust dependency view and the Python source roots side by side; the merged code carries both behaviours.
## [2026-09-24T07:08:12.655Z]
The shipped Kotlin grammar predates Kotlin 2.2: a when-guard, a multi-dollar string or a context-parameter clause made it give up on the rest of the file, so every later declaration vanished without a word. That missed real dependencies and, when a lost declaration duplicated another file's, flipped an ambiguous import into an edge to the wrong component. No released grammar fixes this (the maintained upstream has no npm release and different node types), so the Kotlin extractor now recovers on the grammar it has: it masks those constructs at the same length and re-parses, re-reads what is still damaged one top-level declaration at a time, trusts only the name in a declaration's header when its body will not parse, and marks what it cannot read so resolution fails closed instead of guessing. Declarations local to a function, lambda, initializer, accessor, constructor or object expression are no longer keyed as top-level names, because such a local made a real top-level import of the same name look ambiguous and silenced it. A Kotlin file with top-level functions or properties now also declares its JVM file facade, the class name Java code imports, and a star import is emitted as a package marker the resolver collapses by owner instead of a bare package name nothing could match.
## [2026-09-24T07:08:20.535Z]
The shipped Kotlin grammar marks a one-line class body such as class A { val x = 1 } with a zero-width missing separator, so a file full of ordinary code reported a parse error. Treating any error as damage sent such files through recovery, which re-read the class from its header alone and dropped its members from the symbol table. Only a real ERROR node now counts as damage; a tree whose only defects are missing tokens is read as it stands, exactly as before recovery existed. A recovered declaration is also parsed with a trailing newline so its last member is terminated the way it is inside the file.
## [2026-09-24T07:13:43.255Z]
The preprocessor condition evaluator was restructured around operator tables instead of a long chain of cases, with the same results: a condition built from literals only is evaluated, anything that depends on a macro stays unknown and keeps its include live. Fewer incidental branches make the evaluator easier to test completely, and the repository holds every source file to a coverage floor.
## [2026-09-24T07:15:03.459Z]
The C# extractor and its project-scope module carried defensive checks for tree shapes the grammar never produces (a method without a name, a generic without its argument list, a qualified name without both halves) and a comparator and root test written for inputs that cannot occur. They are replaced by the invariants they guarded, stated where they are relied on, so every remaining branch is one real input can take. The extension-member reading is also narrowed: a local function counts as a C# 14 extension member only when it sits directly in the body of the misread extension block of the static class itself, never when it is a helper inside some other member.
## [2026-09-24T07:20:04.222Z]
The C, C++ and PHP extractors and resolvers produced false edges in a few preprocessor shapes and missed common real forms. A C or C++ include under #if (0), #if false, a constant expression, the else of a taken #if 1, or an #elif 0 after __has_include (which both grammars flatten into an error) was counted although the compiler discards it; conditions are now evaluated from literals only and a text scan covers trees with parse errors, with every unknown condition kept live. Header names are read raw so backslash paths resolve, angle includes are emitted for lookup under a compilation database, absolute names are refused, and include roots come from the database or a probe limited to multi-segment names with exactly one hit. PHP now resolves namespace-relative and alias-qualified class names by the language's own compile-time rules, reads the PSR-4 empty prefix and PSR-0 which the old comment wrongly called forbidden, falls back to every composer.json when the nearest one resolves nothing, and treats a require of __DIR__ or dirname plus a literal as a file dependency. Extractor revisions are bumped so cached facts from the old shapes are not reused.
## [2026-09-24T07:25:15.156Z]
The Python and Ruby extractor fixes and the Kotlin parse recovery with Java cross-source-root imports met in this component at merge. The Python source-root and Ruby lexical-scope extraction and the Kotlin recovery of declarations after syntax the shipped grammar predates touch disjoint extractors, so both sets of changes are kept as each branch wrote them, and the reference cases of every language pass against the combined resolver.
## [2026-09-24T07:25:47.883Z]
The C# extractor changes were carried onto a release that meanwhile changed the Python, Ruby, Rust and Go extractors. The C# code and its project-scope module are unchanged in substance; the component is re-recorded because its combined source is new, and every language matrix, the C# one included, passes against the combined state.
## [2026-09-24T07:32:22.049Z]
C, C++ and PHP relation extraction follows what the compiler and the autoloader actually do. A C or C++ include in a branch that is constant-false, or after a taken branch, is not a dependency, and a header name is read raw so backslash paths resolve; include roots come from a compilation database, else from a probe that needs exactly one hit. PHP class names without a leading backslash resolve by the language rules from the file alone, the PSR-4 empty prefix and PSR-0 are read, the nearest composer.json no longer hides the others, and a require built from __DIR__ or dirname plus a literal names its file. Every one of these keeps silence on ambiguity, so the check gains the dependencies real projects are made of without inventing any.
## [2026-09-24T07:44:15.012Z]
The C, C++ and PHP extractors and resolvers now sit on top of the reworked Java and Kotlin resolution without sharing any of its code paths: C and C++ resolve includes by path through the includer directory, a compilation database or the exactly-one-hit probe, and PHP resolves class names by its own compile-time rules through composer autoload maps. Nothing a JVM import resolves to changes, and no C, C++ or PHP edge depends on the JVM symbol namespace.
## [2026-09-24T07:48:05.280Z]
The C# extractor changes, with their per-project using scopes, were carried onto a release that meanwhile gained the shared Java and Kotlin namespace and Kotlin parse recovery. The two touch different languages and neither changes the other's extraction; the C# code and its project-scope module are unchanged in substance by the merge.
## [2026-09-24T08:37:15.026Z]
The C, C++ and PHP relation fixes (include roots, dead preprocessor branches, C++ header routing, repo-wide composer autoload and PHP name resolution) met the already merged Java, Kotlin, C# and merge-resolve changes in this component. They touch separate language paths and merged without source conflicts; the merged code carries both, and this entry records that the combination is what the verdicts now answer for.
## [2026-09-24T12:39:05.474Z]
With the regenerated TypeScript grammar, `for (await using …)` and a .tsx import type with type arguments parse cleanly, so the only known form that still swallows a statement is a re-export with import attributes. A guard that silenced a bare `require()` left inside an ERROR region when the grammar shed the `import type X =` in front of it is removed: no form reaches it on the shipped grammar, so it was untestable code guarding nothing. The general ERROR-region rescan stays, because the attribute re-export still disappears in every shipped TS/TSX/JS grammar and because it costs nothing on a clean parse.
## [2026-09-25T17:43:41.819Z]
A type-only reference is now a dependency like any import: a file that compiles only against another component's types breaks when those types change, and an architecture that forbids that coupling has to see it. The TypeScript and JavaScript extractor therefore gives an edge for every spelling it used to silence (whole-statement and all-inline type imports, type re-exports including the star form, type-only import-equals, an import() in a type annotation, a type query, a type argument or the type side of as and satisfies), and for a module augmentation, which merges declarations into the module it names. An augmentation counts only when it really is one (a relative name, or any non-wildcard name inside a module file); the same header in a script file declares an ambient module and stays silent. Declaration files are now probed after the sources, the way the compiler resolves them, because a type-only import commonly names one. Ambiguous resolution still gives no edge.
