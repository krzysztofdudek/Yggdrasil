## [2026-07-28T15:06:29.815Z]
Now also calls the relocated allow-list lookup directly from its new engine home.
## [2026-07-28T15:37:14.315Z]
Added the live type-to-type relation gate as a new phase of the same live pass: statically-resolved import edges between two classified endpoints (an explicit node or a type-covered file) are now checked against the architecture's relation allow-list, the same authority the existing relation-target-forbidden validator and undeclared-dependency check already read.
## [2026-07-31T17:59:32.846Z]
An import specifier resolved fresh from source can name any file on disk, including one this graph excludes — a coverage.excluded root, or a nested project's own boundary. The owner index that turns such a resolved file into a node must answer 'no owner' for an excluded file the same way every other ownership question in this graph already does, or an import reaching into an excluded subtree keeps naming its textual owner and wrongly trips the undeclared-dependency check. The Go and Java package resolvers go one step further: given a package/wildcard import, they pick a representative file for the whole directory by consulting an owner index too, so they can attribute the import to that file's owner. That picking index needs the identical guard, or it can land on the one excluded file in an otherwise ordinary package and silently drop an edge that is legitimately reachable through the package's other, non-excluded files — the opposite failure from leaking an excluded file's own ownership, and just as wrong. Every caller that resolves a specifier fresh from source now builds its owner index through one shared, exclusion-guarded constructor, so the index that decides a resolved file's node and the index that arbitrates a package's representative can never drift apart on what counts as excluded.
## [2026-07-31T23:14:34.795Z]
The shared owner-set decision a split package's import resolution depends on used to weigh every file in the directory toward whether the package looked single-owned or genuinely split, including a file the graph excludes from coverage entirely. That let an adopter's own exclusion list decide, wrongly, whether a package still looked split — the excluded file's owner kept a vote over a directory it no longer has any real files in. The decision is now made only over the files that remain once an excluded one is dropped.
## [2026-07-31T23:14:40.380Z]
Correcting the split-package ownership rule further: dropping an excluded file's own vote was right, but the prior version also dropped every OTHER file belonging to that same excluded file's owner, so a component still mapping several non-excluded files in the same package could lose its whole claim on the package the moment any one of its files was excluded. The rule now keeps every surviving file's vote exactly as it was — an exclusion removes only its own file from consideration, never any other file's owner from the tally. Two or more distinct owners left among the surviving files still means a genuine split and stays silent; one owner left attributes the import to it; nothing left because every file was excluded stays silent because the target itself is gone.
## [2026-07-31T23:14:43.814Z]
The same shared resolver's nodeless-package fallback — reached whenever no candidate file in a package has a component owner, the normal case for a package enforced only by its architecture type — still picked a plain first-in-sort-order file with no awareness of exclusion, so whether a forbidden cross-type dependency was reported could depend purely on an excluded file's alphabetical position among its siblings. The fallback now prefers a live, non-excluded file the same way the owned-package path already does, so the live type-relation gate can no longer lose a real finding to a filename accident.
## [2026-08-01T00:40:14.835Z]
The shared Java dependency factory built its ancestor-source-root walk with no exclusion awareness at all, unlike every other resolver this dispatcher wires up. Java has no single canonical source root, so both a precise single-type import and a wildcard package import search the importing file's own directory and every ancestor above it, nearest first, and commit to the first hit — so an excluded nearer candidate still won that search and then vanished, instead of the walk continuing to a farther, still-live candidate the way a genuine miss already does. The factory now threads the same exclusion predicate every other language resolver already receives into the Java one, closing the last resolver this dispatcher built without it.
## [2026-08-06T13:56:24.639Z]
Every caller that needed statically resolved imports assembled the same four dependencies by hand. That duplicated the wiring and forced each caller to declare its own dependency on the extractor registry, inflating how much each one appeared to reach into. A single entry point now assembles and runs the pass, so a caller depends on this layer alone and the wiring lives in one place.
## [2026-09-23T19:15:19.276Z]
An architecture with no node types constrains no relation, since the relation-target validator skips it entirely, yet the refusal message computed an empty allow-list for the unknown types and told the reader no relation type was allowed and to edit the architecture. On an empty architecture the message now offers every relation type and the stanza to paste, because declaring the relation is all it takes.
## [2026-09-23T20:50:36.337Z]
The cache-audit harness had no production caller; it moved into the only test that uses it, so the shipped source and its reviews no longer carry test-only code.
## [2026-09-24T01:25:09.144Z]
The relation pass read every mapped file and resolved every file's facts one awaited call at a time, so a large repository's check spent most of its time waiting on the disk; both now run a bounded number at a time and record results in the original order. The structural feature count walked the tree by allocating a node object per child, a third of a cold run's parse time for an index that never gates; it now walks one cursor and produces identical counts. The owner index looks up candidate owners through the mapping index instead of scanning every mapping entry per file.
## [2026-09-24T06:30:46.553Z]
The disk-backed resolver capabilities for Rust and Go had to grow to match what the language resolvers now need. For Rust it works out which Cargo target a file belongs to, reads the dependency tables of Cargo.toml to map in-repo path dependencies (inline, table form, workspace-inherited, renamed) to their crates, and reads a crate root file to confirm that it declares a name before a crate-root item binds to it. For Go it reads every go.mod from the importing file up to the root and the use members of the nearest go.work, and it unquotes the module path, whose quoted and block forms used to disable resolution for a whole module. The pass also stops reporting one line twice against the same component: resolving every item of a Rust use tree on its own can bind several specifiers on one line to one module file, and one site depending on one component is one finding.
## [2026-09-24T06:38:57.455Z]
Two C# resolution defects had their cause in the shared pass and resolver. First, ambiguity was counted by defining files, not by owner components, so one declaration split across several files of the same component (C# partial classes, Result and Result<T> kept in separate files, Kotlin expect/actual, overloads spread over files) was treated as ambiguous and every dependency on it was silenced even though its target is one component. The resolver now maps the files to their owners and stays silent only when they span two or more components or include an unmapped file; this is language-agnostic, because the target is unambiguous whatever the language. Second, C# global usings and global aliases were aggregated over the whole repository, so one project's global using leaked into another project and produced false edges, and two projects defining the same global alias bound whichever was read last. The pass now asks the C# extractor module for each file's project scope, keyed by the nearest .csproj, and assembles each file with its own project's usings and aliases.
## [2026-09-24T06:40:37.052Z]
One import line whose candidates all resolved to the same target was reported once per candidate, so a Python from-import printed the same violation two or three times and C# lines naming a type twice did the same. Resolved edges are now deduplicated per file, line and target node in one shared helper that both the live pass and the reference-case runner use, and the runner asserts that no edge row repeats. The resolver also gained Ruby's lexical gates: a reference rooted at a Ruby core class or at a ubiquitous framework namespace is always external, because an in-repo class String or module ActiveRecord can only be a reopening and the syntax cannot tell a reopening from a definition, which had made every use of String in specs and every ApplicationRecord superclass a false edge. Extension-less Ruby files such as Rakefile and Gemfile now select the Ruby grammar, and .rake, .gemspec and .ru files are parsed as Ruby, so their dependencies are checked instead of being counted as owned and never read.
## [2026-09-24T06:42:56.168Z]
Cargo target sub-paths are package-relative and never climb, so they are joined directly instead of through the repository-escape check with a fallback that could never be taken. This keeps the branch coverage gate measuring paths that exist. Behaviour is unchanged.
## [2026-09-24T07:01:59.055Z]
The Rust and Go module-tree fixes and the Python and Ruby resolution fixes met in this component at merge. Both keep the one-row-per-line-and-node rule through the shared resolveDetectedEdges helper, and project-root discovery now builds the Rust dependency view and the Python source roots side by side; the merged code carries both behaviours.
## [2026-09-24T07:08:05.268Z]
Java and Kotlin compile into one JVM namespace, yet the symbol table kept a separate namespace per language, so a Kotlin import of a Java class and a Java import of a Kotlin class never resolved in mixed Android and Spring codebases. The table now files both languages' declarations under one shared namespace and can list the files declaring direct members of a package, which star imports need. The resolver gained a JVM route: a Java import that the package-equals-directory probe misses (a sibling Maven or Gradle module, production code imported from src/test, a Kotlin class or file facade) is looked up by its fully-qualified name in that namespace and binds only when exactly one file declares it, so Java recall in module-per-component graphs is no longer close to zero while the zero-false-positive floor holds. A Kotlin star import now collapses by owner the way a Java on-demand import always did, instead of being ignored. Kotlin files the grammar could only partly read carry incompleteness markers; the resolver treats a marker as a possible second definer of the keys in its own package, never as a binding, so a declaration the parser lost can no longer turn an ambiguous import into an edge to the other file.
## [2026-09-24T07:25:13.339Z]
The Python and Ruby resolution fixes and the Java and Kotlin shared-namespace fixes met in this component at merge. The symbol table now keys every declaration by namespace, where Java and Kotlin share one JVM namespace and every other language keeps its own, and it records both the Ruby nested-constant tails and the per-package direct members that star imports need, so each language keeps exactly the lookups it had on its own branch. The resolver sends only Java and Kotlin through the JVM route (shared-namespace lookups, star imports collapsed by owner, the Java source-root miss fallback, and the incompleteness markers that keep a possible ambiguity from an unreadable Kotlin declaration), and applies the Ruby external-root, anchor and inheritance gates only to Ruby, so neither set of rules can change the other language family's outcomes.
## [2026-09-24T07:25:47.092Z]
Counting ambiguity by owner component and the Ruby lexical gates now meet in one resolver. The Ruby gates still run first and can force a candidate absent or ambiguous; only what they let through is counted by owner. The Ruby inheritance guard, which silences a top-level constant when a same-named constant nested elsewhere could be inherited instead, used to apply only to a constant defined in exactly one file, because two files were ambiguous anyway. Now that several files of one component bind, the guard applies whenever any file defines the constant, since the inheritance hazard does not depend on how many files the component spreads it over.
## [2026-09-24T07:48:02.622Z]
Counting ambiguity by owner component and the shared JVM namespace met in one resolver. Java and Kotlin lookups take their own route, so the owner rule is applied there too: several files of one component that declare the same name, such as a Kotlin expect declaration and its actual or top-level overloads spread over files, now bind to that component, while files in two components or an unmapped file stay ambiguous. The incompleteness markers of partly unreadable Kotlin files are counted by owner as well. A marker in a file of the same component as the declaring files cannot move the edge to another component, so it no longer silences the import; a marker in another component or in an unmapped file still silences it, because the lost declaration there could be the real target. A lone unmapped declaring file keeps the earlier fail-closed behaviour, where any other marker file makes the lookup ambiguous.
