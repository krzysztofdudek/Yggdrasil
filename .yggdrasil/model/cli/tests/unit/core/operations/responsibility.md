# Operations Core Unit Tests — Responsibility

Tests for core operation modules: approval, validation, drift detection, impact analysis, dependency resolution, migration, node selection, and LLM-backed review (aspect verification, artifact review). Pure unit tests using in-memory graph fixtures — no filesystem side effects.

Does not test context assembly — that belongs to the context sibling node.
