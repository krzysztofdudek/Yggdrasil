# LLM Unit Tests — Responsibility

Unit tests for the LLM provider module covering provider factory selection, Ollama provider HTTP interactions, response parsing, availability checks, and fallback behavior.
