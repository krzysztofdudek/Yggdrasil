## [2026-07-28T13:55:59.258Z]
New file created by splitting the check command's output rendering into single-responsibility modules — this one owns view selection (--summary/--top/--aspect/details/full) and composes the header and group renderers, with no behavior change from the code it was cut from.
## [2026-08-03T08:01:05.549Z]
The Next: line's residual annotation now excludes a pair this same run's fill already proved cannot run from the count it promises --approve will fill, folding it into the errors-remaining count instead. Before this, an unverified pair that could never be filled was still counted as something --approve would fill, so the footer promised progress a re-run could never deliver for that pair.
## [2026-09-25T21:50:36.729Z]
The step a report names first now states the real cost of the command it names: every pending pair that command fills, advisory ones included, with the reviewer's share given as pairs and calls, because a tier's consensus multiplies what each pair bills and the step used to count only the blocking pairs and only the pairs, so a paid fill was announced at a third of what it billed. The same numbers go into the machine document, where they equal the cost preview of running that command. The step is also taken from what the finding's emitter hands over as data before anything is read out of its sentence, and a sentence may only ever supply a graph file to change, never a source file: an unmapped file used to come out as an instruction to edit that file.
