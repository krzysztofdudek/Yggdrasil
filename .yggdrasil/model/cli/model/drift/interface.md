# Drift Types Interface

Pure type exports — DriftEntry, DriftNodeState, ApproveResult, DriftReport, AuditEntry, and verification result types. TypeScript definitions are the contract; read the source file directly.
