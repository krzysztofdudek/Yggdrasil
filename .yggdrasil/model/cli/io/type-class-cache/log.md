## [2026-08-02T10:19:59.753Z]
Adds a content-hash-keyed local cache for the type-level classification lattice, so an unchanged file skips re-evaluating every classifying type's predicate on the next run. Mirrors the existing AST fact cache's fail-closed shard contract and lives in io/ because core/ files may not touch the filesystem directly.
## [2026-08-02T11:40:12.004Z]
Folds the file's own repository-relative path into the cache key, and keys on raw, un-normalized byte content instead of the line-ending-normalized hash used for verdict/fingerprint hashing elsewhere. Two files with identical bytes at different paths previously shared one cache entry — since every path predicate is evaluated against the file's own path, the second file silently inherited the first's classification, in either direction (falsely covered or falsely uncovered), decided only by directory scan order, on a cold cache within a single run. A CRLF file and its line-ending-normalized LF twin collided the same way at a fixed path across an edit, because content predicates are evaluated against raw bytes while the old key normalized them first. The schema version bumps so no shard written under the old key can be misread under the new one.
## [2026-08-02T15:51:53.388Z]
Schema version moves 2 to 3. The stored meaning of closest changed in a prior commit from a declaration-order top-3 to a canonical (score, then type id) top-3, but a shard written under either of the two builds before that change is still a structurally valid v2 shard, and re-sorting its fields on a cache hit can only fix order, not repair a wrong 3-item selection the write already truncated the rest away from. Bumping orphans every such shard instead of risking one being read as though its top-3 selection were canonical when it might not be.
## [2026-09-24T01:25:08.353Z]
The classification cache kept every architecture revision it had ever seen: each shard's key folds in the architecture, so a shard from another revision can never be read again, yet nothing removed it, and every edit to a type's when added one shard per unmapped file. The cache now records which revision its shards belong to and empties itself when a different revision starts writing, so it holds one revision; returning to an earlier one costs a single re-classification.
## [2026-09-24T01:29:09.140Z]
The revision marker of the classification cache is written through the atomic writer together with the first shard of a new revision, instead of by a raw synchronous write when the cache is opened: the persistence layer writes only through the atomic writer, so a killed run can never leave a torn marker that would make the next run keep or discard the wrong revision.
