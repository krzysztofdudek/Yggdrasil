## [2026-06-12T11:33:08.168Z]
Implements the lock-store I/O adapter: readLock (synchronous, absent file returns empty lock, unrecognized version or garbled content throws LockInvalidError fail-closed), serializeLock (canonical form with code-point sorted keys at every level, one verdict/node entry per line, trailing newline — pinned by golden test), and writeLock (serialize then atomic write via the existing atomic-write helper). The LockInvalidError message follows the what/why/next structure with specific handling for git conflict markers (take-a-side procedure) and both generic recoveries (restore from git / delete and re-verify). Written additively — not yet wired into the check or fill pipelines.
## [2026-06-12T11:45:55.506Z]
Conflict-marker detection in readLock was changed from a substring check (raw.includes('<<<<<<<')) to a line-anchored regex (/^<<<<<<< /m.test(raw)). The motivation: verdict reason strings may legitimately contain the text '<<<<<<< HEAD' when a reviewer quotes merge-conflict fragments from the source being checked. A substring match would throw LockInvalidError on a valid, self-written lock file in that case. The line-anchored form matches only real git conflict markers, which always start a line and are immediately followed by a space — a JSON-escaped string value inside the file cannot satisfy this pattern. The behavioral guarantee is unchanged for actual conflict-markered files: a file with a real unresolved merge conflict still throws.
## [2026-06-13T05:33:52.523Z]
A structurally malformed lock now fails closed with a clear recovery message instead of being silently treated as empty, so a corrupted lock can never be mistaken for an absent one and quietly re-approved over unreviewed code. Detection covers all three git conflict-marker families (the opening, separator, and closing marker forms) so a lock left mid-merge is recognised as conflicted rather than parsed as data.
## [2026-06-13T09:17:41.381Z]
The error raised when the verdict lock is unparseable carries a structured what/why/next message that the command-line layer renders for the user. The engine layer must not format that message itself — formatting is the command layer's job — so the error's internal fallback text is now just the one-line headline rather than the fully-assembled message. The structured data the command layer renders is unchanged; only the engine-side string construction was removed, keeping message formatting in a single layer.
## [2026-06-14T06:55:18.212Z]
The verdict lock gained a second on-disk format version that adds a dedicated section for relation-conformance verdicts alongside the existing reviewer verdicts. The reader accepts the previous version and migrates it forward in memory by injecting an empty relation section, so every verdict recorded under the old version stays valid and nothing is re-verified on upgrade. Strict shape validation and canonical single-line serialization were extended to cover the new section and its evidence payload.
## [2026-06-14T22:16:21.708Z]
The committed verdict lock no longer carries any relation-conformance results. The architecture relation check that verifies every code dependency is declared was moved from a cached verdict to a value computed fresh on every run, so persisting its outcome in the lock would be storing a result that is always recomputed anyway and could go stale against the code. The lock reader and writer therefore drop the dedicated relation-verdict section entirely and the on-disk lock format version is advanced; the reader still accepts the previous format so an older committed lock loads without a manual migration.
## [2026-06-20T11:18:08.118Z]
Split verdict-lock storage from one file into a committed/gitignored triad. WHY: deterministic-check verdicts are a pure local performance cache — committing them added roughly half the lock's bytes and merge noise while a fresh clone can recompute them for free with no key and no model call. The store now reads three files into one unified lock and, on write, partitions verdicts by aspect KIND (deterministic vs LLM) rather than by any per-entry field: an LLM aspect that ships a companion hook also records out-of-subject observations on its entry, so kind is the only safe discriminator. Deterministic verdicts go to the gitignored cache; LLM verdicts and the per-node log/closure baseline stay committed in two separate files so log churn never mixes with verdict churn. A scoped write mode lets a deterministic-only run touch ONLY the gitignored cache, so a CI run produces zero churn in committed files.
## [2026-06-23T06:53:41.052Z]
Stop committing an empty per-node baseline file; remove it when there is nothing to record. The committed baseline file holds per-node source fingerprints and log-history baselines. When no node opts into justification-on-change and none owns a log history, that section is empty — and rather than commit an empty husk, the writer now omits the file entirely and deletes an existing empty one. Readers already treat an absent file as empty state, so this is transparent to every consumer. The two verdict files are unaffected and still write their canonical cold-start shape.
## [2026-06-23T10:28:41.686Z]
Apply the empty-section-means-no-file rule to all three split lock files, not just the per-node baseline. The committed verdict file and the gitignored deterministic cache previously wrote an empty husk when they had nothing to record; now each split file is written only when its section is non-empty, and an existing empty one is removed. Readers already treat an absent file as empty state, so a repo carries only the lock files it actually needs — no empty husks committed or left on disk.
## [2026-06-29T12:52:51.370Z]
Added a read-only content hash of the committed lock triad (the nondeterministic verdict file plus the per-node logs/closure file) for use as an attestation/provenance pin. The gitignored deterministic cache is deliberately excluded because it is absent on a fresh clone and never committed, so folding it would make the same commit hash differently on different machines; the function returns an empty string when no committed lock exists yet so a caller can say 'no committed lock' rather than show a fabricated value. This is a content-addressed digest of the committed artifact, not a re-derivation of any verdict or count.
## [2026-07-30T06:03:52.486Z]
Added a function that reads which aspectIds' verdicts currently live in the gitignored deterministic file, straight from disk, independent of the current graph. The fill stage's garbage collector needs it: when a pruned verdict's aspect has been deleted from the graph entirely, its reviewer kind (billed vs free) can no longer be read off the aspect definition, but the file its verdicts were written to still answers that honestly.
## [2026-08-07T11:58:02.123Z]
The lock's entry serializer is an explicit allow-list, not a pass-through: a field the producer sets but the serializer does not name is dropped on write, silently, and the value is simply gone the next time the entry is read. The reader is closed the same way and rejects an entry carrying a key it does not know. Both now account for the prompt size recorded on an LLM verdict — the writer emits it, the reader accepts it and refuses a value that is not a whole non-negative count, since a size that could not have come from this tool is one a check might gate the wrong way on. A round-trip test now asserts against the entry type's own field set, so a future field added without teaching the serializer fails at the gate instead of vanishing unnoticed.
## [2026-09-06T16:31:57.543Z]
The committed record gained a per-node section for port contract baselines, keyed by port and then by contract version. Its keys are rebuilt in sorted order when serialized because the serializer renders nested values with a plain stringify, which preserves insertion order — without that, the canonical bytes would depend on the order a run happened to visit ports in, and two runs recording the same facts would produce different files. The reader validates the shape strictly, like every other section, so a hand-edited or corrupted record fails loudly instead of being read as an absent baseline, which would silently un-enforce the contract it was supposed to hold.
## [2026-09-06T17:57:10.232Z]
A verdict may now carry the name of a judge who is not a configured reviewer. It is validated as strictly as every other field, because a malformed provenance would let a verdict claim an origin nothing in the repository can account for, and it is enumerated explicitly by the writer for the same reason every other field is: this serializer is an allow-list, and a field a producer sets but the writer does not name is dropped without a word.
## [2026-09-07T08:00:56.083Z]
The committed side gained a per-rule section holding one fact: the standing each rule was last seen at. It sits beside the per-component facts rather than with the verdicts, because it is remembered state of exactly that kind — never a verdict ingredient, so keeping it invalidates nothing and produces no churn in the verdict files. An older file that has no such section is read as one where nothing has been seen yet, not as a corrupt one, so a lock written before rules had a remembered standing keeps working untouched; present, the section is validated as strictly as every other, because a garbled memory of a rule's standing would make a change either go unrecorded forever or be recorded against a status nobody ever set. Its keys are emitted in sorted order for the same reason every nested value here is: the file is compared byte for byte to decide whether to write at all, so key order following the order a run visited rules in would churn a committed file for no change.
## [2026-09-07T08:29:13.188Z]
The memory of where each rule was last seen standing moved out of the committed files and into the gitignored verdict cache. Committed lock files exist only when they hold something worth committing, and they must not churn on a run that is meant to touch nothing but the local cache; a per-rule section carries an entry for every rule in the graph, so keeping it on the committed side made both of those promises false at once. The memory records what one checkout has witnessed, which is local, rebuildable state by nature, so the cache is where it belongs. The section is also written only once it holds something, so every lock file that predates it stays byte-identical.
## [2026-09-09T22:30:18.244Z]
A verdict entry gains two more optional, non-hash fields recording when it was filled and at which commit, validated and serialized the same way the existing prompt-size and judge fields already are: an explicit allow-list, so an entry that never set them serializes exactly as it did before they existed.
## [2026-09-10T09:18:39.812Z]
The lock's node-entry validator no longer tolerates a `ports` key: it goes back to accepting only `source` and `log`, so a committed entry still carrying a port-contract baseline section is refused as an unexpected key, exactly like any other unknown field, rather than parsed and read. The entry serializer's port-baseline-rendering branch is removed along with it, since the type no longer carries a field there to write.
## [2026-09-11T15:57:35.974Z]
The derived, gitignored deterministic lock no longer refuses on a content fault: it is discarded and rebuilt from scratch, with only a debug-log line. The committed files keep the old fail-closed refusal, because they are the source of truth and nothing can rederive them. The reason for the split is that the derived file holds no truth to protect — an empty section reads as unverified, which is still fail-closed — while a refusal over it costs the entire gate. And the way it actually fails is not corruption but version skew: one yg writes a section a slightly older yg on the other side of a container boundary does not yet allow, and the older one refuses to start. That was observed twice on 2026-09-11, when a 5.9.0 host wrote the aspects section and a 5.8.0 container rejected it as an unexpected top-level key. Every later widening of this schema would reproduce it against whatever skew exists then, and some skew between host, container and CI is permanent. Only the content verdict is tolerated; a real I/O failure still propagates from both kinds of file, because that is an environment fault to fix rather than a cache to rebuild.
