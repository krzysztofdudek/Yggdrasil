## [2026-06-29T05:24:52.926Z]
Introduced a single read-only gateway between the read-only audit portal and the engine. Previously the portal pipeline declared direct dependencies on a dozen separate engine subsystems — graph loading, the check runner, lock verification, pair computation, the relation analyzer, the suppression scanner, issue grouping, the effective-aspect cascade, reviewer-tier selection, and the log reader. That spider of edges meant any change to an internal subsystem could ripple into the portal, and there was no single place that guaranteed the portal stays read-only. This module is now the ONE place the portal reaches the engine: it wraps every needed call behind a clean read-only surface, loads the graph committed-only so secrets are structurally unreachable, and imports no lock writer. Concentrating the coupling here lets the read-only invariants (no secrets, no lock writer, committed-only load) be enforced mechanically on this one node, and lets the rest of the portal depend on just this facade plus the data contract.
## [2026-06-29T12:36:31.021Z]
Added three read-only provenance/freshness functions to the single engine seam: a content hash over the committed lock triad (nondeterministic verdicts + per-node logs baseline; the gitignored deterministic cache is excluded so the hash is stable across machines for one commit), a git HEAD commit-ref reader that follows .git/HEAD to its loose or packed ref and reports nothing rather than a fabricated ref outside a git tree, and a per-node source-freshness comparison against the committed fingerprint baseline. Freshness fires only where a committed baseline exists, because the engine records a source fingerprint only for log-required node types; a missing baseline is the normal case and must never be read as a change, so the portal cannot paint the whole repo unverified from absent baselines. The two cases a missing baseline leaves uncovered are already honest elsewhere — a node with reviewer pairs flips them to unverified on any input change, and an unguarded node is already its own distinct non-green state — so this signal adds exactly the one remaining case: a node whose green attests specific committed bytes that have since been edited.
## [2026-07-03T06:25:39.504Z]
The shared suppression scan used by both the audit command and the live portal inventory now treats any mapped node source as an eligible waiver site regardless of file extension. The prose and documentation exclusion is scoped to unmapped files only, because the enforcement path honors a real marker in any mapped file — including mapped documentation or a meta-model document — so excluding such files by extension would let a live waiver be honored yet stay invisible to the inventory. Both callers now derive the mapped-source set from the graph and pass it into the scan, so the honoring path and the audit path agree on where a waiver can live and no file can be a live waiver site while being absent from the inventory.
## [2026-07-03T07:33:08.389Z]
The shared file-eligibility rule that decides which files can hold a honored suppression waiver — and therefore which files the audit inventory must list — was moved into its own focused unit, separate from the suppression-scan logic. Bringing the honoring path and the audit path onto one eligibility rule had grown the scan file past its focused-size boundary; splitting the rule out keeps each file to a single responsibility while preserving the invariant that no file can be a live waiver site while being invisible to the suppressions inventory.
## [2026-07-05T08:55:06.870Z]
The git commit-ref reader assumed .git is always a plain directory under the repo root. A linked development worktree checks out the repository with .git as a one-line pointer file to a private per-worktree git directory (which itself points back to the main repository's shared refs), so the reader silently found nothing and reported no commit ref at all whenever verification ran from such a worktree. The reader now recognizes the pointer-file form, follows it to the private git directory for the worktree's own HEAD, and follows that directory's pointer back to the shared location for branch refs, so the reported commit ref is correct and non-fabricated regardless of whether verification runs from the main checkout or a linked worktree.
## [2026-07-05T18:15:46.597Z]
The audit that inventories active suppress markers must waive exactly what the reviewer waives — never more, never less — or it would either hide a live waiver or invent one that has no effect. For Markdown files, a marker inside a fenced code block is a documented example rather than a live waiver, so the audit now excludes fenced lines using the same fence detector the reviewer-honoring path uses. This required giving the raw-line audit scan the file path so it can recognize a Markdown file among other grammarless files, since only Markdown gets fence handling. Keeping both paths on one shared detector is deliberate: two separate implementations would drift, and a divergence here means the audit and the reviewer disagree about which waivers are real.
## [2026-07-10T08:10:35.938Z]
The suppression scan now flags a waiver that silences a rule labeled as only ever firing on provable violations. Such a rule has no false positives by design, so waiving one is almost always either a mislabel or a real problem worth re-examining. The warning is advisory and the inventory still reports cleanly; the set of such rules is supplied by the caller so this backend stays free of graph-policy decisions.
## [2026-07-10T12:40:56.304Z]
The suppression inventory now classifies a whole-file waiver — an unclosed disable marker whose position falls within the first five non-empty lines of a file — as "file-level", listing it under that label and no longer emitting the "Unbounded range" warning for it. That warning was meant to catch a disable marker whose closing enable was forgotten, but it fired on every unclosed disable, including the sanctioned top-of-file form used to intentionally exempt an entire file; the deliberate whole-file waiver and the accidental forgotten-close were indistinguishable. An unclosed disable placed lower in the file still warns, because there it usually is a forgotten close. The open-disable pairing that decides which disables are unclosed is reused to make this classification, keeping that pairing in a single place. The honored suppression line ranges are untouched — this changes only how the inventory labels a marker and whether it warns.
## [2026-07-10T14:39:30.529Z]
Added a read-only accessor that runs the dependency-resolution pass once and returns the full set of statically-detected cross-node code edges (source component to the set of components it depends on), verbatim and before any declared-versus-detected filtering. This exists so a caller that is not allowed to reach the dependency-resolution layer directly can still obtain the detected half of the structural dependency picture through this single read-only facade. It reuses the exact same pass invocation the boundary computation already performs, writes nothing, touches no verification state, and changes no verdict logic; on a genuine parse failure it returns nothing so the caller can fall back to declared relations only rather than fabricate edges.
## [2026-07-10T18:12:04.488Z]
Aligned the machine-readable suppression inventory's risk labeling with the whole-file-waiver classification used by the human-facing inventory. A bare disable marker with no matching enable, placed within the first few non-empty lines of a file, is the sanctioned way to waive an entire file; the human-facing inventory recognizes that position and labels it a deliberate whole-file waiver, drawing no warning. The data-feed adapter, however, resolved risk independently and still tagged the very same marker as an unbounded (open-ended) range, so the two surfaces contradicted each other on identical input. The adapter now consults the same file-head classification the human-facing inventory renders from — a single shared signal rather than a second, forked rule — so a top-of-file whole-file waiver carries no risk on either surface while a genuinely mid-file unclosed waiver is still flagged as unbounded on both. This is a risk-labeling and reporting alignment only: the resolved line ranges that the reviewers actually honor are unchanged, exactly as before.
## [2026-07-13T14:06:28.233Z]
The portal reaches the engine only through this single read-only gateway, so the new structure panel could not import the structural-metrics core directly without breaking that single-seam guarantee. The gateway therefore now re-exports those pure dependency-analysis functions (the same ones the command-line structural dashboard uses), letting the extraction pipeline reuse the exact analysis while still touching the engine in one auditable place; the gateway already depends on that layer, so this adds no new coupling. The gateway also now flattens the full set of statically detected cross-component dependencies, which the live boundary computation already holds as a map of sets, into plain sorted arrays at this boundary before they can reach the serialized payload, because a map does not survive JSON serialization. Surfacing that already-computed set here is what lets the structure panel be derived without a second dependency parse.
## [2026-07-13T22:29:35.716Z]
The suppression scan gained a resolver that maps each live suppress waiver to the verification units it covers, keyed by rule — the coverage input the rule-health false-block signal joins against past refusals. For a non-wildcard, non-terminator waiver in a file, the file unit is covered, and so is every node whose review surface includes that file: the file's owning node and each of its ancestors, since a whole-component rule on an ancestor reviews that file too. A wildcard waiver silences every rule and is deliberately never attributed to one, mirroring the per-rule waiver count so false-block attribution stays precise. It reuses the shared file-to-node ownership resolution the relation and coverage layers already use, so a waiver's file maps to a unit exactly as the engine maps it — no second, drifting notion of ownership. Placed here beside the scan because interpreting waivers against the graph is the scan's concern, and this layer is already the single audited seam permitted to reach the ownership index.
## [2026-07-13T23:15:55.422Z]
Documented the granularity limitation of the waiver-to-unit coverage resolver used by the rule-health false-block signal. Coverage is resolved at file and component level, never per line: a waiver anywhere in a file marks that file's unit and its owning and ancestor component units as covered wholesale. The consumer that joins these covered units against past refusals can therefore, in the rare coincidence where a same-rule waiver and an unrelated refusal fall on the same component, over-count that unrelated refusal. This is bounded, carries a small-sample honesty label downstream, and never affects whether a build passes, so it is disclosed in the resolver's documentation rather than removed — precise line-level attribution would require refusal records to carry a line number, which is deliberately not added for a signal that never gates.
## [2026-07-15T09:46:50.178Z]
The language-registry module — the pure extension-to-grammar lookup table with no imports of its own — was relocated from the core layer to the utility leaf so that lower layers (AST parsing, relation extraction, structural checks) no longer declare a dependency that points back up into core. This node's import path to that module was updated to the new leaf location. Pure structural relocation: no behavior changes, the lookup table and its helpers are byte-identical.
## [2026-07-20T04:43:34.029Z]
Two fidelity fixes for the local read-only web view of the graph.

The web view's issue set, warning counts, and worklist must match what the command-line check reports. The view was asking the engine for its issues without supplying a clock, which causes the engine to skip the review-cadence check, so review-overdue reminders were missing from the view and its warning count was undercounted. The view now supplies the wall clock the same way the command-line boundary does, so the two agree exactly.

Reading the current commit reference must work on repositories that use the newer, longer object-hash format, not only the classic one. The reference-reading logic matched only the classic forty-character hash across detached-head, loose, and packed reference forms, so on a newer-format repository it failed to resolve the commit. It now accepts both hash lengths in every one of those forms.
## [2026-07-24T12:09:10.255Z]
The portal's read-only reuse of the full check now supplies a real wall clock the same way the primary command-line entry point already does; without it, a whole category of time-based warning was silently absent from the portal view, so the portal would show fewer problems than the command line for the identical repository state, undermining its purpose as a faithful mirror. Separately, the portal's own reading of which commit is currently checked out was widened to recognize the longer commit-identifier format some repositories now use in addition to the shorter legacy one, so a repository that has moved to the newer format does not silently lose its commit-reference display.
## [2026-07-24T13:37:05.509Z]
This facade must supply the check engine with every boundary input the command-line surface supplies, not merely the ones it happened to need first. The engine deliberately skips a boundary-injected check when its input is missing rather than fabricating one, so an input this facade omits is not a smaller report — it is a silently absent finding. That is how a project with a drifted, hand-edited, or entirely absent agent-rules install came to warn on the command line while the browser showed nothing at all, which inverts the one guarantee the web view is for: it must never read greener than the gate.

The committed rules snapshot is therefore read here, alongside the wall clock, and both are read rather than accepted as parameters so that no caller can leave one out. The project root is derived from the graph itself, the same way the engine derives it, so there is no second place for the two to disagree.

The declared ceiling on this node's direct dependencies was raised by one to admit the shared reader. That ceiling exists to make each new dependency on this seam a deliberate, recorded decision, and this one is: the alternative — a private copy of the same read living inside the portal — is precisely the drift the ceiling is meant to surface, because two readers can disagree about what was installed while one cannot.
## [2026-07-27T14:26:51.561Z]
Supplies the portal's read-only check with the same real git-tracked-file data the command-line check now uses, so the dashboard reports the same file-visibility anomaly instead of reading cleaner than the command line for the identical repository state.
## [2026-07-28T19:46:11.774Z]
The portal's pair-count helper now accepts the same file-type classification the CLI's own check can be given, so a future caller can keep the portal's counts from disagreeing with the command line's.
## [2026-07-30T07:13:52.748Z]
Adds a facade that classifies type coverage once per portal extraction and exposes both the full result and its reduced lock-verification shape, so every portal call that needs the classification shares one computation instead of each answering about a component-only universe.
## [2026-07-30T21:02:19.792Z]
Reworded a comment on the portal's once-per-extraction type-coverage classification so it explains, in its own words, why the classification is computed once and shared across every consumer, instead of leaning on a short internal code with no meaning to a reader of this repository.
## [2026-07-31T04:22:18.827Z]
The live suppression inventory now accepts the caller's type-coverage classification and threads it into the shared suppress scan, so a file covered by the architecture type-level lattice is recognized as a live waiver site exactly like a mapped node source, matching the same fix made to the yg suppressions command.
## [2026-07-31T05:51:17.350Z]
The suppression scan drew its candidate file list from a plain repo walk, which structurally excludes the top-level .yggdrasil/ directory and any gitignored path. A marker on a file a node maps to but the walk could not see was still honored by the deterministic runner, so it stayed invisible to yg suppressions, yg advise, yg aspects --health, and the portal at once. The scan now draws its candidate list from the union of that walk and every node's mapping entry expanded through the same resolver the runner itself uses to build a node's own reviewed file set, with any nested graph's own subtree excluded from that expansion the same way the walk already excludes one, so the audit surface and the enforcement surface agree on exactly which files can host a live waiver.
## [2026-07-31T07:06:24.423Z]
The suppression-marker audit's mapped-file universe now calls the same shared nested-graph-aware expansion the enforcement side uses, instead of hand-composing the expansion and the exclusion itself, so the audit and the runner it audits can never disagree about which files belong to a nested checkout.
## [2026-07-31T08:26:09.677Z]
Exposed a facade wrapper that drops the per-run cache of on-disk separate-project boundaries, so the extraction pipeline can force a fresh read of that state on every refresh instead of reusing whatever a long-lived server process cached on its first request.
## [2026-07-31T12:04:36.890Z]
The portal's live suppression inventory needs the adopter's coverage.excluded config threaded into the same scan the CLI's own suppression audit now receives it in, so the portal's waiver list agrees with the CLI rather than drifting from it the moment a project excludes a path by config instead of by nested-project boundary.
## [2026-07-31T18:00:26.142Z]
The portal's boundary computation builds its path resolver from the same exclusion-guarded owner index the live relation pass itself uses internally, instead of a raw one, so a Go or Java package's excluded member can no longer make the portal's phantom-dependency and detected-edge views miss an edge that the package's other, non-excluded files still legitimately carry.
## [2026-07-31T23:26:12.052Z]
The node-attestation panel used to show only the number of mapping declarations a node carries, never the real number of files those declarations resolve to on disk — a directory entry covering hundreds of files and one covering a single, already-excluded file both looked identical. This facade now exposes a per-node real file count, reusing the same exclusion-aware, child-carve-out-aware file expansion the node's own source fingerprint already computes, so the new number can never diverge from what the engine already considers this node's real mapped source.
