## [2026-05-15T17:44:12.755Z]
Phase 2: split from cli/ast — loader hook, parser, check runner, suppress. Type: ast-adapter.
## [2026-05-15T19:28:49.972Z]
Move IssueMessage type from formatters/message-builder to model/validation — engine→formatter import violation fix for boundaries enforcement
## [2026-05-26T09:43:17.807Z]
Add find-comments.ts reading comment node types from language registry per ctx.language. Two discriminated forms: file ({ ast, language }) or subtree ({ rootNode, language }). Both present throws AST_FINDCOMMENTS_AMBIGUOUS_TARGET; unknown language throws AST_FINDCOMMENTS_UNKNOWN_LANGUAGE. Errors propagate through runner's AST_CHECK_THROWN path. Task 28 reassigns to cli/ast/report alongside walk/file-path/index.
## [2026-05-26T09:50:11.164Z]
Add AST_CHECK_FILE_NOT_IN_CONTEXT runtime guard. check.mjs cannot synthesize violations for files outside ctx.files (protects suppress mechanism — markers cannot reach unknown files). Throws via existing AstRunnerError; cli/approve.ts catch (added in Task 3) flows it through errorSource: 'astRuntime'.
## [2026-05-26T10:52:26.554Z]
Remove find-comments.ts from mapping — this file was reassigned to cli/ast/report as part of the public API consolidation. The runtime node now only contains: loader hook, parser, check runner, and suppress marker extractor.
## [2026-05-28T10:07:11.726Z]
Added optional ParseCache parameter to RunAstAspectParams and runAstAspect. When provided, the cache is checked before reading and parsing each file — if the file is already in the cache, the cached content and AST are reused directly. After parsing, the result is stored in the cache. This avoids redundant file reads and re-parses when the same source files are visited by multiple AST aspects in a single dry-run or approve invocation. The ParseCache type is exported for use by CLI-layer callers.
## [2026-05-29T06:40:35.395Z]
Extract the ParseCache type alias from the AST runner into a small neutral module so the upcoming structure runner can share the same per-invocation cache shape. Runner re-exports the type for callers that import it from the AST entry.
## [2026-05-30T07:12:18.331Z]
The four check.mjs export-shape guards — named export present, not a default-only export, is a function, takes exactly one argument — now come from one shared validator so the AST and structure runtimes cannot drift apart in what they accept or in the wording of the error they raise. Error codes are unchanged; the only text change is a trailing aspect-id suffix on the default-export message.
## [2026-05-30T14:09:01.061Z]
The per-aspect language declaration was removed from the system. An aspect that ships a parsed-tree check used to carry a `language:` list naming the languages it targeted, and the system validated that list with four dedicated checks. Nothing in the runtime ever read it: the engine already determines each source file's language from its file extension through one shared registry, then loads the matching grammar. The declaration was therefore inert metadata that could silently disagree with what the engine actually parsed — an authoritative-looking field that governed nothing.

This change deletes the field, the validators that policed its shape, and the documentation and tests that described it, and promotes the extension-to-grammar registry to the single authority for matching a file to its parser. The motivation is to remove a confusing mismatch between what an aspect claimed about languages and what the engine did, and to collapse the duplicated extension-knowledge that had drifted into more than one place.

The language a parsed-tree check sees for a given file is now derived solely from that file's extension; an unrecognized extension yields no parsed tree rather than a per-aspect error. The drift identity of a graph-aware structural check is deliberately held stable across this change so existing approvals are not invalidated by metadata that never affected behavior.
## [2026-05-31T10:45:42.357Z]
Made the parser locate its grammar binaries correctly in a published install. The grammars are bundled with the package, but two layout assumptions were wrong: the binaries were copied under a short name the parser never looks up, and the directory the parser searched was computed one level too high for the flattened published layout — so a real install found no grammar and fell back to a build-only dependency that ships only to developers, failing with an opaque module-not-found. The parser now searches the directory beside the bundled entry first, keeps the previous location as a fallback, and degrades to a clear could-not-find-grammar message when no grammar package is present, instead of leaking a raw require error. This is what makes the local-language checks usable by an adopter rather than only in this repo's own test runs.
## [2026-06-03T07:05:07.509Z]
Suppression-marker scanning now resolves comment nodes per source language instead of assuming one comment syntax. Different grammars label comments differently, so a single hard-coded node type silently missed markers in some languages, letting suppressed violations slip past or unsuppressed ones get hidden. Resolving the comment node kind from the active language makes yg-suppress detection correct across every supported grammar.
## [2026-06-08T19:22:20.137Z]
Inline suppression markers must be recognized wherever an author can legitimately place them, including inside a block comment that spans several physical lines. The marker-matching expressions previously anchored the end-of-text without multi-line semantics, so a marker sitting on any line other than the very last of a multi-line comment was silently ignored — the documented multi-line and bracket-disable placement forms did not actually take effect. Matching is now line-aware so a marker is found on whichever line it appears, keeping the parser behaviour consistent with what the suppression documentation promises authors.
## [2026-06-08T19:56:24.041Z]
A deterministic check that only inspects file text must be able to see every file in a node's mapping, including files whose extension has no parser (documentation, shell scripts, JSON, and the like). The single-file deterministic runner previously parsed every file up front and aborted the entire run on the first file it could not parse, with a message that told the author to delete exactly the file the check was meant to read. That contradicted the promise made to check authors that content-only rules may iterate non-parseable files, and it disagreed with the graph-aware runner used elsewhere, which already hands such files to the check with no parse tree attached. The runner now treats a file with no registered grammar as non-parseable: it is still delivered to the check with its text and an absent parse tree, so text and pattern rules run over it, while a genuine failure to load a grammar that should exist still fails closed. Suppression markers, which are discovered through the parse tree, simply contribute nothing for a file that has none.
## [2026-06-08T20:29:14.366Z]
Inline suppression waivers must be honored in every source language, not only those with a parser. Markers were previously discovered solely by walking a file's parse tree for comment nodes, so a file whose extension has no registered grammar yielded no waivers at all. Once content-only deterministic checks began running over such files, an author flagged in, say, a SQL or shell file had no way to record an approved exception there — the escape hatch was missing for exactly the files that needed it. Suppression collection now falls back to a language-agnostic scan of the raw file text when there is no parse tree, keying on the distinctive suppression token rather than on any particular comment delimiter, so a waiver works regardless of language. Files that do have a parser keep using their comments, which keeps the token from being mistaken for a marker when it merely appears inside a string. The collector accordingly receives the file text alongside the optional tree.
## [2026-06-08T22:08:23.047Z]
Loading a parser grammar must be safe when many checks run at once. The one-time initialization of the parsing runtime and the loading of each language grammar were previously gated by a flag and a cache that were only set AFTER the awaited work completed. When several deterministic checks ran concurrently — as they do when approval verifies multiple targets in parallel — they could all observe the "not ready yet" state in the same window, all kick off their own initialization or grammar load, and one of them could pick up a grammar object that was still only half-loaded, which the parsing library reports as an incompatible language version. The symptom was a deterministic check spuriously failing in a batch while the very same check passed when the target was processed on its own. The fix records the in-flight work as a shared promise the moment it starts, so every concurrent caller awaits the same single initialization and the same single grammar load; a load that fails is removed so a later attempt can retry rather than inheriting a cached failure. Determinism is the whole point of these checks, so a concurrency-dependent failure had to be eliminated rather than worked around by serializing approvals.
## [2026-06-13T10:37:55.841Z]
A diagnostic message shown when a deterministic check crashes told the agent to re-run a standalone approve command that no longer exists. The only verdict-writer now is the unified check-with-approve step, so the suggested next action was corrected to point there. Text-only fix to the agent-facing next-step guidance.
## [2026-06-13T11:23:06.992Z]
The inventory scan that lists active suppression markers matched marker syntax anywhere in a file's raw text, including inside string literals — so source that merely quotes or talks about the marker syntax (tests, templates) was falsely counted. A new comment-only scan walks the parsed comment nodes (the same comment extraction the marker-honoring path already uses) so the inventory counts a marker only where it would actually take effect. The marker-honoring path was already comment-aware and is unchanged; this only corrects the inventory's accuracy.
## [2026-06-21T16:25:13.099Z]
A helper now expresses the set of waived line ranges for a given rule in a form that can be shown to a model reviewer, so a waiver covers exactly the same lines whether the rule is enforced by a model or by a local check. The marker-matching logic itself is unchanged.
## [2026-06-24T10:23:02.883Z]
Fix WASM heap exhaustion that caused Aborted() crash on large repos.

web-tree-sitter Parser and Tree objects are allocated in the WASM heap and require explicit .delete() to release memory — JavaScript GC does not free them. On repos with many files and aspects, accumulating hundreds of unreleased parsers and trees exhausted the heap, triggering an abort() call from within the WASM runtime that surfaced as an unclassified Aborted() error after all pairs were verified.

Two changes: (1) Parser objects are now cached per language rather than created fresh on every parseFile() call. Language objects were already cached; parser creation is synchronous once the Language is loaded, so one instance per language is safe to reuse across sequential parse() calls. (2) A destroyParseCache() helper was added to parse-cache.ts that iterates all entries and calls ast.delete(), clearing the map. Both ast/runner.ts (locally-parsed trees when no external cache is provided) and structure/runner.ts (locally-owned parse caches) now call this on exit via try/finally so cleanup runs on every code path including exceptions.
## [2026-06-25T15:24:58.777Z]
Fixed two WASM tree lifecycle bugs in the AST runtime layer.

In runner.ts, a Tree created by parseFile() was registered for local cleanup (localTrees.push) after a hasError guard that could throw AstRunnerError. If that guard fired, the tree was never pushed and therefore never deleted by the outer finally block, causing a permanent WASM heap leak. The fix moves localTrees.push to immediately after parseFile() succeeds, before any conditional branch that might exit early.

In parser.ts, added withParsedFile — a new exported function that parses a file, calls a callback with the resulting Tree, and guarantees tree.delete() in a finally block. This is the canonical WASM-safe API for bounded parse operations. All callers that do not need to transfer tree ownership should use withParsedFile instead of parseFile directly.

The root cause across all related bugs was that parseFile() returns a WASM-heap Tree whose lifecycle is invisible to the JavaScript garbage collector. Every call site had to manually remember to call .delete() inside a finally block — a discipline that cannot be enforced by the type system and was repeatedly forgotten. withParsedFile eliminates the manual discipline by encoding the lifecycle guarantee in the API itself.
## [2026-06-27T14:52:16.341Z]
Added a way to fingerprint each tree-sitter grammar by hashing its compiled WebAssembly binary, derivable without invoking a parse. This exists so the relation-analysis layer can fold the exact grammar version into its per-file cache key: identical source bytes can parse to a different syntax tree under an upgraded grammar, silently changing the extracted dependencies, so an unchanged file must be re-analysed whenever the grammar changes. The grammar's declared version metadata is unreliable (those registry fields are empty placeholders), so the binary content hash is the only trustworthy version signal. It is computed eagerly because a run that serves every file from cache never loads a parser at all, so a lazily-derived hash would never form and a grammar upgrade would go unnoticed.
## [2026-07-02T14:48:36.225Z]
Suppression markers are now recognized only in an anchored position: the yg-suppress / yg-suppress-disable / yg-suppress-enable token must be the first token of its comment line, preceded by at most one comment-delimiter token from a fixed whitelist, and every listed aspect id must be either the wildcard or a plain path-like identifier. Previously the token was matched anywhere inside a comment and the id could be any text, so ordinary documentation prose that merely mentioned the syntax was treated as a real waiver: a mid-sentence mention of a wildcard suppression directly above a violating line silently waived an enforced violation, a reasonless prose mention aborted verification of the affected pair with an error blaming the wrong component, and the audit inventory listed garbled phantom waivers with bogus warnings.

Anchoring closes the silent-waiver hole in the safe direction — the failure mode of the stricter grammar is more enforcement, never less. The reviewer-honoring path and the inventory scanners now share one matcher, so the list of auditable waivers is exactly the set of waivers that can take effect. Marker positions inside multi-line block comments are now stamped at the marker's own row rather than the comment's first row, eliminating a divergence where the honored line differed from the inventoried line. Markers that relied on the loose grammar (mid-sentence mentions, quotation-style tokens behind doubled delimiters, or tokens following code on a raw-scanned line) intentionally stop being honored, so any violation previously waived by such a token resurfaces at its next re-verification.
## [2026-07-03T06:25:21.950Z]
Hardened the shared suppression-marker matcher against three correctness gaps found in review. A marker carrying a reason in a file authored with Windows line endings was silently ignored, because the line scanner split on the newline but left the carriage return glued to the end of the reason; the matcher now strips a trailing carriage return before matching, so a waiver behaves identically regardless of line-ending style. When scanning a file whose language has no parser (documentation, SQL, shell), the leading comment delimiter is now mandatory: a bare line that merely begins with the marker token — a wrapped prose sentence or a string-literal line — no longer counts as a live waiver, closing a path where ordinary documentation could accidentally or deniably waive an enforced rule; a real comment marker with a delimiter still works. A trailing block-comment terminator is stripped from the reason only when it matches the comment style that actually opened the line, so a line-comment reason that legitimately ends in such characters is preserved. Inverted line spans (an enable immediately after its disable, or a disable on the last line of a file) are no longer emitted, so no impossible range reaches a reviewer. Finally, a reasonless marker now raises a distinct malformed-marker signal that names the marker and its location, rather than a generic parser error, so the failure is attributed to the marker rather than to the rule checker that merely read the same file.
## [2026-07-05T18:15:43.801Z]
A suppress marker written inside a Markdown fenced code block (backtick or tilde delimited) was being honored as a live waiver. Markdown has no code grammar, so its markers are found by scanning raw lines, and that scan could not tell a documented example from a real comment — a page merely showing the suppress syntax silently waived that rule on the line beneath it. Fenced lines are now recognized and skipped before marker matching, so an example inside a fence is inert. The rule for authors is that a genuine Markdown waiver must be an HTML comment placed outside any fence. Any ambiguity in detecting a fence resolves toward enforcement: an unclosed fence is treated as running to the end of the file, so a marker is at worst skipped, never honored over code no human approved. The fence detector is a single shared routine used both by the path that actually waives a rule and by the path that audits active waivers, so the audit can never list a marker the reviewer would not honor, nor hide one it would.
## [2026-07-05T19:17:32.430Z]
Made the Markdown fenced-code-block mask that renders documented suppression examples inert follow CommonMark's fence-closing rules exactly. A suppression marker written inside a fenced example in a docs file must never be honored as a live waiver; the mask exists solely to neutralize such examples. The prior mask compared only the fence delimiter CHARACTER, so it mistook two things for a closing fence: a shorter run of the same character nested inside a longer opener (a three-backtick line inside a four-backtick block), and a fence line carrying an info string (a language tag). Either mistake ended the mask early, exposing a marker written between the inner delimiters as a real, honored waiver — the exact laundering hole the mask was built to close. Close detection now follows CommonMark: a closing fence must use the same character, run at least as long as the opener, and carry no info string (trailing text whitespace-only); anything else is interior content that keeps the fence open. Every rule only ever makes a fence stay open LONGER, preserving the fail-toward-enforcement invariant — an ambiguous fence can only suppress a marker, never leak one out.
## [2026-07-10T12:40:49.557Z]
The shared suppression-marker scanner now records, per marker, whether it sits within the first five non-empty lines of its file. This positional fact exists to let the inventory distinguish a deliberate whole-file waiver — a bare disable placed at the top of a file, which intentionally runs to end-of-file — from a bare disable buried lower in the file, which usually means its closing enable marker was forgotten. Blank and whitespace-only lines do not count toward that five-line budget, so a marker after a shebang or a short header comment still reads as top-of-file. The signal is computed identically for both the comment-scanned (registered-grammar) path and the raw-line (non-grammar) path so the two cannot diverge. It is a classification input only: it does not change which lines any reviewer waives, so the resolved suppressed ranges are byte-for-byte unchanged.
## [2026-07-11T03:28:01.077Z]
Added an opt-in graph-access sentinel to the single-file check runner so a graphless "re-run a rule over example files" mode can tell apart two failure kinds that otherwise look identical: a check that legitimately needs whole-graph context (node/graph/fs/parseYaml) versus a check with a real bug. When the sentinel is enabled, reading any graph-context accessor throws a distinct, trappable error that the caller reclassifies as an unsupported capability gap (recorded, never scored) instead of a generic runtime error. The sentinel is opt-in and never enabled on production verification paths, so for every normal run the context object is unchanged and behaviour is byte-for-byte identical to before — a regression test pins that the same graph-reading check still surfaces as a runtime error when the sentinel is off.
## [2026-07-12T07:23:27.678Z]
The deterministic-check harness that backs the interactive rule-exercising
and single-rule diagnostic commands used to abort a case whenever the subject
file could not be parsed cleanly by its grammar — a syntax error, or a raw
control byte that corrupts the parse. The production verification path never
does this: it parses best-effort and hands the check whatever tree the parser
produces, so a content-only rule (one that inspects the raw file text and never
the syntax tree — for example a scan for raw control bytes) refuses a broken
file cleanly at the gate. The harness aborting on the parse error meant such a
rule could not be exercised against a broken same-extension fixture: the harness
recorded the case as "could not evaluate" while the gate recorded it as
"refused" — a fidelity gap between two surfaces that are meant to reach the same
verdict on the same input.

The harness now mirrors the gate. It delivers the parsed tree best-effort even
when the tree carries parse errors, so a content-only rule runs identically in
both places, and a rule that does consume the tree receives the same error-laden
tree the gate would hand it — introducing no new divergence. Preserving that
harness-equals-gate equivalence is the invariant this runner exists to uphold:
a rule author must be able to trust that a case exercised in the diagnostic
surface behaves exactly as it will in the enforcing one.
## [2026-07-15T09:46:48.865Z]
The language-registry module — the pure extension-to-grammar lookup table with no imports of its own — was relocated from the core layer to the utility leaf so that lower layers (AST parsing, relation extraction, structural checks) no longer declare a dependency that points back up into core. This node's import path to that module was updated to the new leaf location. Pure structural relocation: no behavior changes, the lookup table and its helpers are byte-identical.
## [2026-07-19T14:12:13.999Z]
The Markdown fenced-code mask that shields documented suppression examples from being read as live directives failed on files authored with carriage-return line endings. The fence-detection regular expression anchors its trailing run to end-of-line, and neither the any-character class nor the end anchor spans a carriage return, so a fence delimiter followed by a carriage return never matched. On such a file the mask came back empty and every suppression marker written inside a code example — the kind that appears in rule-authoring documentation — was honored as a real waiver, silently switching off enforcement for the line beneath it. A repository edited on a system that uses carriage-return line endings, mapping any such Markdown file, would quietly lose enforcement wherever an example marker sat. The mask now strips a trailing carriage return before matching, exactly as the marker scanner already does, so a carriage-return-authored file masks identically to a newline-authored one and an example can never leak out as a live waiver.
## [2026-07-20T04:43:27.845Z]
Two independent fail-safe hardenings landed in the single-file check runtime.

The graphless replay path runs a deterministic rule over case files only, with no graph context available. When such a rule reaches for any graph-context accessor, that must be recorded as unsupported for a graphless replay (noted, not scored), rather than surfacing as a thrown, misclassified failure. The set of trapped accessors had drifted behind the real check-context contract, so a rule touching one of the newer context members was wrongly classified as an unrun error instead of unsupported. The trap now mirrors the full context contract — everything except the case files themselves — and the operator-facing wording lists the complete set. Whenever the check-context contract gains a member, it must be added to this trap in lockstep.

Suppression-marker masking in Markdown must never honor a marker that appears inside a documented code example; only real waivers in live prose count. Two example forms previously leaked through: a fenced block indented more than three spaces (as happens inside a list item or blockquote), and a plain four-space or tab indented code block, which carries no fence syntax at all. Both are now masked. The guiding invariant is to fail toward enforcement: over-masking can at worst skip a genuine marker, but can never let a fenced example act as a live waiver.
## [2026-07-25T07:06:59.315Z]
A waiver marker must carry a justification, and that requirement turned out to be reachable around. The reason is taken from whatever follows the rule id on the marker's line, with a trailing block-comment terminator removed first, because a terminator is comment syntax rather than something the author wrote. Only one of the two terminators that the HTML comment syntax actually permits was recognised. Written the other legal way, the terminator survived into the reason — so a marker carrying no justification at all came out with a non-empty reason consisting of that leftover punctuation, and passed the emptiness check whose whole purpose is to force a human to state why a rule is being waived. A waiver that nobody had to justify is the one thing this mechanism must not allow, which is why this counts as more than cosmetic.

The fix recognises both terminators. It is deliberately narrow rather than a permissive sweep: the removal stays aware of which delimiter actually opened the marker's comment, so a line-comment justification that legitimately ends in punctuation resembling a terminator still keeps its text. Widening the match without that constraint would trade one silent loss of author intent for another.
