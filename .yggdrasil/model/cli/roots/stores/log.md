## [2026-08-19T09:24:18.974Z]
Lands the roots store: generic model.json header/body I/O (writeModel/readModel), the I2a RootsModelHeader type, the rootsVersion schema constant, a typed seeds.jsonl reader, and a per-store-file content hash used to compute the header's seedsHash/decisionsHash/ledgerHash fields. Canonical-JSON (deep-sorted keys) plus atomic temp-file-then-rename writes, mirroring the committed lock triad's own discipline.
## [2026-08-19T09:52:54.883Z]
Hardened the model reader and completed the store layout: reading a snapshot written under a different schema version now fails loudly naming both versions instead of silently misreading fields, the gitignored derived-cache and runtime-state directory paths are now exported by the one module that owns the store layout so later work cannot scatter them, and the three missing-file read paths route through the shared missing-file helper instead of three hand-rolled copies.
## [2026-08-20T13:56:11.559Z]
The mined model already reads maintainer-authored seed priors from a committed store; it now also needs to read hook-shaped marks recorded against specific scopes and surfaces, so that later mining work can cap a fact that roots itself once nudged into existence rather than letting that fact silently reinforce its own recommendation. This adds a reader for that committed mark log with the same tolerant, skip-the-bad-line behavior the seed reader already has, plus the path helpers that locate the new derived-cache subdirectories a resumable, history-aware index will write into — a sharded blob cache, a replay-state directory, and a build lock file, all beside the existing cache root.
## [2026-08-21T09:41:42.794Z]
The schema-version comment at the top of this store now states, in place, why the mined model's body can gain optional history-fed fields and lose the coverage/debt keys without a version bump: nothing has ever shipped a v1 model.json to an adopter, so no committed snapshot exists that a version-gate check could disagree with, and every reader already treats each of those fields as independently optional rather than assuming a fixed set. The version gate itself now says the same thing at the point where it actually fires, so a later reader hitting it during a real schema disagreement has a record of why past body-shape changes never tripped it.
## [2026-08-23T12:00:13.630Z]
The two append-only store record shapes this node reads now come from a sibling module that lives beside this node's own engine counterpart rather than the shared graph-types module; the shared module still re-exports both shapes unchanged for every other importer. This node's own former dependency on the shared graph-types module is retired, since nothing here reads it any more. A new import-boundary aspect now polices this node's own '../' import surface the same way it now polices the engine's, ahead of a planned future extraction of both into their own package.
