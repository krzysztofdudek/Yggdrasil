## [2026-06-08T07:35:32.227Z]
New pure helper module implementing the coverage-tier partitioning algorithm for the coverage-modes feature. The three exported functions — normalizeRoot (strips slashes, maps / to empty string for whole-repo matching), matchesRoot (prefix-safe directory matching), and partitionByCoverageTier (longest-match-wins, excluded wins equal-length ties) — encode the core invariant that the default CoverageConfig of { required: ['/'], excluded: [] } must behave identically to the pre-existing whole-repo required-coverage behaviour. These functions are pure (no I/O, no side effects) and are re-exported by check.ts so existing callers are unaffected. The module is isolated here so check.ts stays within the 40,000-character reviewer budget.
## [2026-06-08T08:23:58.914Z]
Extended check-coverage-tiers.ts with two coverage-issue builder functions previously living in check.ts. buildCoverageIssue was moved here to keep check.ts under the 40,000-char node budget; it builds the unmapped-files error issue for the required tier. buildCoverageAdvisoryIssue is a new function that builds the uncovered-advisory warning for the middle tier — files that are tracked but outside any coverage.required root. Both functions use a type-only import of CheckIssue from check.ts (erased at runtime, no circular runtime dependency) and are re-exported from check.ts to preserve the public API.
## [2026-06-08T08:36:13.495Z]
Removed the dead second parameter totalGitFiles from buildCoverageAdvisoryIssue. The parameter was accepted but immediately discarded via 'void totalGitFiles'. The function signature is now buildCoverageAdvisoryIssue(uncoveredFiles: string[]) — callers no longer pass the total file count, which was never used by this function.
## [2026-06-08T09:59:43.623Z]
Added internal double-slash collapse to normalizeRoot (Fix 3): roots like 'services//nested' produced by user config or programmatic construction would never match single-slash git-tracked paths. The fix adds .replace(/\/{2,}/g, '/') after the leading and trailing slash stripping, ensuring any run of two or more slashes in the middle of a root collapses to a single slash.
## [2026-06-08T16:05:31.488Z]
Coverage roots (the required and excluded tiers) now accept the same forms as a node mapping entry — an exact path, a directory prefix, or a glob — by matching through the shared matcher. A team can now exclude generated files anywhere by pattern, or scope the blocking tier to a glob, instead of being limited to directory prefixes. The whole-repo root and the longest-match precedence (excluded wins an equal-length tie) are unchanged.
## [2026-07-24T14:33:21.883Z]
The coverage module now answers one question for a caller outside the check itself: given a set of paths, which of them would the gate report as blocking unmapped-file errors under this project's coverage settings? It is asked right after setup writes the agent-rules files into a project's root, so an adopter upgrading from an older release learns in that moment that a project requiring its whole tree will now fail on them, and how to settle it.

The prediction lives here, beside the tier partitioning it depends on, rather than in the command that asks: it must be computed from the same two primitives the gate itself uses — is the path covered by any component's file list, and which tier does it fall into — so a caller cannot approximate the rule with its own copy and then disagree with the gate it is predicting. That divergence is the failure mode worth designing against: a warning that fires when the build would be green, or stays silent when it would be red, is worse than no warning at all.
## [2026-07-27T19:29:28.316Z]
The uncovered-files advisory message now describes the affected files as coverage-visible rather than tracked — the universe this check governs is the disk walk, which it has always been, and the old wording implied a git-tracking distinction the check has never actually drawn.
## [2026-07-28T12:11:27.266Z]
Coverage exclusion is now absolute: a file matching any excluded root is dropped before required/middle tiering is even considered, instead of the more specific of a matching required/excluded root pair winning. The exclusion test is extracted into its own predicate so every coverage consumer asks the same question the same way, and a new check flags a required root that can never match a file because an excluded root fully contains it — a config line that used to fail silently.
## [2026-07-28T13:02:32.385Z]
Reworded the coverage-required-shadowed warning's WHY sentence to plain language: it no longer names the internal middle tier, since that vocabulary appears in no other user-facing message.
## [2026-07-30T21:01:53.090Z]
Reworded a comment naming the live type-to-type relation gate so it stands on its own, dropping a short internal code with no meaning to a reader of this repository.
## [2026-07-31T12:04:35.237Z]
The one-file coverage.excluded predicate needed to be reachable from the persistence layer while expanding a mapping to real files, and a persistence-layer module may not depend on the engine layer this predicate used to live in. It now lives in the shared utility layer both sides may call, re-exported here so nothing importing it from this module needed to change.
