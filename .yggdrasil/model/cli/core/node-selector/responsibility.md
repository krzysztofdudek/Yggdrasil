# Node Selector Responsibility

Selects graph nodes, aspects, and flows relevant to a natural-language task description. Uses keyword matching against graph artifacts — deterministic and fast. Does not use semantic search, embeddings, or fuzzy matching: the graph's artifact text is designed to contain the terms agents would use in task descriptions, making keyword matching sufficient.
