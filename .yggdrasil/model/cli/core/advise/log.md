## [2026-07-12T11:42:18.502Z]
Adds the pure engine behind the advise feed. One half turns the graph's live attention signals into stable, evidence-bound nominations: each carries a canonical snapshot of exactly the evidence it rests on, hashed so that a decision made against it stops applying the instant that evidence changes. The other half folds the recorded decisions over those nominations to decide what an operator sees now versus what stays hidden — a dismissed item stays hidden while its evidence holds, a deferred item returns on or after its date, a closed item stays hidden, and any item whose evidence has moved returns as new. It keeps no clock and touches no filesystem of its own: the date is injected and file-derived inputs are passed in, so the same inputs always yield the same feed, and it never reads or writes a verdict.
## [2026-07-12T13:49:30.927Z]
The nomination engine gained the local-diagnostic and history-derived sources on top of the structural ones, plus the one-line-per-class attention aggregate. A recorded regression miss surfaces as a live concern only while the rule source still hashes to the value captured when it was measured; once the rule changes it degrades to a benign refresh note rather than a false alarm. An advisory rule with a clean recorded record is nominated for promotion with its numbers, and a rule the reviewer judged the same input inconsistently across repeated runs is nominated for sharpening; both carry honest labels when the sample is small or the history is local only. The fixed class precedence places the structural concerns above the history-derived ones. Every value drawn from the repository is stripped of control characters and rendered as quoted data with its provenance so it can never read as an instruction, and every suggestion names a human action that requires approval. The engine stays pure and deterministic: all telemetry arrives as parameters.
## [2026-07-12T14:42:35.284Z]
Every repository-derived string surfaced on the always-read attention feed is untrusted data and must render as inert quoted data, never as prose an agent could follow as an instruction. The built-in structural-health signals — a rule that attaches to nothing, a rule referenced by nothing, a rule running past its standing review date — were previously carried onto the feed with their authored message text verbatim, unlike every other source which already neutralized its embedded values. Those message strings now pass through the same control-byte neutralization, so no escape sequence or line break carried in a rule identifier can read as an instruction on the feed. The neutralization used for a full authored message deliberately carries no length limit — only a single embedded value is length-bounded — so a long but legitimate message survives whole while any injected control byte is still stripped. The control-byte neutralization and the length-bounded value quoting now share one core so the two can never diverge.
## [2026-07-12T16:10:54.097Z]
The per-rule health view now shows how often a rule has actually caught a violation against how often it was exercised, distinguishing a genuine exercise from a cached re-render and never pooling the two reviewer kinds whose false-positive mechanisms differ. It reports an honest plain-words uncertainty range instead of a false-precision number when observations are few. A rule that is enforceable yet never violated is read as possibly deterring the very violations it would otherwise catch, rather than assumed useless, and a demotion is only ever suggested when several independent signals agree, never on the catch count alone.
## [2026-07-12T16:57:00.674Z]
Every telemetry timestamp the attention engine interpolates into a nomination rationale and its honesty labels is now routed through the same control-character neutralization already applied to every other repo-derived string it renders. The attention feed is read by an agent each session, so every displayed string — including tool-written timestamps — must be quoted data that cannot be read as an instruction. This makes the invariant literal: nothing shown on the feed escapes neutralization.
## [2026-07-13T00:55:33.361Z]
The attention aggregation gained a second signal class: a bare count of files that look structurally unusual among their same-language neighbours. Like the existing class it is one aggregate line, emitted only when the count is positive so a clean repo shows nothing, and it carries no per-instance ranking. Per-file detail is deliberately kept out of this feed because it is read every session; the line is a summary that points the reader elsewhere for the specifics. The count itself is computed at the command boundary and passed in as plain data, so this aggregation stays pure and free of any file or clock access.
## [2026-07-13T01:40:15.208Z]
Added a pure per-component change-frequency counter and a new attention suggestion that flags a component which changes often yet has no enforced rule covering it. The premise is that the code most in motion tends to be the least protected, so surfacing it directs attention to where coverage is most likely missing — always as a suggestion requiring human approval, never as an automatic rule.

The change count itself is measured from version-control history outside this engine and handed in as already-parsed data, so this layer stays deterministic and free of input/output. Attribution of a changed file to its owning component reuses the single canonical owner resolver, so activity is charged to a component exactly as every other subsystem sees ownership. The "has no rule" test reuses the single canonical effective-rule query, so the full attach cascade and draft semantics are honoured: a component whose only rule is still a draft counts as uncovered, because a draft enforces nothing.

The suggestion is self-clearing — it disappears the moment a rule or coverage lands on the component, or the component's recent activity ages out of the look-back window — and it ranks below every structural signal so it never crowds out a more urgent finding. The evidence it carries (the count, a short sample of changed files capped with a "+N more" marker, and the window) is rendered strictly as quoted data, never woven into an instruction, because the whole feed is read by agents each session and untrusted repository text must never read as a command.
## [2026-07-13T18:13:57.120Z]
The attention layer gains two data lines sourced from the committed incident ledger, both fixed sentences over a count rather than narrator voice. One is a standing reality counter — the number of recorded escapes — shown even at zero, because the point is to keep the reader aware that an outside reference exists at all; unlike the internal structural lines it is never suppressed. The other appears only when at least one escape was tagged as a miscalibrated rule, and notes as evidence that the rules themselves may need review — the external counterpart to the internal catch-and-exposure health story. The attribution is deliberately aggregate: which specific rule is implicated is left to a human to determine, not guessed here.
## [2026-07-14T00:54:30.138Z]
Corrected the in-code contract description for the attention aggregation so it matches what the function actually emits. The aggregation had grown to render three signal classes, but its own docstring still described only two structural classes. The truthful behavior: an incident reality-counter line is always shown (even at zero, because the committed incident ledger is the only signal from outside the graph and its presence keeps the tower aware it has an external reference at all), plus a guarded evidence line when any incident is tagged as a wrong-rule miscalibration, plus the two structural classes (cross-architecture dependency reach, and files that look structurally unusual among their same-language neighbours) shown only when their counts are above zero. This is a comment-only correction to keep the code-level description of the contract from drifting away from the emitted output; the user-facing documentation had already been aligned to three classes and this closes the remaining gap in the source.
## [2026-07-14T08:47:54.286Z]
The attention feed's always-on reality counter and its wrong-rule evidence line are built by dropping a count into a fixed sentence. At exactly one item the fixed plural noun read ungrammatically. Both lines now select singular or plural to agree with the count, so a single recorded item reads as one incident rather than a plural. Only the noun changes; the rest of each sentence, and the condition under which the evidence line appears at all, are unchanged.
## [2026-07-14T14:23:05.135Z]
Added two lowest-priority nomination classes to the pure nomination builder, both below every history- and structure-derived class already present. One turns each fresh entry of an offline file-family analysis into a single proposal naming the member files, the fitted scope pattern, and the cluster tightness as quoted data, asking a human to draft a rule for that scope and supply the rationale themselves. The other turns each group of mutually-depending module blocks (a dependency loop in the structural quotient) into a proposal naming the groups plainly and suggesting a cut or a boundary contract. A pure freshness gate accompanies the first: it accepts the analysis file only when its own format version is the one understood here and the engine live structural-analysis schema still matches the one the file was produced against, so a moved schema omits the families rather than presenting them as current — an evidence check, not a time-based one. Every repo-derived string renders as quoted data with provenance so the feed can never be read as an instruction, and each item binds to a hash of its evidence so a dismissed item returns only when that evidence actually moves.
## [2026-07-14T15:43:04.396Z]
Hardened the freshness coupling behind the "a tight file family shares no rule of its own" proposal so the only natural reconciliation is the complete one.

Those proposals are read from an offline miner's output file, which carries its own format version and is validated against a separate live code-analysis shard schema. A build-time coupling anchors the two: if the live shard schema advances past the anchor, the build goes red until a human re-validates the miner.

The gap closed: when the live shard schema later moves, that coupling could be re-satisfied by bumping ONLY the anchor constant while leaving the accepted file-version gate unchanged. A candidates file produced under the OLD schema would then still parse and be surfaced to a human as a live rule-family proposal, even though the analysis it rests on no longer reflects the current schema. A stale proposal presented as live could push someone toward creating a rule from analysis that no longer holds.

The fix requires bumping BOTH the anchor AND the accepted file-version gate in lockstep (and having the miner emit the new file version) whenever the shard schema moves, so an old-version file is rejected at parse rather than rendered as fresh. The accepted file-version gate is what actually rejects the stale file; the build-time coupling only guards the anchor. This was made self-documenting via the coupling guard's failure message and the two constants' doc-comments, and the accepted-version constant was exported so the guard can be asserted against the real value rather than a duplicated literal.

No runtime behavior or on-disk data shape changed: the file version, the candidates-file layout, and the producer/consumer field contract are all untouched. This is purely a stronger, self-documenting reconciliation guard.
## [2026-07-14T17:38:16.579Z]
Documented, in the regression-nomination engine, that it now receives only actionable drill outcomes — the calling boundary pre-filters recorded outcomes to in-repository runs whose case still lives in the aspect's current regression corpus. The engine therefore no longer has to reason about orphaned residue (external measurements or cases since removed): every outcome it sees names a case that can still be re-run or retired. This keeps the pure engine's documented contract consistent with the caller's new behavior so a future reader is not misled into thinking the engine itself guards against orphaned telemetry.
## [2026-07-30T19:19:28.103Z]
The nomination that flags a rule as effective nowhere accepted an optional enforcement classification, but the command driving it never supplied one, so a rule reachable only through an architecture type could be nominated as dead even while the structural check already treated it as live. It now receives the same classification the command computes for a related signal, so the two can never disagree.
## [2026-07-31T18:00:25.443Z]
The owner resolver churn attribution shares with the relation-conformance pass and coverage now excludes a file this graph excludes from coverage, the same way every other ownership answer in this graph already does — resolving the exclusion set requires a real filesystem read, so the resolver is now async.
## [2026-08-02T06:21:52.370Z]
advise's nomination engine gains a class that flags a churning file the architecture type tier classifies but no component owns, backed by a new churn counter keyed by file path so a file with no owning component is no longer silently dropped from the churn signal the way the existing per-component counter drops it.
## [2026-08-02T08:02:32.412Z]
The type-covered-churn nomination fired at any churn above zero and broke ties alphabetically by file path within its own class, so on a project with many type-covered files the display cap filled with whichever paths sorted first — files touched only by the commit that created them — while the one file genuinely under active edit could be crowded out entirely. It now requires a file to have been edited at least once beyond its creating commit, and within the class it ranks by how much a file has actually churned, busiest first, so the cap always keeps the strongest signal. The class also now requires the file's matched architecture type to genuinely enforce something on it; a type that attaches nothing is an uncovered file, not one the type tier is carrying, and saying otherwise misled a reader into looking for rules that do not exist. Its cluster sentence used to say 'both files' regardless of how many partners it actually named; it now says how many.
## [2026-08-02T09:07:00.989Z]
clusterPartnersOf accepted any same-type import partner present in the churn map at all, so a nominee's WHY sentence could name a partner that would never itself qualify for a nomination — one with churn 1 (only its creating commit) or one whose matched type enforces nothing on it — and call it carrying real weight. It now applies the exact same two-part gate, the churn floor and the enforcement check, to a partner as to the nominee itself, so a file cited as evidence in another file's WHY would always have qualified for a nomination of its own. Also softened MIN_TYPE_COVERED_CHURN's comment: the floor screens out the one shape it can prove (a lone, still-visible creating commit), not every unedited file — a rename, a merge, or a creating commit that scrolled out of the read window can each hide real edits behind the same low count.
## [2026-08-02T09:46:20.774Z]
An earlier entry in this log stated that the type-covered-churn floor 'requires a file to have been edited at least once beyond its creating commit.' That is superseded: the floor cannot prove a file was never edited, only that no edit is visible on its current path inside the read window. A creating commit that scrolled out of the window, a rename (whose destination path carries none of the source path's older history under name-only logging), or a merge that introduces the file with no file list of its own can each hide a real edit behind a churn of zero or one. What actually holds, as the next entry after the superseded one already stated in the source comment: the floor screens out the one shape it can prove — a lone, still-visible creating commit — not every unedited file.
## [2026-09-06T20:12:51.961Z]
The attention engine now also folds in proposals another tool measured and handed over, and it treats them as strictly weaker evidence than anything it derived itself. A document is read only under a contract this build knows by name, only its documented fields are read, and an item whose kind this graph has no vocabulary for is refused at the door rather than stored as an opaque suggestion nobody could act on — storing one would put an item on the feed with no next step behind it. Each item's evidence is carried verbatim under the producer's name and never re-derived or summarized, because the whole value of an outside measurement is that a reader can tell it apart from this graph's own conclusion; for the same reason imported items rank below every class the graph nominates itself, so an outside opinion can never crowd out a structural finding. Importing is not accepting: an imported item is a proposal like any other, and dismissing, deferring or acting on it stays the user's own recorded act against the evidence hash it rests on. Two self-contained nomination classes moved into files of their own beside the engine. That is not tidying: the engine file is the one whose assembled reviewer prompt runs closest to its ceiling in this repository, and a class with a single entry point is the cheapest thing to move without changing what any rule sees.
