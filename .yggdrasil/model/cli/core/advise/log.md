## [2026-07-12T11:42:18.502Z]
Adds the pure engine behind the advise feed. One half turns the graph's live attention signals into stable, evidence-bound nominations: each carries a canonical snapshot of exactly the evidence it rests on, hashed so that a decision made against it stops applying the instant that evidence changes. The other half folds the recorded decisions over those nominations to decide what an operator sees now versus what stays hidden — a dismissed item stays hidden while its evidence holds, a deferred item returns on or after its date, a closed item stays hidden, and any item whose evidence has moved returns as new. It keeps no clock and touches no filesystem of its own: the date is injected and file-derived inputs are passed in, so the same inputs always yield the same feed, and it never reads or writes a verdict.
## [2026-07-12T13:49:30.927Z]
The nomination engine gained the local-diagnostic and history-derived sources on top of the structural ones, plus the one-line-per-class attention aggregate. A recorded regression miss surfaces as a live concern only while the rule source still hashes to the value captured when it was measured; once the rule changes it degrades to a benign refresh note rather than a false alarm. An advisory rule with a clean recorded record is nominated for promotion with its numbers, and a rule the reviewer judged the same input inconsistently across repeated runs is nominated for sharpening; both carry honest labels when the sample is small or the history is local only. The fixed class precedence places the structural concerns above the history-derived ones. Every value drawn from the repository is stripped of control characters and rendered as quoted data with its provenance so it can never read as an instruction, and every suggestion names a human action that requires approval. The engine stays pure and deterministic: all telemetry arrives as parameters.
