## [2026-08-07T11:11:41.334Z]
Split out of the fill stage's substrate into a component of its own, and renamed to match. Lock verification now shares these same per-rule, per-component parse caches for the companion hooks its prompt-size gate may run, and it cannot depend on anything that depends back on the check report — which the substrate does, through the stage's public contract. These helpers need nothing but the pair shape and the parse cache itself, so a home of their own is what lets both callers stand on them without a cycle. The old name said fill; two callers now, and only one of them is a fill.
