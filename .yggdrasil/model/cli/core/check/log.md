## [2026-05-15T10:17:15.671Z]
Fix deterministic violation: replace toLocaleString() with direct number interpolation — locale-independent output
## [2026-05-15T13:42:55.515Z]
R0.1 Phase 2: populate messageData alongside message at each buildIssueMessage call site. Structured IssueMessage now carried in CheckIssue; message string computed from it for backward compat.
## [2026-05-15T13:45:43.238Z]
R0.1 Phase 3: cli/check.ts now reads messageData via msg() helper to render issue output; buildIssueMessage called at CLI layer. Fallback to .message for issues not yet migrated in Phase 4.
## [2026-05-15T14:17:01.394Z]
Drop deprecated message field and buildIssueMessage import: CheckIssue objects now set only messageData; buildIssueMessage import removed from core layer. R0.1 Phase 5.
## [2026-05-16T04:54:08.442Z]
Remove dead flow-related branches: layer === 'flows' check in describeUpstreamCause and flowMatch block in groupCascadeByCause — flow YAML is no longer tracked, so these paths are unreachable. Also remove 'flow: --flow' from flagMap in computeSuggestedNext.
## [2026-05-16T05:58:05.490Z]
Phase 4.7 (no-direct-fs): route all fs calls through io/graph-fs.ts; update hash and paths imports from utils/ to io/ following module moves
## [2026-05-16T19:31:38.898Z]
Updated effective-aspects import path to core/graph/aspects following the file move.
## [2026-05-16T19:44:32.245Z]
Updated context-files import path to core/graph/files following the file move (collectTrackedFiles + TrackedFile). cli/core/context node drops context-files from its mapping; cli/core/graph node claims it.
## [2026-05-26T10:11:38.382Z]
Add comment explaining smaller STRUCTURAL_CODES set is internal-filter only, not CI blocking. The two sets have diverged historically; aligning them is tracked as dogfood cleanup, not in this change.
## [2026-05-28T10:37:57.697Z]
Drift cascade formatter now distinguishes reference-file changes from aspect-content changes. In describeCascadeCause, when a tracked file in the 'aspects' layer does not match the .yggdrasil/aspects/<id>/ path prefix, the file is a reference declared in some aspect's references: list. The formatter scans graph.aspects to find the declaring aspect(s) and emits 'reference file X (declared by aspect Y) changed' instead of the previous generic 'tracked file changed' fallback. This makes upstream-drift causes self-explanatory in yg check output for the new reference cascade source.
## [2026-05-28T13:13:47.979Z]
Render aspect-status by status: check now emits aspect-newly-active for non-draft effective aspects without a baseline verdict, aspect-violation-enforced (error) and aspect-violation-advisory (warning) for refused baselines. Per-node short-circuit moved from computeEffectiveAspects to hasNonDraftEffectiveAspects so all-draft nodes skip drift detection without spurious findings. GC predicate aligned with approve: drift state for nodes with no non-draft effective aspects is removed silently. Legacy baselines lacking aspectVerdicts are tolerated as 'implicitly approved' so the 5.x upgrade does not flood the user with aspect-newly-active. CheckResult adds advisoryWarnings and draftSkipped tallies; suggestedNext prefers an error's next over a warning's so an enforced violation outranks a co-emitted advisory.
## [2026-05-29T09:56:03.437Z]
check.ts core orchestrator updated to produce structured CheckResult output consumed by the new renderer. The grouped rendering logic was separated from the orchestration logic in this refactor.
## [2026-05-29T09:56:51.359Z]
Changed countDraftAspectsAcrossGraph to count UNIQUE draft aspect IDs (aspects whose aspect-level default is draft) rather than counting (node × aspect) pairs. The old count inflated the tally by multiplying one draft aspect across all nodes it reached via cascade channels. The new count reflects how many distinct aspect rules are dormant, which is the number that actually matters when an agent is assessing how much coverage is inactive.
## [2026-05-29T10:07:14.007Z]
core/check.ts updated: the check orchestrator now returns a structured CheckResult type with typed issue codes and cascadeCauses data, consumed by the new grouped check renderer.
## [2026-05-29T17:08:10.071Z]
Cross-node files that a structure-shape rule reads through a related component now count toward the dependent component's verification identity and its change-impact blast radius. Previously such a rule could inspect a sibling component's file, but the set of files brought into the dependent component's drift fingerprint was only assembled when the stored approval baseline was supplied to the file-collection routine — and the routine that gates everyday drift checks did not supply it. The consequence was a silent gap: editing a file that only a neighbour's structure rule reads never marked the dependent component as needing re-verification, and the change-impact query reported zero affected components for that file. The baseline is now supplied at the drift-detection points, and the canonical fingerprint is recomputed after the structure runner records the touched files, so the first approval already folds those cross-component files into the identity. Files the component owns directly stay classified as ordinary source changes; only genuinely cross-component paths receive the dedicated layer, and they are deliberately excluded from the owned-file map so a deleted-file scan never mistakes them for a vanished local file. A targeted single-rule re-approval now carries forward a sibling rule's recorded touched-file set instead of dropping it.
## [2026-05-30T04:57:35.096Z]
Removed leaked internal planning references — task, phase, and decision identifiers and section citations — from source-code comments. These tokens named ephemeral planning artifacts that mean nothing to a future reader and violated the project's comment self-containment rule. The substantive explanation each comment carried was preserved; no code or behavior changed.
## [2026-05-30T06:29:17.474Z]
Stop maintaining a private copy of the drift-layer lookup; consume the shared classifier instead.

The gate that reports drift must decide, for each changed file, whether the change is a direct source edit or an upstream cascade from a graph element. That decision previously relied on a lookup table built inline here, duplicated almost verbatim in the approve path. Keeping two copies invited them to diverge — and they had, in how a file reached through a directory-style mapping was resolved. Delegating to a single shared classifier removes that hazard: both the report and the approve step now answer the question the same way, so a file is never labeled a source edit by one and a cascade by the other.
## [2026-05-30T21:05:41.490Z]
Drift attribution was sharpened so a dependent node is no longer fully re-reviewed when it drifts only because a cross-node file read by one of its graph-aware deterministic checks changed. Previously such a change was un-attributable to any specific check, so the whole node — including its human-language reviewer aspects — was conservatively re-verified; that re-verification is paid and non-deterministic, so it produced flaky, costly churn on nodes whose own code never changed. Now the change is attributed to the specific deterministic check that reads the file (via the per-node record of which files each check touched), so only that local, free check re-runs and the human-reviewer verdicts carry forward unchanged. The motivation is correctness-preserving cost control: a check that only reads cross-node files cannot affect what a human-language reviewer sees, so re-running it alone is exactly right.

Specific to this node: the drift report that explains why a cascade fired now names the owning check for the synthetic per-check identity causes (the touched-file-set summary and the resolved-reviewer-tier summary), instead of attributing them to an unknown source.
## [2026-05-30T21:30:16.991Z]
The helper that turns a drift cause into a human-readable explanation was made reachable from tests so that every branch of its message logic can be verified directly — the messages that name an aspect, the messages for the synthetic per-aspect identity causes, the reference-file attribution (declared by zero, one, or several aspects), the parent and dependency cause messages, and the generic tracked-file fallback. The motivation is that these messages are the agent's only explanation of why a re-verification was triggered, so each variant must be pinned by a test; exposing the pure function is the least intrusive way to cover its branches exhaustively without constructing a full drift pipeline per case.
## [2026-05-30T21:54:03.457Z]
Internal names that still referred to the retired single-file and graph-aware reviewer kinds were renamed to reflect the one unified deterministic kind: the per-aspect record of which files a deterministic check read, the synthetic drift-identity key summarising that set, the tracking layer those cross-node files sit on, and the error tag for a crash inside a non-reviewer (deterministic) runner. This is a clarity-only rename with no behaviour change — the verification mechanics, drift detection, and carry-forward are identical; only the identifiers and the persisted token strings changed so the code no longer speaks of reviewer kinds that no longer exist. Baselines were migrated in place to the new token names.
## [2026-05-30T22:03:14.858Z]
The loop that reports files which existed at the last approval but are gone now was computing a path with host separators normalised to forward slashes, yet emitting the un-normalised original into the agent-visible drift report — both the 'deleted' lines and the cascade-cause entries. On a host whose native separator is the backslash this would have leaked backslash paths into output that must always be POSIX, so the report could read inconsistently across platforms. Every classification lookup and output string in that loop now uses the normalised path. The motivation is the cross-platform output contract: the drift report an agent reads must use forward slashes regardless of the host.
## [2026-05-31T06:24:49.484Z]
An internal name was changed for clarity: the per-aspect record of files a deterministic check read, its synthetic drift-identity key, and the tracking layer for those files were renamed from a deterministic-prefixed form to a check-prefixed form, so the identifiers name the check.mjs artifact they describe. This is a clarity-only rename with no behaviour change — drift detection, attribution, and carry-forward are identical; only the identifiers and the persisted token strings changed, and in-repo baselines were migrated in place to the new tokens.

Additionally: Two agent-facing diagnostics were corrected. A multi-node cascade target list rendered with a doubled path separator; it now renders with a single separator. And a configuration whose schema version is newer than this CLI supports was being wrapped as an internal bug with a file-an-issue prompt; it is now classified as the expected user error it is — telling the user to upgrade the CLI — without the bug framing. Neither changes the underlying detection; both make the output read correctly.
## [2026-05-31T07:08:06.001Z]
An aspect-implies cycle introduced after a node already had an approved baseline crashed the check command with an unclassified internal error instead of the structured cycle error the validator already produces. The drift pass computed effective aspects for a node with a baseline and the implies expansion threw on the cycle, escaping before the validator's clean message could render — so the same cycle reported nicely without a baseline but crashed with one. The cycle signal is now a dedicated error type, and the drift pass skips a node whose effective-aspect computation hits a cycle, since the graph is already invalid and the validator reports it. The check command now reports the same structured cycle error whether or not a baseline exists. The motivation is that a detectable graph error must always surface as a clean, actionable diagnostic, never as a please-file-an-issue crash.
## [2026-05-31T09:10:41.976Z]
Improved the wording of several re-verification cause messages that previously rendered an unhelpful placeholder. When a change propagates to a component because of its own definition, because a dependency's published contract shifted, or because a file that one of its programmable checks reads was edited, the explanation shown to the agent now names the actual cause instead of an empty or 'unknown' label. Accurate cause messages matter because the whole point of surfacing a re-verification is to tell the agent precisely what changed and which command resolves it; a placeholder forces guesswork and undermines that contract.
## [2026-05-31T09:59:01.843Z]
Named the rule in two re-verification messages and attached the rule identifier to the just-became-active finding. When a component must re-verify because a rule's definition metadata changed, the explanation now reads as that rule's definition changing rather than referencing an opaque internal key; and a finding that a rule has newly become active on a component now carries which rule it concerns as structured data, so the batch-by-rule tooling can attribute it precisely instead of parsing prose.
## [2026-05-31T16:03:32.677Z]
Replaced the hand-inlined path-separator normalization with calls to a single shared helper. The same small idiom — convert backslash separators to forward slashes, and in most places also strip a trailing slash — had been copied across many modules, so the normalization rule lived in dozens of places at once and any change to it risked drifting them out of step. Consolidating it behind one well-named helper means the rule lives in exactly one spot and each call site reads by intent instead of by a repeated regex. Behavior is unchanged: the helper bodies are byte-for-byte equivalent to the expressions they replace, and the full test suite passes identically.
## [2026-05-31T16:28:48.517Z]
Two related correctness improvements to the validation layer. First, the set of structural error codes — the codes that always block the check regardless of drift — had been hard-coded in two places, once for the summary tally and once for the rendered grouping, and the two had silently diverged so the count could disagree with what was shown to the agent. They now share a single definition, so the tally and the grouping can never drift apart again. Second, a new validation rejects a node mapping that resolves outside the repository root — an absolute path, or one that climbs above the root with a parent-directory segment. The mapping-path normalizer only converts separators and strips a leading current-directory prefix and trailing slashes; it does not collapse parent-directory segments, so without this guard a mapping could claim files outside the project and bypass coverage and aspect enforcement.
## [2026-06-01T06:20:25.280Z]
Moved the drift-cause description and identity-diff helpers out of this node into a dedicated sibling module, importing them back. This was required because the check engine had grown past the per-node reviewer-context budget once the drift baseline gained a typed upstream-identity block; an oversized reviewer prompt risks window truncation that falsely rejects unchanged code. Pure code move, no behavior change — the same cascade causes are still classified and rendered identically.
## [2026-06-01T07:03:30.364Z]
Removed the now-unnecessary second argument (graph) from the call to describeIdentityCause after the function signature was simplified to take only the cause. The function never used graph; the parameter existed as a speculative placeholder and was removed per YAGNI. The call site is the only caller in production code and required only the argument-count update.
## [2026-06-03T07:05:08.274Z]
Check now surfaces the new aggregating-aspect kind and the zero-source fail-closed condition alongside existing drift and coverage reporting, so an aggregate attach point and an LLM node that can never be verified are both visible in the gate rather than passing silently.
## [2026-06-03T07:49:05.037Z]
yg check now passes reuseByMtime=false to hashTrackedFiles so the gate always re-reads content from disk. Previously, a stored mtime match caused the stored hash to be reused without verifying the file's actual content, allowing a content edit followed by touch -r to restore the mtime and pass the gate undetected.
## [2026-06-03T08:20:09.656Z]
The drift gate recomputes a node's canonical baseline hash (which folds source files, typed identity, and per-aspect verdicts) and previously only reported a divergence it could attribute to a changed source file or a graph-identity change. A divergence whose only cause was the folded verdict set — for example a committed verdict hand-edited from refused to approved while the recorded hash was left untouched — matched neither path, so the computed mismatch was silently discarded and the gate passed clean over a baseline the reviewer never re-validated. Any unexplained hash mismatch now blocks: when the recompute disagrees with the recorded hash but no file or identity cause is found, a blocking integrity error is emitted. The same condition also catches a baseline that predates a hash-scheme change; both resolve by re-approving the node or restoring its drift-state from version control, so the single signal serves both without false positives on a consistent baseline.
## [2026-06-08T07:35:25.087Z]
The three pure coverage-tier helpers (normalizeRoot, matchesRoot, partitionByCoverageTier) were extracted to a sibling module (check-coverage-tiers.ts) rather than remaining inline in check.ts. check.ts re-exports all three so the public API is unchanged. The extraction was required because adding the helpers pushed check.ts past the 40,000-character per-node reviewer budget, and the helpers form a self-contained logical unit with no I/O or side effects that belongs in its own node. check.ts now has a single re-export line in place of the inline implementations.
## [2026-06-08T07:50:36.737Z]
Applied nested-graph subtree exclusion to the coverage scan and source-count computation in check.ts. Imported excludeNestedGraphSubtrees from the repo-scanner module and applied it at the top of scanUncoveredFiles (on the gitTrackedFiles list passed in) and in the runCheck source-count filter (wrapping gitTrackedFiles before the ygg-prefix predicate). Files under a nested .yggdrasil subtree are now silently dropped from both the total-file count and the uncovered-file list, so a parent graph no longer reports coverage gaps for code governed by a child graph.
## [2026-06-08T08:11:27.050Z]
Improved code clarity: added an idempotent comment before the scanUncoveredFiles call in runCheck to make it explicit that scanUncoveredFiles applies excludeNestedGraphSubtrees internally (the call is safe with a raw list because the filter is idempotent). Also updated the scanUncoveredFiles JSDoc to document that it excludes nested-graph subtrees in addition to the bound graph's own .yggdrasil/, matching the actual behavior introduced by the nested-graph subtree filtering.
## [2026-06-08T08:23:53.416Z]
Wired tiered coverage into runCheck: instead of a single unmapped-files error for all uncovered files, the coverage section now calls partitionByCoverageTier to split uncovered files into a required tier (error) and a middle tier (non-blocking warning). The coverageIssue variable was replaced with a coverageIssues array built from buildCoverageIssue (required tier) and buildCoverageAdvisoryIssue (middle tier). The buildCoverageIssue function was moved out of check.ts to check-coverage-tiers.ts to keep check.ts within the 40,000-char node budget; it is re-exported from check.ts so the public API is unchanged. buildCoverageAdvisoryIssue was added to check-coverage-tiers.ts and re-exported the same way. The default config (coverage.required: ['/']) preserves the existing behavior where every uncovered file is an error.
## [2026-06-08T08:36:09.244Z]
Eliminated a duplicated inline literal for the default coverage config. The inline fallback '{ required: ["/"], excluded: [] }' in runCheck was replaced with an import of DEFAULT_COVERAGE from config-parser, making config-parser the single source of truth for that constant. Also removed the dead second argument from the buildCoverageAdvisoryIssue call site (the function no longer accepts totalGitFiles). Added a clarifying comment above the advisoryWarnings tally to document that it counts only aspect-status advisories, not the coverage uncovered-advisory warning.
## [2026-06-08T09:59:38.342Z]
Fixed coveredFiles metric computation order (Fix 1): moved the coveredFiles assignment after partitionByCoverageTier so excluded-tier files no longer depress the coverage ratio. Before this fix, coveredFiles = totalFiles - uncovered.length subtracted ALL uncovered files including silently-excluded ones; the correct formula is totalFiles - (required.length + middle.length). Excluded files are intentionally ignored and must not affect the ratio shown in the header.
## [2026-06-08T15:11:09.287Z]
The uncovered-file coverage scan now treats a file as covered when any node mapping entry matches it by glob pattern, not only by exact path or directory prefix. This keeps the coverage gate aligned with the new ability to scope a node to a subset of a directory by naming pattern: files matching the pattern are owned and silent, files in the same directory that do not match remain uncovered and surface as expected.
## [2026-06-08T16:40:57.639Z]
Source-existence detection for drift now expands a glob mapping entry and treats it as present when it matches at least one file on disk, instead of probing the literal pattern string. Probing the pattern string always failed, so a node mapped only by a glob falsely reported that its source files were never created and was treated as source-gone.
## [2026-06-08T17:36:20.324Z]
Corrected an inline comment in drift classification that mis-stated where implies-cycle rejection originates. The status-aspect computation converges (does not throw) on a cyclic graph by design, so the cycle error caught during per-node drift classification comes from the effective-aspects traversal reached via tracked-file collection, not from the status computation.
## [2026-06-12T13:31:32.574Z]
Removed the local private copy of getChildMappingExclusions. The function is now imported from core/pairs.ts, which is the single canonical implementation of the child-wins carve-out model. The prior duplication between approve.ts (exported) and check.ts (private) meant two sites had to be kept in sync — a maintenance hazard that was masked by the approve.ts v8 ignore annotation pointing at check.ts as the authoritative test location. Now both consumers share one tested implementation.
## [2026-06-13T03:12:07.904Z]
The gate is a read that never mutates state: it computes which rules are expected on which review units, validates the recorded verdicts against current inputs, runs deterministic checks live, and reports — so continuous integration can verify the codebase against its architecture without provider credentials or model calls.
## [2026-06-13T08:59:32.963Z]
The coverage scan decided a file was 'covered' if it merely matched a node's mapping pattern, ignoring that the file-expansion layer drops gitignored files reached through directory or glob mapping entries. A file that is both git-tracked and matches a gitignore pattern — legal in git when a file is force-added or a gitignore rule is added after the file was already tracked — was therefore counted as covered yet excluded from every review subject set, so an enforced rule passed over it with no reviewer ever seeing it: a false green. The scan now additionally flags, as a blocking error, any git-tracked file that is matched by a mapping, is gitignored, and is not rescued by a directly-named single-file mapping entry (direct entries bypass gitignore). This is purely additive — it does not change which files are expanded; it only refuses to call a silently-dropped tracked file 'covered'.
## [2026-06-14T06:55:19.215Z]
Plain verification now re-validates relation-conformance verdicts without parsing any source — it re-hashes the recorded inputs and re-resolves ownership only — and raises a blocking error for any component that is refused or whose verdict is no longer current. This keeps the read path free of source parsing while still catching an undeclared cross-component dependency.
## [2026-06-14T22:16:28.934Z]
The architecture relation-conformance check — the gate that refuses a component which depends on another component without declaring that dependency — is now computed live on every read-only check rather than re-validated from a stored result. The motivation is correctness over caching: a cached relation result can silently disagree with the current code after an edit that is not otherwise re-verified, whereas parsing and resolving the dependencies on each run guarantees the reported edges are always the present truth. The relation pass parses source locally and makes no model calls, so the read-only check stays keyless and cheap; only genuinely undeclared cross-component dependencies are surfaced as blocking errors.
## [2026-06-21T13:05:51.032Z]
Added rendering for a companion that fails to resolve during the prompt-size gate: a clear per-pair diagnostic naming the aspect, the unit, and the hook's error, blocking for an enforced aspect and a warning for an advisory one, with a matching branch in the suggested-next-step cascade so the agent is pointed at the cause rather than a generic unverified message.
## [2026-06-21T16:25:12.370Z]
Recovery guidance for a damaged per-node history pointed at a lock file name that no longer exists, so the restore command aborted without restoring anything; it now names the committed history baseline file so the restore works. A history file left carrying unresolved merge conflict markers is now detected up front and routed to the dedicated reconcile command, instead of being misreported as a generic format problem that would tempt a hand-edit and break the append-only integrity chain. Each reported problem also now records whether it concerns a model-judged or locally-judged rule so a summarised view can group them.
## [2026-06-23T05:55:51.927Z]
Enforce the mandatory-log requirement on the read-only check, decoupled from rules. The requirement — that a change to a node whose type opts into justification-on-change must carry a fresh note — had been checked only while filling verdicts, and only for nodes that produced work to verify. A node with no effective rules (all parked, none defined, or a change touching files no rule inspects) was never reached, so its source could change unlogged and still pass. The requirement is a property of the node's type plus a source change alone, so it is now computed live from each node's source fingerprint during the plain read and raised as a blocking error there. Filling only records the closure baseline once the entry exists; it is no longer the sole place the requirement is checked, which is why integration checks on the read alone now catch an unlogged change on a rule-less node.
## [2026-06-23T06:53:40.289Z]
Consult the shared log-gate freshness predicate from the read-only log module rather than the fill module. The read path needs that predicate, but importing it from the fill module made the read path depend on the write/fill path — and because the fill path already depends on the read path, that closed a forbidden cycle in the dependency graph. The predicate is read-only, so it now lives in the neutral shared log module and the read path consults it there: same single source of truth, no dependency on the fill path, no cycle.
## [2026-06-27T14:52:16.848Z]
The relation-conformance step was pointed at the new per-file syntax-fact cache directory in place of the retired per-language symbol-index cache. The relation verdict is still recomputed live on every run — only the expensive parsing is cached, never the conclusion — so this changes how fast the check runs, not what it decides.
## [2026-07-03T07:20:07.958Z]
Two independent surfaces decide "what to fix next" on a failing check — the single highest-priority triage view and the one-line next-step pointer — and they must always agree on which problem is most urgent. For errors that carry no explicit priority (graph-shape defects, coverage gaps, and the like) the two ranked differently: the triage view ordered them by category, putting graph-shape defects ahead of coverage gaps and breaking ties alphabetically by rule, while the next-step pointer picked whichever such error happened to be discovered first during validation. On a repository whose most urgent problem was one of these unranked graph-shape defects, the triage view could highlight a different rule than the next-step pointer named — breaking the promise that the single-group triage view is exactly the group the next-step line draws from. The next-step pointer now selects the same alphabetically-first graph-shape defect the triage view shows, and always names a concrete next error even for problem kinds outside the documented priority cascade, so the two surfaces can never disagree about which problem to address first.
## [2026-07-05T12:27:16.526Z]
A build reported as fully verified previously gave no indication of how much of that assurance came from a deterministic, locally-run check versus an LLM reviewer reading the code. Both kinds of verdict are trustworthy, but they carry different cost and confidence characteristics: a deterministic verdict is free and mechanical, while an LLM verdict comes from a reviewer inspecting the source and costs a request. Collapsing both into one combined count made it impossible to tell, at a glance, how much of a clean result was machine-checked for free versus actually reviewed by an LLM. The per-pair verification pass now also tallies how many of the verified pairs came from each kind, so downstream reporting can disclose that split honestly instead of surfacing only the combined total.
## [2026-07-12T10:11:09.562Z]
The check orchestrator now accepts an injected clock and, only when one is supplied, emits a non-blocking warning for any rule whose standing review-by date has passed; with no clock it skips the comparison entirely, so the core never reads a clock and stays deterministic. The warning writes nothing, changes no judgment, and never gates approval, it only asks a human to renew or retire the rule. The review-by date is deliberately excluded from every verification hash, so recording it invalidates no existing judgment.
## [2026-07-12T21:52:35.895Z]
The check orchestrator now emits a silent, best-effort attention index of source files whose structure stands out from their component's other same-language files, derived from the per-file parse facts it already gathers for the dependency check, so it adds no extra parsing. Outliers are judged with robust statistics — the median and the median absolute deviation, never the mean and standard deviation — so a single extreme file cannot distort the baseline it is being compared against; and a file is flagged only when its group has enough same-language peers to be worth comparing and the dimension actually varies. Small samples and flat dimensions stay silent rather than manufacturing precision or dividing by zero. The index is written only when the reporting read asks for it, and only after the full issue set is already assembled, so a write failure can never change what the check reports or its exit code, and every other caller stays a pure read with no side effect. Each entry is pinned by the file's own content hash, threaded from that same pass, so a later reader can trust an entry exactly while the file's bytes are unchanged. The whole thing is instrumentation: it is never part of any stored verification result and can neither invalidate one nor be invalidated by one.
## [2026-07-12T22:36:54.227Z]
Correcting where the silent structural-fingerprint attention index is refreshed: it is maintained on BOTH the read-only report and the report produced after an approval-and-fill run completes, rather than only on the read-only path as an earlier note claimed. The correction matters because a project that fills automatically on every check would otherwise never get an index built at all. The write still happens only after the full issue set is assembled, and is best-effort and gitignored, so it can never change the reported result or the exit code; the cost-preview run and the internal re-checks other callers perform continue to write nothing. The robust-statistics outlier test and the small-sample and flat-dimension guards are unchanged — this is purely about which report boundary triggers the write.
## [2026-07-12T23:12:14.841Z]
The silent structural-attention index and its calibration dump now consider only the repository's tracked, graph-governed files — never gitignored or scratch files that merely fall under a mapped ancestor directory. The reason is a correctness one: the relation pass parses every file a component maps, and a component that maps a broad directory can pull in gitignored scratch or an entire nested working-copy left on disk. Including those makes the attention layer describe files the repository does not version — and an unrelated leftover working-copy was observed swamping the result. The universe is now confined to exactly the set the coverage layer governs: the tracked files, minus any nested-graph subtree. A file outside that set is dropped even if it has an owning component, treated the same as a file with no owner. When the tracked set is unavailable, nothing is written, because an unknown scope must never be silently treated as an empty or unfiltered one. This is instrumentation only — best-effort and never part of any stored verification result — so the scoping changes what the attention layer talks about, never whether a run passes.
## [2026-07-13T00:06:23.482Z]
Add a reader for the structural-deviation index and a shared on-disk schema, and recalibrate the outlier-admission sensitivity.

The reader returns a file's recorded outlier status only when the index still describes the file's exact current bytes, so an index that predates an edit stays silent rather than speaking about content it never measured. The on-disk filename, version, and record shape live in one shared definition so the reader and the writer can never drift apart; the reader is held to a strict import boundary so read-only attention data can never reach a verdict, an exit code, or a suggested next step.

The admission sensitivity was raised after measuring how often files are flagged across this repository: the goal is that the note stays rare and high-signal, firing on well under one in fifty comparable files. Reaching that needed a threshold far higher than a normal distribution would suggest, because the raw structural counts are integer-valued with tight but non-zero spread and the robust score built on the median absolute deviation produces many moderately-large values. The threshold changes only which files are pointed at for attention; it is deliberately kept out of everything a saved verification result depends on, so moving it re-checks nothing.
## [2026-07-13T00:55:27.629Z]
The reader for the local structural-deviation index gained an aggregate: how many of the listed files are still live outliers right now. A file counts only while its current contents still hash to the value the index recorded, re-derived by reading the file text and hashing that text (not its raw bytes), which is the exact same derivation the single-file lookup uses. Sharing that one rule guarantees the count can never disagree with the per-file hint: a file is counted here if and only if the single-file lookup would still speak about it. A file that changed, vanished, or is unreadable is not counted, so a stale index never inflates the total.

The reader was also hardened against a corrupted index. Each recorded deviation must be an object with a text dimension name and a numeric score; a malformed element is dropped, and an entry left with no well-formed deviation is rejected whole, since the writer is sparse and only ever records a file that has at least one real deviation. This keeps a garbage measurement from ever reaching a consumer while the reader stays tolerant and never throws on a bad file.

A stale note on the writer side was corrected: the match key is the hash of the file's text, not of its raw bytes, so a future reader is not misled into re-deriving the wrong value.
## [2026-07-15T09:46:49.519Z]
The language-registry module — the pure extension-to-grammar lookup table with no imports of its own — was relocated from the core layer to the utility leaf so that lower layers (AST parsing, relation extraction, structural checks) no longer declare a dependency that points back up into core. This node's import path to that module was updated to the new leaf location. Pure structural relocation: no behavior changes, the lookup table and its helpers are byte-identical.
## [2026-07-16T13:50:52.911Z]
Corrected a stale internal documentation comment describing which callers cause the silent feature-field deviation index to be written after a check. The comment overstated the number of other entry points and wrongly asserted that the post-fill report path never triggers the write, when in fact that path forwards whatever its caller requests and does write the index on a real approve run. The comment now states the true behavior so a future reader is not misled about where the index is maintained. Comment-only — no behavior change.
## [2026-07-20T04:43:29.668Z]
Two correctness fixes to the check engine.

The live dependency-conformance check must parse every mapped source file to discover its cross-node dependencies. If a file cannot be parsed because its language grammar failed to load — a missing or corrupt parser rather than merely malformed source — treating it as having no dependencies would let real, undeclared dependencies pass unchecked, and would do so for every file in that language at once when the grammar is unavailable. The check now fails closed on such an infrastructure fault, surfacing one blocking error per affected language and pointing at reinstalling the tooling, so the build stays red over code no analysis ever saw rather than going quietly green. This runs on every check, with no approval needed, like the rest of the dependency-conformance check.

Separately, the single next-step the check surfaces must name the same issue group that the prioritized, narrowed views render first. The selection previously used raw iteration order, which is not the locale-sorted order the grouped output uses, so the surfaced next step could name a different rule than the top group — a self-contradiction. Selection now applies the same priority-then-locale tie-break end to end, and a warnings-only run now points at the highest-priority remaining warning rather than only ever an advisory rule violation.
## [2026-07-24T12:09:08.932Z]
The orchestrator that composes every pass of a full check gained one more pass: comparing a repository's committed summary of the operating rules against the canonical version the currently installed tool would produce, so drift is caught the same way any other repository hygiene issue already is. It had to be wired through the same read-only reporting path used everywhere else in this orchestrator specifically so that both a plain read-only run and a verdict-writing run describe the identical set of problems; an agent must never see a different picture depending on which mode it happened to run.
## [2026-07-24T17:22:22.097Z]
Documented, on RunCheckOptions itself, the distinction between an issue-gating injected input (one written as an options?.<key> conditional whose absent branch yields no issues, currently the review-cadence clock and the rules-artifacts snapshot) and a side-effect switch deliberately set at only one call site. The distinction exists because every call site invoking this function must supply every issue-gating input or it silently reports a narrower issue set than the others with no error surfaced anywhere — this has already happened twice in this codebase, once for a fill-stage report and once for the portal, each caught only by manual review rather than any check. The new comment points future editors at the deterministic rule that now enforces this invariant across every call site, so the next issue-gating input added here does not repeat the same silent-omission mistake.
## [2026-07-24T18:07:02.529Z]
Extended the note on the options type to spell out the obligation a new optional field carries. The rule that verifies call sites now also demands that every optional field be classified: either expressed as the conditional whose absent branch yields no issues (so it derives automatically and every call site must supply it), or recorded as a switch that can never add, remove, or alter an issue. Without that second half the verification silently narrows — a gate written as an if-statement, or as a conditional whose alternative is not an empty list, is never derived, never demanded of any caller, and never noticed, which is precisely the class of silent omission the rule exists to prevent. The comment now tells a future editor which of the two a new field must be declared as, because the choice is not inferable from the field's type and getting it wrong costs nothing at compile time and everything at review time.
## [2026-07-27T14:26:36.021Z]
Replaces the coverage scan's git-tracked-file conflict detector with one that reads a real git-tracked-file list injected by the caller, instead of assuming the coverage file list itself came from git. That file list is actually a filesystem walk which already excludes gitignored paths, so the old detector compared a filtered list against itself and could never fire. The new check compares an injected real git-tracked list against the walked list directly, flags any file present in one but not the other, excludes the graph's own directory and any nested graph, and grades severity by whether the file sits under a required coverage area.
## [2026-07-27T15:11:12.808Z]
The tracked-file-gitignored anomaly check previously treated any tracked file absent from the disk walk as gitignored, but the walk also skips a symlink, a submodule reference, and residue from a directory it could not read — none of those is a gitignore match, and flagging them produced a false claim about a symlink and destructive advice for a submodule reference. The check now only reports a candidate after confirming it against the real ignore rules, the same confirmation the retired detector used, restoring the read that a prior simplification of this check had dropped.
## [2026-07-27T15:56:59.551Z]
The coverage section now optionally runs a type-level classification lattice when coverage.type_level is on: a file matching exactly one non-strict classifying type is treated as covered without needing a node of its own, a file matching two or more non-strict types (and no strict type) is now a blocking ambiguous-node-type error naming both types, a file whose matching type's when could not be evaluated is now a blocking file-unreadable error, and the plain unmapped-files message for a file matching no type at all now says so explicitly. The flag stays off by default, leaving today's file-mapping coverage unchanged.
## [2026-07-27T16:43:31.573Z]
Fixed two review findings in the type-level coverage lattice: the bulk unmapped-files/uncovered-advisory listing (and its count) now drops ambiguous, strict-claimed, and unreadable files, since each already carries its own more specific verdict — only genuinely-unmatched files remain, so the 'no type for this file' message no longer needs a hedge. Also gave ambiguous-node-type its own dedicated suggestedNext branch, since it is a STRUCTURAL_CODES member with no owning node and the generic structural fallback was discarding its two-exit guidance for a useless '.yggdrasil' pointer.
## [2026-07-27T17:33:29.050Z]
The check result gained the data its header and coverage section need to stop under-reporting coverage when type-level classification is on: how many files are satisfied through an architecture type rather than a node, and how many architecture types can even classify a file at all. The second count is computed independent of whether a coverage scan runs at all, since it reflects the architecture alone, and it is what lets the caller say plainly when the setting can never match anything yet.
## [2026-07-27T18:23:50.928Z]
The check result's node-owned count reused the pre-existing coveredFiles field, which also counts files under a coverage-excluded root as covered — so the header's new node-owned label was claiming ownership for files no node actually maps. Two new fields now separate that conflated total into files an actual node mapping covers and files sitting under an excluded root, computed from the same primitives the coverage scan already produces, so a rendering surface can name each honestly instead of inheriting the older count's blended meaning.
## [2026-07-27T19:29:24.915Z]
The tracked-and-gitignored anomaly check now derives severity from the same longest-match coverage-tier split every other coverage check uses, rather than a separate required-only test that ignored excluded roots entirely — a force-added gitignored file under an excluded root no longer blocks a repository that never opted into coverage there. The check also now exempts a file named directly (not via a directory or glob) in a node's mapping, since such a file is hashed and reviewed regardless of gitignore status and was never actually invisible; flagging it there duplicated an existing, more specific check with a contradictory remedy. The type-level unreadable-file message now distinguishes a file that genuinely could not be read from one that was simply over the content-scan size limit, giving the latter an honest description and an actionable next step instead of a misleading permissions-error framing, and reports one issue per file naming every affected type instead of one issue per file-type pair. The zero-classifying-types notice no longer claims every file needs an explicit node when nothing on a fresh, require-nothing project actually needs anything. The unmapped-file 'no type for this file' explanation keeps its actionable command in the next-step field rather than the explanation, and is now attached only to a file the classification lattice actually evaluated and found no match for, never to one the lattice silently skipped before classifying anything at all.
## [2026-07-27T19:36:14.114Z]
Removed two now-unused local imports (normalizeRoot, matchesRoot) left over after the tracked-and-gitignored anomaly check was rewritten to derive severity from the shared coverage-tier split instead of computing it by hand; both names are still re-exported for other modules that import them from this file.
## [2026-07-28T12:11:34.790Z]
Coverage exclusion is now absolute rather than a longest-match comparison: the new dead-required-root warning is wired into the coverage section, firing unconditionally since it needs no file list. Removed a guard that used to reconcile a disagreement between the classification lattice's exclusion handling and the coverage tier split — that disagreement can no longer occur once both go through the same exclusion authority, so the guard was dead code. Doc comments describing the retired longest-match precedence are corrected to describe the new absolute rule.
## [2026-07-28T12:32:04.677Z]
Removed a redundant initial empty-array assignment to coverageIssues now that the coverage section seeds it directly from the dead-required-root check, since the redundant initializer was flagged as an unused assignment by the linter.
## [2026-07-28T13:56:14.741Z]
The zero-classifying-types notice constant moved to check-codes.ts (this file never consumed it directly, only re-exported it to two command files) — and runCheck now declares one shared FileContentCache and threads it into both validate() and computeTypeCoverage(), so an uncovered file's content is read at most once per check run instead of once per consumer. The gitTrackedFiles parameter/field naming was also renamed to coverageVisibleFiles throughout, to name what the value actually is (the coverage-visible file list, not a git-derived one).
## [2026-07-28T15:37:18.445Z]
The coverage section's type-level classification now runs earlier in the check sequence so its result is available to the relation pass, which needs to know which files are type-covered (and as which type) before it can widen its import enumeration to include them. This amends the ambiguous-node-type NEXT text landed previously to name the new live type-relation gate, since resolving that ambiguity may surface fresh findings once the file joins it.
## [2026-07-28T19:46:18.628Z]
Hoisted the file-type classification earlier so the reviewer-required check can see it too, and issue reporting now carries a review subject's own identifying key so a finding with no owning component can still name the exact file it is about — in a summary row, in the suggested next command, and in a blocking unreadable-file error — instead of collapsing it into a generic repository-level bucket or pointing the fix at the graph directory.
## [2026-07-30T07:13:52.111Z]
Accepts an already-computed type-coverage classification so a caller that classified once for its own use this run can reuse it here instead of paying for a second classify pass over the same uncovered files.
## [2026-07-30T13:40:04.689Z]
Assembles a per-type report of what the type-level tier enforces on a covered file and what it does not, with the reason, so a reader can no longer mistake type coverage for full enforcement when a rule attached to a type cannot actually run against a given file.
## [2026-07-30T15:20:29.319Z]
Threads the real verified pairs (not just the static drops) into the type-visibility report, so enforced/advisory are derived from what actually ran rather than inferred from the absence of a drop. The per-type block now separates advisory rules from enforced ones under their own heading, since a rule that only warns must never be reported the same way as one that blocks.
## [2026-07-30T15:29:45.917Z]
Shrank the type-visibility wiring added here to a single-line call into the new shared toAppliedPairs helper (type-visibility.ts), instead of restating the filter/map inline — this file's own reviewer prompt has little headroom, so the derivation logic itself now lives next to the type it produces rather than being duplicated at each call site.
## [2026-07-30T15:33:11.729Z]
Replaced the intermediate applied-pairs variable with a direct verifiedPairs: VerifiedPair[] (a type already imported here) and moved the toAppliedPairs call to the single call site that needs it — this file's own reviewer prompt has almost no headroom, so every added line here costs real margin against the tier limit.
## [2026-07-30T15:36:43.556Z]
Moved the type-visibility assembly inside the lock-verification try block, reading verification.drops/pairs directly instead of copying them into two separate outer variables first — this eliminates the intermediate staticDrops/verifiedPairs bookkeeping entirely (one hoisted typeVisibility variable, same lock-invalid-safe undefined default, replaces both). This file's own reviewer prompt has almost no headroom against the tier limit, so every avoidable line here matters.
## [2026-07-30T15:40:23.848Z]
Trimmed two inline comments to reclaim reviewer-prompt margin on this file — it has almost none against the tier limit, so a few extra words here are the difference between a prompt fitting and not.
## [2026-07-30T18:01:02.850Z]
Passes the lock verification's uncomputableTypeCoverage list into the type-visibility report it assembles, so the whole-run yg check summary can tell a type-covered file whose rules an aspect implies cycle stopped from being resolved at all apart from one whose type genuinely attaches nothing — a distinction the per-file surfaces (yg owner, yg context --file) already made, but the whole-run surface did not.
## [2026-07-31T18:00:25.790Z]
The live relation pass's path resolver is now built from an owner index already guarded against this graph's exclusion set, rather than a raw one — a Go or Java package import's representative-file lookup could otherwise land on the one excluded file inside an otherwise ordinary package and silently drop a cross-node dependency that is legitimately reachable through the package's other, non-excluded files.
## [2026-07-31T19:30:10.499Z]
The hidden attention-dump calibration view now builds its import resolver through the same exclusion-guarded constructor every other live-relation-pass caller uses, closing the one remaining direct caller of the unguarded constructor.
## [2026-08-02T06:21:53.463Z]
the silent structural-attention index widens its file-grouping step so a file the architecture classifies only by type, with no owning component, can be admitted to its own comparison group instead of being silently excluded from every structural-deviation comparison.
## [2026-08-02T07:57:11.441Z]
The same widening also bumped the on-disk schema version of the local structural-attention index, since a reader built for the old two-segment family key would otherwise silently misread the new three-segment one. This costs an adopter nothing beyond one extra rebuild of that local, gitignored index on their next run — nothing durable changes.
## [2026-08-02T10:20:10.846Z]
The check gate now constructs the type-level classification cache once per run and passes it into the coverage computation, so an unchanged file's classification is skipped on the next invocation instead of re-evaluated from scratch.
## [2026-08-02T11:40:12.698Z]
No longer constructs its own classification cache directly — computeTypeCoverage now owns that construction and reuse internally, since it already receives the same graph this check run classifies against. Moving the construction (and the relation it required) off this node also keeps a growing check orchestrator, already at its reviewed relation ceiling, from needing an extraction under time pressure the next time a check gains a dependency.
## [2026-08-02T12:19:13.341Z]
Corrects the previous entry: the function that now owns cache construction is computeTypeCoverageCached, a separate function from computeTypeCoverage — the latter stays pure (no disk cache) so a caller that classifies a fixture's graph in place for a live answer, as this codebase's own test suite does in several places, never triggers a cache write it did not ask for. This node calls the Cached variant explicitly.
## [2026-08-02T14:37:33.458Z]
Corrects an earlier entry on this node: no caller here classifies a fixture's graph in place — every test that exercises this check without the persistent classification cache first copies its fixture project to a scratch directory, so there was never anything on disk for a silently-constructed cache to write into. computeTypeCoverage stays pure by default for a different reason: a TypeClassCache write is a durable side effect on the real filesystem, unlike the FileContentCache these functions already take unconditionally (an in-memory read cache that never writes anything), so whether a call can ever touch disk must be readable off the function's own name rather than discovered by tracing which optional argument happened to be passed. This node still calls computeTypeCoverageCached, not the bare function — unchanged from what the earlier entry described, only the reason for the bare function staying pure was wrong.
## [2026-08-03T00:22:30.713Z]
Two doc comments in check.ts -- one scoping the silent feature-field index before it writes, one on the hidden calibration dump's own doc -- described the coverage-visible file set they draw from as "the repository's tracked, graph-governed universe," contradicting this same file's own authoritative parameter doc for coverageVisibleFiles a couple hundred lines above, which already states plainly that the set is "NOT git-derived." A third local variable, inside the uncovered-file scan, was named tracked for the same reason. feature-index-write.ts carried the identical confusion four times over: its module doc called the index "TRACKED-ONLY," two more comments described its includedPaths parameter the same way, and one inline comment repeated it. Every one of the seven now says what the set actually is: the walkRepoFiles disk walk, minus nested-graph subtrees, independent of the git index. The genuinely git-derived tracked-file-gitignored detector elsewhere in check.ts is unaffected -- it really does consume real git-tracked-file output and already said so correctly.
## [2026-08-03T05:51:54.377Z]
runCheck now accepts an optional same-run handoff of a component-free rule's runtime disposition and folds it into the type-visibility report, so a caller that just watched a check fail can say plainly why instead of only marking the pair unverified.
## [2026-08-03T08:00:52.342Z]
The unverified case now distinguishes a pair --approve can still change from one this same run's fill already proved cannot run at all: the suggested-next pick and the per-pair message both prefer a fillable pair's own generic remedy, falling back to a cannot-run pair's real remedy only once nothing among the unverified set is fillable. Before this, every unverified pair shared one generic 'run --approve' message regardless of whether that would do anything for it, so a report could point an agent at a command it had, in the same breath, already shown cannot succeed.
## [2026-08-06T13:31:35.917Z]
The read-only check orchestrator had grown past the point where the reviewer could assemble a prompt for it at all: the rule governing its diagnostic wording exceeded the configured prompt ceiling, so that rule could not be judged and the repository could not go green. The ceiling had already been raised four times for this same surface, and raising it again would only defer the same failure. Instead the read was separated by responsibility — the public issue and result contract; the translation of one verified pair's state into the issues it reports; log integrity, format and the mandatory-log requirement; everything gated on the verdict lock being readable; the uncovered-file and committed-but-ignored scans; the coverage tiers together with the type-level classification lattice; and the single next step a finished check points at. What remains is an orchestrator that owns the order of operations and the injected-input gating that decides which optional checks run, and little else.

Two orderings were preserved deliberately because the whole point of them is subtle. Import resolution still runs before structural validation, so a rule whose applicability depends on a file's real dependencies is answered from resolved imports rather than from an absent index that would silently answer every such condition the same way. And relation-conformance and type-gate findings are still only reported when the lock can be read, even though the pass computing them now runs earlier — a corrupt lock produces exactly the output it did before.

Behaviour is unchanged throughout: the same issues in the same order, the same wording, the same exit codes. The options contract stayed with the orchestrating function rather than moving out with the other types, because the rule proving every caller supplies the engine's injected inputs derives that option set from the same declaration, and separating the two would leave it with nothing to check call sites against.
## [2026-08-07T11:46:36.140Z]
The read now accepts two more already-computed inputs from a caller that ran them in this same process, alongside the type classification it already accepted: the import-resolution pass, and — only for a caller that has written nothing since it computed it — the lock verification. Reusing the pass is sound on every path because it reads source and a fill writes only lock and log files. Reusing the verification is sound on exactly one path, the cost preview, which returns before the verdict writer is even constructed; a real fill writes verdicts in between and must re-verify, or the report would describe a lock that no longer exists. Both were being redone from scratch, so a preview advertised as free cost about what a real run costs.
## [2026-08-12T12:58:51.873Z]
The check engine now accepts the scope of the current change as plain data, and its result and findings carry the fields a scoped report will need. Nothing reads any of it yet: a run given a scope returns exactly what a run without one returns. The shape is fixed first so every surface that asks for a report is already handing it over before the step that acts on it exists. A surface still missing it at that point would quietly report a different set of findings from every other, with nothing anywhere to signal the difference.
