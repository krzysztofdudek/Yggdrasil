## [2026-05-29T22:21:12.791Z]
The graph validation checks were extracted from a single oversized validator module into domain-grouped modules (architecture, aspects, mapping and coverage, relations and flows) plus a small shared helper, leaving the orchestrator that runs every check and assembles the result in place to compose them. The move was verbatim: no change to which rules run, their order, their diagnostic messages, or their verdicts. The motivation is that one very large source file degrades reviewer accuracy and risks context-window truncation that can spuriously reject unchanged code; keeping each unit small enough to be reviewed in full removes that failure mode.
## [2026-05-30T18:08:09.451Z]
The vocabulary for how a rule is verified was reduced from three kinds to two. Previously a rule was checked by one of: a human-language reviewer, a single-file programmatic check, or a graph-aware programmatic check. The two programmatic kinds are now a single "deterministic" kind, leaving just deterministic-or-reviewer.

The motivation: the three-way split was drawn on the wrong axis. It described HOW a programmatic check reached its context (one file at a time, versus the whole graph), but the distinction that actually matters to a rule author and to cost is whether verification is local-and-free or requires the paid, non-deterministic reviewer. The single-file kind was already a strict subset of the graph-aware kind — every input the former could see, the latter also provides — so maintaining two of them forced rule authors to make a false choice up front and forced the engine to carry two parallel handling paths for one concept. Collapsing them removes that false choice and the duplicated handling, and routes every programmatic check through the one graph-aware path.

The language a programmatic check infers for a source file is determined solely from that file's extension, so a check no longer declares which languages it targets. A rule's verification kind being deterministic is also no longer carried as a separate synthetic identity signal — a deterministic rule's identity is fully covered by the files it already tracks — which keeps re-verification of such rules free.
## [2026-05-30T18:40:33.542Z]
A reference path embedded in a validation message was written without normalizing its separators, which on some platforms would emit a non-POSIX path at an output boundary that must always use forward slashes. The path is now normalized before it is placed in any message; the filesystem path used to stat the file is left platform-native, since only the message is an output boundary.
## [2026-05-31T16:03:32.849Z]
Replaced the hand-inlined path-separator normalization with calls to a single shared helper. The same small idiom — convert backslash separators to forward slashes, and in most places also strip a trailing slash — had been copied across many modules, so the normalization rule lived in dozens of places at once and any change to it risked drifting them out of step. Consolidating it behind one well-named helper means the rule lives in exactly one spot and each call site reads by intent instead of by a repeated regex. Behavior is unchanged: the helper bodies are byte-for-byte equivalent to the expressions they replace, and the full test suite passes identically.
## [2026-06-03T07:05:08.637Z]
Aspect-contract validation now recognises three reviewer kinds inferred from which rule source is present: content.md means LLM, check.mjs means deterministic, and neither-file-but-implies means an aggregating aspect that is a named bundle with no own reviewer. The kind is inferred from file presence so authors do not have to declare it; an explicit reviewer.type, if given, must agree with what the files imply, and a mismatch is a validation error.
## [2026-06-08T19:22:36.423Z]
An aspect's effective enforcement level is the strongest level contributed by any source that reaches a node, and that set of sources must include the aspect's own declared default — not only the levels named at individual attach sites. The downgrade guard previously compared an attach site's level against the other attach sites alone, omitting the aspect default, so a lone attach site that named a weaker level than the aspect's default escaped detection and silently weakened enforcement. The guard now folds the aspect default into the baseline it compares against, so any attempt to attach an aspect at a level below what it already guarantees is rejected.

Separately, a conditional applicability predicate may key off a consumed port, and such a predicate must reference a port that genuinely exists somewhere in the graph; a predicate naming a phantom port is a graph error. That check previously only fired for the paired relation-and-port shape and missed the bare port-name shape. Both shapes are now validated against the full set of declared ports so an unknown port is reported regardless of how the predicate spells it.
## [2026-06-13T10:52:16.035Z]
A reviewer-configuration error called a project with only mechanical checks an 'AST-only' project. The system's vocabulary for mechanical check.mjs aspects is 'deterministic', never 'AST', so the wording was aligned to avoid an undefined term an agent has to guess at.
## [2026-06-19T19:18:52.818Z]
Reject companion misuse at validation time before any review runs: a companion resolver only makes sense for an LLM reviewer, so an aspect that ships one without review content, or alongside a deterministic check, is a configuration error.
## [2026-07-05T20:58:33.243Z]
Missing-reviewer detection now consults the effective, non-draft judgment pairs through the canonical pair computation and stays silent when none exist, so a keyless project is a legal green state rather than a blocking error.
## [2026-07-05T21:17:23.050Z]
The conditional missing-reviewer detection now short-circuits before the full pair enumeration when the graph defines no judgment (LLM) rule at all, so a script-only project never pays the per-component file walk on a routine check; the accurate effectiveness decision still runs when a judgment rule is present.
## [2026-07-10T08:10:35.051Z]
Added a graph-wide contract that the error-direction label is legal only on a locally-run rule, because that label describes how such a rule can be wrong: it may over-flag, it may only ever flag provable cases, or it may be exact. On a rule judged by a reviewer the label is meaningless, so it is now a blocking error that names the fix. The check fires only where the label exists, so an absent label is always a clean pass.
## [2026-07-10T10:30:22.500Z]
Added a dead-attach linter that raises a non-blocking warning when a rule ships an actual reviewer definition, is not parked as a work-in-progress, yet — after every channel by which a rule can reach a component and after all of its conditional applicability predicates — applies to no component at all. Such a rule looks enforced but is never checked anywhere ("dead law that looks enforced"), which almost always signals a graph-authoring mistake: a predicate that matches nothing, or an attachment aimed at a component or type that does not exist yet. The linter reuses the very same effectiveness computation the reviewer itself uses to decide where rules apply, so it can never disagree with what actually gets verified. It stays completely silent while the model still has no components, so a design-first workflow (rules authored before the code they will govern) is never spammed. It excludes bundle-style rules that carry no reviewer definition of their own, because an unused bundle is already reported separately.
## [2026-07-12T10:11:13.622Z]
Adds the review-cadence linter: a rule may carry a standing review-by date, and once that day has passed the linter emits one grouped, non-blocking warning asking a human to renew or retire the rule; it never changes the date, never writes a verdict, and never gates approval. The comparison runs against a clock supplied from outside so this layer stays clock-free and deterministic, and the date is excluded from every verification hash so recording it invalidates no existing judgment.
## [2026-07-15T07:22:28.586Z]
Adds a per-type dead-attach linter (architecture-default-aspect-unreachable, warning): when the architecture declares an aspect as a default of a node type but that aspect, after the full cascade and every when predicate, is effective on zero nodes of that type, the rule looks enforced for the type yet verifies nothing there. This catches the class where an aspect's own when cancels the architecture's attachment of it to a sibling type. It reuses the verifier's own effective-aspect engine so cascade and when semantics are never re-derived, is silent for a type with no instances, and never blocks — it surfaces a graph-authoring mistake as a warning.
## [2026-07-28T19:46:03.694Z]
The reviewer-required check now also counts a judgment rule that only ever applies to files enforced by their architecture type alone, so a project with such a rule but no configured reviewer is still caught.
## [2026-07-30T13:40:05.016Z]
The two dead-law linters now count a rule effective only on files enforced by their architecture type as live, and treat a type with no components but at least one matching file as having instances -- so the type-level tier no longer causes a false dead-law or unreachable-default warning for law that is genuinely enforced only through type coverage.
## [2026-07-30T15:20:25.475Z]
The two dead-law linters now union in only the type-covered aspect ids that could actually produce a pair (computeTypeReachableAspects), not the raw cascade-effective set. A whole-unit (per: node) rule reachable only through a type-covered file was being counted as live the instant a single file matched its type, even though it can never run there — the exact opposite of what these checks exist to catch. A file-level rule reachable the same way still counts as live, unchanged.
