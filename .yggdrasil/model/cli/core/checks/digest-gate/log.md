## [2026-07-24T12:09:09.601Z]
This is the actual comparison behind the new staleness signal: given a snapshot of the relevant committed file contents and the canonical text the installed tool currently produces, it decides whether the two have drifted apart, whether one is missing, hand-edited so its own embedded checksum no longer matches, duplicated, or simply older than what the installed tool would write today. It is written as a pure function over a snapshot handed to it, rather than reading files itself, specifically so the core verification logic keeps zero direct filesystem dependency for this concern; the actual file reading and canonical-hash computation stay at the outer boundary of the tool where such side effects already live, which keeps this comparison fully exercisable without touching a real filesystem.
## [2026-07-24T14:33:22.209Z]
Two corrections, both about telling the truth to whoever reads this check's result.

The import line this gate looks for in a project's Claude-specific instructions file is now recognized by the same fence-aware line scan that already governs the digest block, rather than by a bare line comparison. A repository that documents its own agent setup routinely shows that exact import inside a fenced example, and both the writer and this gate counted such an example as the real thing — so the writer skipped the file believing it already installed, and this gate confirmed it. The two agreed with each other and both were wrong, which is the worst shape a disagreement can take: no warning anywhere, and an agent silently working with no rules at all. One scanner for both sides makes that impossible to reintroduce.

The finding also no longer names a synthetic component. It is about three files at the repository root and belongs to no component in the graph; attaching a placeholder made every component-shaped surface report one that does not exist — a count in the grouped report, a row of its own in the per-component summary, and an entry in the web view's component lists with no page behind it. The finding now carries no component, and the report renders repository-level findings without the component framing.
## [2026-07-24T18:31:38.674Z]
Switched the human-facing finding labels this check emits — for the digest-block host file, its import line, and the standalone copy of the digest body — to be built from one shared module of artifact names rather than typing each name out again in this file. The values themselves are unchanged; only where they come from moved.

This check exists specifically to catch drift in the installed rules distribution, so it was the wrong place to keep re-typing the very names whose drift it is meant to catch — a second, independent spelling of the same name is exactly the shape of bug this file's own history already produced once, when a different collaborator's independent spelling fell out of sync with the installer's and made a correctly-installed repository look uninstalled. A new enforced check now refuses a re-typed literal of any of these names in this file (and its two collaborators), so that class of drift cannot recur here either.
## [2026-07-24T18:58:28.460Z]
### Supersedes: 2026-07-24T18:31:38.674Z

The entry added here when the artifact names moved to a shared definition claimed that the new enforced rule refuses a re-typed literal of any of those names in this component and its two collaborators. That claim is too strong, and this entry corrects it before anyone relies on it.

What the rule actually refuses is a string literal whose WHOLE value is one of the names — the digest host file, the file that imports it, the standalone copy's directory or its filename, those two joined, or the import-line spelling. Matching is case-insensitive and by exact value, never by substring. Its reach is now every file in the CLI's own shipped source rather than the three that had already drifted, because the divergence it exists to prevent is a FOURTH place learning these names independently, and a rule watching only the three already-fixed files could never see one.

The human-facing labels this component emits are therefore not protected by it. Each of them embeds a name inside a longer phrase — a name followed by a description of the artifact — and no such string equals any guarded value, so re-typing one of those phrases by hand would pass unnoticed. That is a deliberate boundary rather than an oversight: those strings are display text, and only the values used to RESOLVE and READ an artifact can drift into the failure this rule guards against. Building the labels from the shared definition remains the convention here, but it is a convention, not something enforced.
