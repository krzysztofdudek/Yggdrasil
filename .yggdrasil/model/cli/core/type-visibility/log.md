## [2026-07-30T13:40:12.338Z]
New engine assembling the type-level tier's honesty artifact: for a file covered by its architecture type, which attached rules actually enforce, which do not and why, a half-expanded rule bundle, where the inherited chain stops, and the files that satisfy coverage with no enforcement at all.
## [2026-07-30T13:55:58.986Z]
Adds an enforced-file count per aspect id (enforcedCounts) alongside the plain enforced list, so a rule live on only one accidental file reads as a count of 1 in the rendered report rather than an uncounted name -- the mitigation the zero-enforcement honesty line's own design calls for.
## [2026-07-30T15:20:04.769Z]
Enforced/advisory are now derived from the real (file, aspectId, status) pairs that produced a pair, never inferred from the absence of a drop row — a silent gap upstream can no longer be misread as enforcement. Added an advisory bucket (enforced vs advisory are reported under separate headings, since a rule that only warns must never be counted as enforced) and three new drop reasons (aspect-undefined, unreadable, binary-subject) so every non-pair the nodeless enumeration decides carries a reason. The chain-termination wording for an absent parent chain no longer claims whether the author omitted parents: or wrote an explicit empty list, since the loader collapses both before this report ever sees them.
## [2026-07-30T15:27:38.296Z]
Added toAppliedPairs, the one shared narrowing from a pairs array (component + nodeless) to the nodeless (file, aspectId, status) rows this artifact treats as ground truth for enforced/advisory — extracted so yg check and yg context --file's typed view derive applied pairs identically rather than each restating the same filter/map, and so the derivation lives next to the type it produces.
