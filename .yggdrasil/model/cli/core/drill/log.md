## [2026-07-11T03:28:01.378Z]
New falsifiability runner. It discovers a per-rule library of example files whose directory prefix encodes the expected verdict (a "violates" example MUST be refused; a "satisfies" example MUST pass), re-runs the real rule over them, and classifies each as pass, a MISS (a violation the rule failed to catch), a FALSE-ALARM (a clean example the rule wrongly rejected), unrun (could not be evaluated), or unsupported (the rule needs context this mode cannot supply). This exists so a maintainer can confirm a rule still catches what it should after the rule or the surrounding code evolves. The module is kept reproducible and free of both the code-execution layer and the network reviewer by having the command layer inject those two impure operations; everything the module does itself (discovery, hashing, prompt assembly, classification, exit-code logic) is deterministic. It never writes saved verdicts.
## [2026-07-11T11:17:05.512Z]
Two source comments here described the surrounding code inaccurately, which misleads a future editor more than no comment would. One claimed a content hash normalized line endings when it in fact hashes raw bytes unchanged; because that hash only drives internal change-detection and never contributes to a recorded verification outcome, the honest description is that it is deliberately left un-normalized. The other did not explain why the suppression-resolution step re-encodes already-decoded text instead of reading the original bytes — the two are byte-for-byte identical for the valid-UTF-8 inputs a case corpus is required to consist of, and that equivalence is now stated so the harmless re-encoding is not mistaken for a defect and "fixed" into one. Unresolvable pointers to an unpublished design document were also removed from comments here, with their substance kept.
## [2026-07-14T17:38:01.035Z]
Added a corpus-membership filter that decides which recorded regression-drill outcomes are still actionable for the read-only attention and health surfaces. Recorded drill outcomes accumulate in an append-only local telemetry log, so an outcome for a case that was later removed or renamed — or one measured against an external, out-of-repository corpus — lingers indefinitely. The read-only surfaces were treating that residue as a live "regression no longer caught" alarm even though nothing in this repository can re-run or retire such a case. The filter keeps an outcome only when it came from an in-repository run and its case still exists in the aspect's current regression corpus; every orphan is dropped silently because there is nothing left to act on.
