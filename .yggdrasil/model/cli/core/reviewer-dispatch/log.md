## [2026-08-06T14:23:26.192Z]
Talking to the reviewer is now its own kind of code, held apart from the stage that sequences it. Everything that actually asks a model for a verdict lives here: grouping a run's fillable pairs by resolved tier so a provider is stood up and probed once per tier rather than once per pair, loading subject and reference bytes byte-identically to the verifier, resolving paired files and suppressed line ranges before anything is spent, measuring the assembled prompt against the tier's ceiling, casting the consensus votes, and folding the answer into a content-addressed entry. The rule that a component's code must produce the same result from the same input does not apply to it, and cannot: a model's answer to identical input is not guaranteed identical, so demanding otherwise would forbid the work rather than improve it. What holds instead is that every verdict is stored under a hash of the inputs that produced it and is honored only while those inputs still hash to the recorded value, so reproducibility is proven where the result is kept rather than where it is obtained. Every other rule the surrounding code answers to still applies here, including the mechanical one that this code must take its clock, its randomness, and its environment as inputs handed to it rather than read off the running process. Separating it removes the only reason anyone had to switch a rule off in this area: previously both files carried a marker disabling that rule, which hid the mismatch instead of naming it and left a small unchecked island inside code that is otherwise judged in full.
## [2026-08-07T11:02:26.017Z]
The size of the prompt actually sent to the reviewer is now recorded alongside the verdict it produced. It is a record of inputs the verdict hash already covers, never an input of its own, so writing it invalidates nothing and an older lock that lacks it simply keeps being measured the old way. It exists so a later verification can answer the prompt-size question without resolving companions and reassembling the prompt, which was the dominant cost of checking a repository where nothing had changed.
## [2026-09-06T17:57:06.854Z]
The per-pair work of gathering what a judge sees, and computing the identity the resulting verdict is stored under, moved out of this stage into the single module that now owns it. Nothing about the dispatch changed: this stage still decides whether to spend, still gates the assembled size against the tier's ceiling, still fails closed on every disposition that is not a real judgement. What changed is that it no longer holds its own copy of the assembly, so a second kind of judge cannot end up judging something subtly different from what this stage would have sent.
## [2026-09-23T11:59:58.191Z]
The external-judge channel is removed: the reviewer configured in yg-config.yaml, through yg check --approve, is the only judge of a prose rule, by the owner's decision that there is to be no other. The review package assembly now serves the fill alone, and verdicts an earlier release recorded through the channel still re-prove by the same hash, so no adopter's lock is broken.
## [2026-09-23T19:26:20.331Z]
A reviewer that could not run, or failed mid-review, was reported with one generic sentence about an endpoint, network and credentials, whatever the provider was. A missing CLI binary, a missing API key, a refused key, an unknown model and a timeout therefore looked identical, and the adopter could not tell whether to install something, log in, fix the model name or raise the timeout. The availability gate now asks the provider for its own reason and prints it, every infra report ends by naming debug: true and the debug log where the provider full output goes, and the reason is kept on each infra report entry so the closing summary can show it.
## [2026-09-23T19:41:28.671Z]
When a reviewer tier is unreachable, the fill reported it once per tier on stderr and nowhere else. It now also keeps the diagnostic per pair, so the post-fill report and the machine document can say which pairs failed for that reason and point at fixing the provider instead of repeating the fill.
## [2026-09-23T20:10:06.099Z]
The description justified the single review-package assembly by the removed external-judge channel. The reason that still holds is that the verdict is stored under the hash of exactly the prompt the reviewer was sent, so the description now gives that one.
## [2026-09-23T23:00:43.603Z]
The reviewer phase reports each pair's start and outcome to the progress tracker as fill events instead of a text sink, following the fill stage's move from finished sentences to data that a formatter words.
## [2026-09-24T01:18:20.712Z]
Consensus votes now run concurrently because they are independent calls over identical input and running them one after another multiplied a pair's wall time by the vote count. A provider-error vote no longer counts as a refusal: only verdicts vote, and when the verdicts are not a majority of the votes asked for the pair is left unjudged as infrastructure, since a transient timeout must never harden into a recorded refusal. The approval reason is handed on to the local events line so approvals can be audited, and the first-fill prompt-size remedy now says truthfully that raising the cap re-verifies nothing while moving tiers re-reviews the aspect; the old wording read as if raising the cap re-billed the whole tier.
## [2026-09-24T08:10:12.743Z]
A reviewer that cannot be reached is reported with a count that agrees with its noun, in the one grammar every command now speaks.
## [2026-09-24T13:51:18.898Z]
The over-limit prompt remedies are numbered at the margin instead of indented inside the message, and the resolved companion files are introduced to the reviewer as 'paired files' rather than 'file(s)'; the prompt revision is unchanged, because the revision this wording belongs to has not shipped. The engine emits data and the renderer owns layout, so no what, why or next this module writes carries a newline followed by indentation any more: a numbered set of exits, a file list or a node list now starts every line at the margin (or sits on one line), because the renderer aligns each line of a message under its field and a message indenting itself came out double-indented in text and carried stray spaces into the yg-check/1 JSON a machine reads. A test scans the shipped source for such a message and another checks every message string of yg check --json on the golden corpus.
## [2026-09-24T14:24:01.064Z]
The same idea went by several names across the docs, the agent manual and the CLI's own messages, so a reader could not tell whether two words meant one thing or two. The words this component prints now follow the Glossary, which defines each term once: the reviewer is only the model configured under reviewer:, a rule is a reviewer rule, a script rule or a bundle, the --approve run is a fill whose verdicts are passed or refused, a rule's draft/advisory/enforced is its status, covered only means the graph accounts for a file, tier only means a reviewer tier, and a file enforced by its type alone is a type-covered file. JSON fields, codes and config values are unchanged, so no machine consumer is affected.
## [2026-09-24T14:30:34.041Z]
When the reviewer cannot run, the remedy now leads with the provider's own reason ("Make the reviewer reachable: …"), then the re-run. It said "Fix the cause above", but the report on stdout carries this remedy and never the warning on stderr where the cause was, so a reader of stdout — every agent and every JSON consumer — was pointed at text it never saw, and the next step fell back to the debug hint's config file.
## [2026-09-24T15:09:44.894Z]
Reapplied on top of the output-guards work, which routes every write through the output layer and flattens multi-line messages: this node's changes for the Next contract (the whole command's cost, the user's decisions asked for, one next step per run, the node-not-found error shared by every command, the context gate narrowed to errors that affect the context) now write through the same layer, so both changes hold at once.
## [2026-09-24T15:11:15.838Z]
Two release lines met here: one that routes every piece of CLI output through the shared output layer with count() for grammatical numbers, and one that gives each meaning a single word across the CLI text (reviewer rule, script rule, bundle, passed, look-alike group, type-covered file, status) with retired synonyms guarded against. Both are kept whole in this node: its messages use the glossary's words and still go through the output layer, so neither change undoes the other.
## [2026-09-25T13:39:25.728Z]
Reapplied on top of the output-guards and one-terminology work: this node's Next-contract changes (the whole command's cost, the user's decisions asked for, one next step per run, the shared node-not-found error) now write through the output layer and use the release vocabulary — reviewer rules, and a fill's outcomes counted as passed — so both changes hold at once.
## [2026-09-25T17:43:46.569Z]
Type-only imports now count as dependencies between components, the same as value imports. This phase named the verdict writer, the progress tracker and the diagnostic item through the fill orchestrator's modules, and the orchestrator calls this phase, a cycle between the two components. It now names them through the fill's shared primitives, taking the progress tracker as the per-pair progress interface it implements.
