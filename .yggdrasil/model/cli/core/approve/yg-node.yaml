name: Approve Core
type: engine
description: Core approve logic — records drift baseline, implements binary change detection and blackbox enforcement. Deterministic only — no LLM calls.
blackbox: false
aspects:
  - deterministic
  - posix-paths
  - what-why-next
  - diagnostic-logging
relations:
  - target: cli/io
    type: uses
  - target: cli/utils
    type: uses
mapping:
  - source/cli/src/core/approve.ts
