## [2026-09-23T20:15:57.983Z]
Non-interactive init reads the openai-compatible key from the variable the provider itself reads at run time, so the key init reports as missing or found is the one reviews will actually use, and never the OpenAI key.
