## [2026-09-06T07:20:44.508Z]
A repository does not drift into being governed by an architecture graph; someone decides that it is. Until now the product had no way to make that decision, and no way to record it. A proposed graph was moved into place by hand: nothing checked that it would even load, nothing recorded the verdicts that cost nothing to record, nothing said how much of the code already present the new rules refuse on the first day, and nothing anywhere carried who accepted what. This command is that missing signature.

Three properties are deliberate and each answers a way the hand-moved version went wrong. It is a transaction: either the repository ends with the accepted graph, or it ends exactly as it was, including when the graph turns out not to fit the code it names once it is finally beside it. It never merges: a rule taken from one graph and a component from another have never been checked against each other, so accepting over an existing graph is refused outright unless asked for explicitly, and even then the old graph is moved aside rather than destroyed, because replacing is a decision about which graph governs and not a decision to lose work. And it reports the number nothing else can supply: how many places in the code that is already here the new rules refuse today. A mined rule earns its status from how the code is usually written, never from a check that the repository is clean, so a perfectly well-formed graph can refuse hundreds of existing files the moment it is switched on. Somebody accepting one is entitled to know that before they do, not after.

It also runs before a graph exists, which makes it the second command that cannot open one first. That is why every refusal it can reach beforehand is its own rather than the shared missing-graph message, and why the rule describing the command layer had to stop saying that the initializer was the only such command.
## [2026-09-06T09:32:27.404Z]
A rule's identity is a path, and it may nest to any depth: a generator that groups the rules it mines by the area they came from gives every one of them a multi-segment identity, and that is the ordinary shape of a mined proposal rather than an unusual one.

The acceptance summary reports how many places in the code already present the arriving rules refuse today — the one number nothing else can supply, because a mined rule earns its status from how the code is usually written and never from a check that the repository is clean. That number was gathered by looking one level below the collection of rules for the record each rule carries. For a single-segment identity that lands on the rule itself; for a nested one it lands on a grouping directory where no rule and no record live. Every grouped proposal was therefore reported as carrying no measurement at all, while in fact carrying one on every rule — and 'not measured' and 'measured as none' are deliberately different claims, so the summary was making the wrong one.

The counts are now gathered per rule from the identities the graph reader itself resolved when it loaded the proposal. That keeps the place a record is looked for derived from the same reading that decides what a rule is and what it is called, so the two cannot drift apart, and it also means a record left beside something that is not a rule of this graph can never be counted.
## [2026-09-22T20:37:22.534Z]
A dry run of yg adopt now previews over a repository that already has a graph instead of being refused before it is reached. It writes nothing, and the refusal for a real acceptance pointed the reader at that very dry run to compare first, so the only way to preview was a replace flag nobody mentioned. The preview now says that accepting over the existing graph takes --replace. The Already broken count also leaves out draft rules: yg check never runs a draft rule, so the sites it would refuse are not something accepting the graph turns red, and a generator that proposes most rules as drafts made the count read far worse than what the acceptance actually brings.
## [2026-09-23T12:22:26.407Z]
The Already broken count now includes a rule whose default status is draft when some attach site raises it above draft, since yg check runs it there; leaving it out hid breakage the acceptance does bring. A Grain proposal's provenance is read only in grain-proposal/1, the version this CLI knows: a newer version may mean something else by the same fields, so its version is named instead of its fields being read under the wrong meaning. Adoption also gitignores the family-candidates files a proposal carries in, which are local analysis and never meant to be committed.
## [2026-09-23T20:24:26.047Z]
The adoption fill passes the deterministic per-check wall-clock budget into the engine, so a rule script that never returns cannot hang an adoption.
## [2026-09-23T20:49:08.693Z]
The deterministic baseline fill now passes its clock explicitly, because the engine no longer falls back to reading the wall clock itself. A mapping entry that differs from the disk only in letter case is a new refusal that depends on the files being in place, so it is deferred until a proposal is installed, like the other mapping checks.
## [2026-09-23T22:37:48.034Z]
The graph-governance and lock work met the release work already merged in this command: the adoption fill now passes both the per-check wall-clock budget for deterministic rules and the clock the engine requires as an explicit input, so a rule script that never returns still cannot hang an adoption and the engine still reads no ambient clock.
## [2026-09-23T23:00:34.739Z]
Command errors now go through the shared CLI output layer (fail, or failAndExit where the command exits at once) instead of each command writing its own red Error: what/why/next line to stderr, and the commands that kept a private failWith or emitError copy of that line now use the shared one. One place now owns how a command error reads and where it goes, so wording and stream change once for every command. When the invocation answers in JSON, that layer also writes a yg-error/1 document (code, what, why, next) to stdout, because a machine reader used to get zero bytes on stdout for a failed command and had to parse English from stderr.
## [2026-09-24T01:25:02.222Z]
The deterministic fill now also takes a worker ceiling sized from the largest unit one worker may parse, because a pool sized from the parent's resident size alone let memory grow with workers times the largest component. The adoption baseline runs that same fill, so it passes the same machine-measured ceiling the check command passes.
## [2026-09-24T07:23:25.135Z]
The CLI now speaks one output grammar: every finding is a block headed error[label] or warning[label] with lowercase labelled at:, why: and fix: fields, every command error is error[code]: what with a labelled why and a next: step, and a report ends with one next: step and optionally a then: step. Scripts that parsed the old capitalised Why:, Fix: and Next: lines are sent to the JSON documents, which carry the same facts. This command's remaining capitalised Next: or Warning: lines are lowercase, its counts agree with their nouns, and its first-level text says reviewer rule where it said LLM aspect.
## [2026-09-24T14:22:54.724Z]
The same idea went by several names across the docs, the agent manual and the CLI's own messages, so a reader could not tell whether two words meant one thing or two. The words this component prints now follow the Glossary, which defines each term once: the reviewer is only the model configured under reviewer:, a rule is a reviewer rule, a script rule or a bundle, the --approve run is a fill whose verdicts are passed or refused, a rule's draft/advisory/enforced is its status, covered only means the graph accounts for a file, tier only means a reviewer tier, and a file enforced by its type alone is a type-covered file. JSON fields, codes and config values are unchanged, so no machine consumer is affected.
