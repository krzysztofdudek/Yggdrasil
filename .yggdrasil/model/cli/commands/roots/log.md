## [2026-08-19T23:15:31.971Z]
Adds the two commands that make convention mining usable from the command line: one that reads the repository and writes a committed snapshot of the patterns it finds, and one that reports what the last such run found without ever failing a build on its own. Both stay fully dormant on a project that has not opted in — nothing is read, written, or changed anywhere else in the CLI until a roots configuration block exists. Running the mining command on a project with no such block adds one with sensible defaults automatically, printing what it added before doing anything else, so getting started never requires hand-authoring configuration first. A real problem — no initialized project here, or a genuinely broken configuration — still stops the mining command with a clear explanation of what to fix; a repository that simply has nothing interesting to report is not treated as a failure.
## [2026-08-20T01:36:26.501Z]
The scaffold writer's documentation claimed the rewritten configuration survives byte-for-byte, but the YAML serializer re-spaces flow-collection brackets while preserving every key, value, and comment — the claim now states exactly what is preserved and what may move. The command layer's seed wiring also gained an end-to-end pin: a committed seed entry must reach the mined model and the status report, which guards the join between reading the seed store and handing it to the mining engine — a join that previously could be severed without any test noticing.
## [2026-08-21T07:59:07.699Z]
The index command now loads the committed ledger of hook-shaped marks and the working tree's dirty-file set and threads both into the mining engine so instance weights are backed by real git history instead of a flat constant. An adopter running the command sees mined conventions weighted by how long the underlying code has stood and by who wrote it, and any code the tool previously shaped itself is excluded from counting as evidence until enough time and a human touch have passed.
## [2026-08-21T11:40:46.295Z]
The index command now has to decide, before doing any real work, whether the repository has moved at all since the last time it ran, so that re-running it on an unchanged tree costs nothing rather than re-mining from scratch every time. It also now has to make sure two copies of itself can never write the same mined state at once: it waits briefly for a run already in progress to finish, and only refuses outright if that run is still going after the wait, naming which process is holding the write so the person running it knows what to check.
## [2026-08-21T14:08:11.879Z]
`yg roots status` now reports on the history the index has read in total: how many commits and how many historical file versions the snapshot is built on, how far behind the current code that reading is, and whether a history window is narrowing what gets mined. On a repository with no usable history at all, including a shallow CI clone, it now says so honestly: nothing there can be scored by how long it has stood, so what is reported reflects what the code looks like now rather than what has proven itself.
## [2026-08-22T20:47:56.504Z]
An earlier entry here overstated what a hook-shaped mark does to evidence: it said marked code is excluded from counting until time and a human touch pass. The landed behavior, correct then and now, is gentler — a mark caps that pattern's weight at a low floor, so it counts for much less but never for nothing, and the cap lifts once the code has stood long enough with a later human touch. Recorded so a reader of this history is not misled by the older wording.
