## [2026-05-16T05:57:54.659Z]
Phase 4.7 (no-direct-fs): inject appendToDebugLog as third argument to initDebugLog; debug-log DI refactor decouples utils/debug-log from node:fs
## [2026-05-16T17:37:13.178Z]
Replaced inline 'No .yggdrasil/ directory found' error block with the shared loadGraphOrAbort helper from formatters/cli-preamble.ts. Reason: the same string and exit-1 logic was duplicated across 12 CLI command handlers; centralization eliminates a copy-paste class and routes the missing-graph message through buildIssueMessage uniformly. Other errors continue to flow through the surrounding catch and will be migrated to buildIssueMessage in the next task.
## [2026-05-16T18:22:19.910Z]
Migrated remaining ad-hoc stderr errors to buildIssueMessage (constant-text errors wrapped inline) and routed generic catch-blocks through the new abortOnUnexpectedError helper from formatters/cli-preamble.ts. Reason: even after the loadGraphOrAbort centralization, command-specific errors and option-validation messages bypassed the what/why/next structure; this commit aligns them so the AST aspect added in the next commit can enforce the rule mechanically.
## [2026-05-16T19:31:39.148Z]
Updated effective-aspects import path to core/graph/aspects.
## [2026-05-27T07:22:10.452Z]
Phase 6 type-bridge: updated reviewer display from aspect.reviewer ?? 'llm' to aspect.reviewer?.type ?? 'llm' to match the new AspectReviewerSpec object shape replacing the former string union.
## [2026-05-27T13:54:31.635Z]
Display the resolved reviewer tier (or "(default)") alongside the type so authors see which named tier the aspect will use at approve time.
## [2026-05-28T14:01:29.171Z]
Surface effective aspect status in yg aspects output. Each aspect row now renders [<status>] (defaulting to enforced when unset) right after the id, so adopters can see at a glance whether an aspect is draft, advisory, or enforced before running approve. Aspect-default status is graph-wide — per-node effective status appears in yg context / yg impact instead.
## [2026-05-29T10:05:50.865Z]
Test suite for this command was updated to reflect the redesigned yg check output format. The old format had section headers (Structural:, Cascade summary:), per-node cascade repetition, and a Result: footer. The new format uses a single-line verdict header, grouped cascade blocks, and Why:/Fix: labelled output. Tests that asserted the old format strings were updated to the equivalent new format assertions.
## [2026-05-31T17:27:15.949Z]
Moved the shared command-layer support helpers — the graph-load-or-abort wrapper and the unexpected-error funnel — out of the formatters layer and into the command layer. These helpers must reach both the engine (to load the graph) and the formatters (to build the uniform what/why/next message), and only the command layer may legally depend on both; keeping them in the formatters layer was an upward dependency on the engine that the layering rules forbid. The helpers register no command of their own, so they live under a dedicated command-support classification rather than as a command handler. Command handlers now import them from their new command-layer location.
## [2026-06-13T05:34:12.530Z]
Aspect listing now orders entries by a direct lexical byte comparison of the aspect identifier rather than a locale-sensitive string comparison, so the printed order is deterministic and identical regardless of the host's locale or platform. A locale-aware comparison can reorder identifiers differently across machines, which would make otherwise-identical runs produce divergent output.
## [2026-07-10T09:59:33.876Z]
Added an at-a-glance health view to the aspect listing so a maintainer can see, per rule, how broadly it applies and whether its recorded results still match the current code — without paying for a full review. The guiding intent is honesty about what has actually been checked: when a rule's units have no valid result on record — never checked, or checked and since gone stale — the view must show them as not-yet-checked, never as a clean zero. Reporting an unchecked rule as passing is exactly the false confidence this view exists to prevent. Waivers that silence every rule at once are summarized on their own line rather than blamed on any single rule, since attributing a silence-everything waiver to one rule would misstate where the risk sits. The view is strictly read-only: it inspects existing state, records nothing, and calls no reviewer, so looking at rule health can never itself change a result.
## [2026-07-10T18:11:56.973Z]
Fixed a subject-verb agreement bug in the per-rule health summary's wildcard-waiver note. The sentence conjugated the noun (marker/markers) and the copula (is/are) to the waiver count but left the intervening verb hard-coded as "apply", so the single-waiver case rendered "1 wildcard suppress marker apply ... and is not counted" — a grammatical error in a message adopters read directly. The verb is now conjugated with the count too ("applies" when one, "apply" when many). This is user-facing wording polish only; it changes no counting, no data, and no exit behavior. A unit assertion now pins both the singular ("applies") and plural ("apply") renderings so the disagreement cannot silently return.
## [2026-07-10T20:42:25.076Z]
The aspect-health table's kind column now reports the reviewer kind inferred from rule-source presence rather than the declared reviewer field. An implies-only bundle that ships no rule file but whose declaration mistakenly names an LLM reviewer was being rendered as an LLM aspect, which misrepresents a content-less bundle as something that runs a reviewer. Deriving the displayed kind from whether a content rule, a deterministic check, or only an implied-aspect list is present keeps the health view honest even when a declaration and its files disagree.
## [2026-07-11T18:56:07.319Z]
The per-aspect health view now surfaces how long each rule has existed, derived from the first appearance of its rule definition in version-control history, so a maintainer weighing a rule can read its track record alongside how often it refuses and how many waivers target it. History that cannot be read — a shallow clone, or a working copy with no repository — is reported honestly as unknown rather than a fabricated zero, so a rule with no readable history is never mistaken for a brand-new one. The lookup runs only in this explicit health view; the plain aspect listing and its runtime are left untouched, so the default path pays nothing for the added insight. The reference instant that turns a creation time into a coarse duration is read once at the command boundary and threaded inward, keeping the rendering itself free of any clock read so its output stays stable for a fixed instant and remains testable.
## [2026-07-12T16:10:53.762Z]
The per-rule health view now shows how often a rule has actually caught a violation against how often it was exercised, distinguishing a genuine exercise from a cached re-render and never pooling the two reviewer kinds whose false-positive mechanisms differ. It reports an honest plain-words uncertainty range instead of a false-precision number when observations are few. A rule that is enforceable yet never violated is read as possibly deterring the very violations it would otherwise catch, rather than assumed useless, and a demotion is only ever suggested when several independent signals agree, never on the catch count alone.
## [2026-07-13T22:29:18.961Z]
The rule-health view gained a false-block signal: a per-rule count of how often that rule's refusals were LATER waived or overturned by a human — an honest read of a rule that may be blocking wrongly, so a maintainer can spot and retire the worst offenders. It joins the recorded verification-event history (past refusals) against a live scan of suppress waivers: a refusal is a false block when a waiver for the same rule now covers the refused unit, distinguishing a still-refused block a human waived from one that was re-approved after a waiver moved. It is read-only telemetry rendered only under the health flag, always with a small-sample honesty label and a plain-words rate, and it never influences an exit code, a verdict, or the next-step suggestion — it feeds a human retirement ritual, never an automatic block. The default listing is deliberately left untouched.
## [2026-07-13T22:49:03.301Z]
Continues wiring the rule-health false-block signal after its coverage resolver was moved out of the shared suppression-scan module into a focused sibling: that module sits under a strict per-file size boundary, and the graph-aware waiver-to-unit resolution the false-block join needs would have pushed it past that boundary, so it was split into its own focused derivation rather than growing one file over its cap. The command now reads the false-block coverage from that dedicated resolver. The signal itself is unchanged in intent — a read-only, small-sample-honest count of how often a rule's refusals were later waived or overturned, shown only under the health flag, feeding a human retirement ritual and never gating a build.
## [2026-07-14T09:37:55.069Z]
The rule-health view now surfaces, per rule, how many recorded miscalibration reports name that rule, shown as an honest count carrying a small-sample humility label because such reports are sparse, hand-written testimony rather than a measured rate. The committed testimony is read here at the command boundary and handed to the health builder as plain per-rule counts, exactly as the local telemetry is threaded in, so the shared health-signal engine never reads that testimony itself and no observability source can reach a verdict. The signal is read-only and never gates. The boundary matters: keeping the read at the presentation layer preserves the rule that the core reasoning engine stays free of any reader of external or local record.
## [2026-07-14T17:38:16.272Z]
The per-aspect health view's regression cross-reference now consults only actionable recorded drill outcomes — an in-repository run whose case still exists in the aspect's current regression corpus — applying the same actionability rule as the attention feed so the two surfaces tell one consistent story. A stale or externally-measured drill outcome was making a never-violated rule read as though a regression case had stopped being caught and the rule "may be weakening", when in fact no in-repository regression case had failed. The health reading must reflect the current corpus, not append-only residue. The lookup is fail-open so the read-only view can never break.
## [2026-07-16T13:50:52.281Z]
Narrowed this command's module surface as part of a cleanup pass. A health-report assembly helper that is only ever called inside this module carried an unnecessary export marker, which misrepresented it as part of the module's public interface. Removing the marker makes the exported surface honestly reflect what other modules actually consume. Purely a visibility change — the function, its single in-module call site, and all behavior are unchanged.
## [2026-07-28T19:46:11.398Z]
The rule-health table now counts a distinct file enforced by its architecture type alone separately from a component, so such a file can never inflate the reported component count by one.
## [2026-07-30T19:19:24.535Z]
The usage listing counted only real component nodes, so a rule enforced solely through files matching an architecture type, with no owning component, was reported as unused even though the structural check already treated it as enforced. Usage now also counts such files, shown under their own label, so a genuinely enforced rule is never called unused.
## [2026-07-30T21:01:41.746Z]
The --health view now threads the same type-coverage classification the plain aspects listing already computes into its lock verification, so a rule live only on files enforced by their architecture type (no owning component) has its pairs and refusals counted, instead of silently reading a smaller, node-only universe that disagreed with what the check command itself reports. The view also renders a files column showing how many distinct type-covered files carry a review pair for each rule; a comment that said the column did not exist yet was corrected to match. Two other comments were reworded to drop a short internal code with no meaning to a reader of this repository.
## [2026-07-30T22:18:14.041Z]
The health table's files column now renders an em-dash when coverage.type_level is off, matching every other on-demand column instead of printing a literal 0 for a question that was never asked. The column itself is now appended to the header only when the tier is on, so a repository that never enables it keeps byte-identical --health output to before the column existed.
## [2026-07-31T04:22:12.842Z]
The suppress scan behind the health view now also treats a file covered by the architecture type-level lattice as a live waiver site, exempt from the noise filter the same way a mapped node source already is, reusing the type-coverage classification the health view already computes for lock verification rather than reclassifying a second time.
## [2026-07-31T12:04:33.144Z]
Same threading as the other suppression-scan callers: the health-check summary's suppression count must agree with the same exclusion authority the enforcement surface now honors, so an excluded file's waiver is never counted here either.
## [2026-08-02T12:05:03.752Z]
Now calls computeTypeCoverageCached instead of computeTypeCoverage, so yg aspects benefits from the persistent on-disk type-classification cache instead of paying full classification cost every invocation.
## [2026-08-03T00:22:37.720Z]
Renamed the local variable holding this command's type-coverage classification walk from gitFiles to repoFiles, matching the plain disk walk it actually is rather than implying a git-tracked source. The sibling local variable a few dozen lines away, already named repoFiles for the identical walk in this same command's main body, is unchanged -- the two now agree.
## [2026-09-06T19:32:48.367Z]
The rule listing gained a machine form for the same reason the run's report did: a layer that needs to know which rules exist and whether any is running unreviewed should not have to parse sentences. It counts each rule's drill corpus rather than running it, so the inventory stays a read with no reviewer and no check execution behind it. The health projection is refused alongside the inventory rather than folded into it — it has different columns and costs a whole verification pass, and letting one schema name two things that expensive would make every consumer pay for whichever half it did not want.
## [2026-09-07T08:00:55.019Z]
A rule now has the two commands a component has had all along: one to write its history, one to read it. Before this, a rule's story lived in commit messages or — worse — was written into the logs of every component the rule happened to reach, so one rule's history was smeared across a dozen places that are not about it and reconstructable from none of them. Recording a change of standing is deliberately a record and not the change itself. The rule's own file stays the user's to edit, so the command refuses to record a standing the file does not already carry, and refuses one with no evidence behind it: what justified moving a rule is the part nobody can reconstruct a year later, and the part that matters most. Where the rule moved from is derived rather than asked for — from its own history first, then from the standing the tool last saw — and when neither knows, the entry says so instead of assuming the default and stating a standing that may never have been true.
## [2026-09-11T16:58:04.539Z]
The rule inventory can now name the units behind its usage counts, not just count them: yg aspects --json --reach adds, per rule, every unit it judges with the effective status there and the channel it arrived through. Three independent consumers needed exactly this and each fell back to reading the gate document, which has two problems worth fixing at the source. The gate never enumerates a pair it does not judge, so a rule sitting at draft over real subjects reads there as a rule that reaches nothing — the one reading no ladder from draft to enforced can afford to get wrong. And a whole verification pass is a heavy way to answer a question the pair enumeration alone answers. The enumeration runs the same expected-pair computation the gate runs, with draft rules included, so the two can never disagree about what a rule reaches while still telling not-yet-judged apart from reaches-nothing. It is opt-in because it walks every mapped file, and the document without it is unchanged.
## [2026-09-23T20:03:19.176Z]
Reading a rule's history now takes the same flags as reading a component's: --top for the newest few entries and --all for every one, with --limit kept as another name for --top because scripts already use it. The default stays the whole history, because the machine document is read by other tools that expect every entry unless they ask for fewer. Recording a change of standing now refuses a standing the rule already stood at: without that, a second promotion to the same level wrote an 'enforced → enforced' entry, and the history the command exists to keep gained a change nobody made. When the tool itself already wrote down an edit made directly in the rule's file, recording it afterwards is the person supplying the evidence, so that change is measured from where the rule stood before the edit and is not refused.
## [2026-09-23T20:24:31.151Z]
The health view counts committed refusal drill cases, so a rule whose drills exist but were not run in this checkout reads as unconfirmed with drills present, not as having no drill at all.
## [2026-09-23T20:49:12.301Z]
Usage counts now tell hierarchy inheritance and port requirements apart from implies-expansion. Reach through an ancestor used to be counted as implied, which misled anyone judging a rule's reach or removing an implies edge; implied now means only another rule's implies.
## [2026-09-23T22:12:10.551Z]
The environment-robustness fixes and the release work already merged met in this command: aspects --health says a rule's drills exist but were not run here when its committed corpus holds refusal cases, and the aspects log command keeps the release's earlier changes (the top and all reading, the refused no-op status). The merged source carries both behaviours.
## [2026-09-23T22:37:49.738Z]
The graph-governance work met the release work already merged in this command: usage counts tell inheritance through an ancestor and port requirements apart from implies-expansion, while the health view still counts committed refusal drill cases and the rule-history read keeps its --top and --all flags. The two sets of behaviour do not depend on each other and both survive.
## [2026-09-23T23:00:35.669Z]
Command errors now go through the shared CLI output layer (fail, or failAndExit where the command exits at once) instead of each command writing its own red Error: what/why/next line to stderr, and the commands that kept a private failWith or emitError copy of that line now use the shared one. One place now owns how a command error reads and where it goes, so wording and stream change once for every command. When the invocation answers in JSON, that layer also writes a yg-error/1 document (code, what, why, next) to stdout, because a machine reader used to get zero bytes on stdout for a failed command and had to parse English from stderr. yg aspects on a graph with no rules now says so in one line instead of printing nothing, which read as a failure.
## [2026-09-24T06:15:39.550Z]
The health view can now be asked for as JSON, and answers in a document of its own rather than being refused. Refusing the combination kept the rule inventory from meaning two things, which still holds: the inventory keeps its schema and its meaning, and health gets a separate schema name. The layer above the agent that notices a rule gone quiet read the rendered table and the sentences under it, which are about to change; per rule the document carries the counts, the signal and the same plain-words reading the table prints, with null for a question never asked so an unjudged rule never reads as clean.
## [2026-09-24T07:23:42.893Z]
The CLI now speaks one output grammar: every finding is a block headed error[label] or warning[label] with lowercase labelled at:, why: and fix: fields, every command error is error[code]: what with a labelled why and a next: step, and a report ends with one next: step and optionally a then: step. Scripts that parsed the old capitalised Why:, Fix: and Next: lines are sent to the JSON documents, which carry the same facts. This command's remaining capitalised Next: or Warning: lines are lowercase, its counts agree with their nouns, and its first-level text says reviewer rule where it said LLM aspect.
## [2026-09-24T13:50:50.251Z]
Every byte this command prints now goes through the output layer: stdout and stderr through writeOut and writeErr, colour through paint, instead of process.stdout.write, process.stderr.write and a chalk import of its own. The repository now refuses a direct stream write, a console call or a chalk import outside that layer, so that where output goes, what guards it and when it is decorated are decided in one place and cannot drift command by command again, as they had across hundreds of write sites before the layer existed. Its usage line counts nodes and type-covered files with count(), so one reads '1 node' rather than a spliced-on plural.
## [2026-09-24T14:24:24.313Z]
The text listing labelled a rule's kind 'Reviewer:', which made a script rule and a bundle look as if they had a reviewer. It now prints 'Kind: reviewer rule' (with its tier), 'Kind: script rule' or 'Kind: bundle', and the health table's kind column uses the same words; the reviewer is only the model configured under reviewer:. A change of a rule's draft/advisory/enforced is called a change of status, not of standing. JSON output keeps its llm / deterministic / aggregate values, so scripts reading it are unaffected.
## [2026-09-24T14:46:08.100Z]
The health report ended its wildcard-marker remark with a hand-built capitalized Note: prefix, the old grammar every other command has left; it now goes through the output layer as a lowercase note: line like the rest of the CLI.
## [2026-09-24T14:51:41.369Z]
Output that names a next step now goes through the output layer next and then lines instead of being laid out by hand, and a source comment that spoke about the reviewer is reworded to describe the split plainly, so the one output grammar and the untrusted-content prompt hold for these files too.
## [2026-09-24T14:53:21.271Z]
The wildcard-marker note in the health report now uses the lowercase note label and the shared count helper, like the report s other note, so both notes read in the one output grammar.
## [2026-09-24T15:11:23.919Z]
The wildcard-marker note in the health view was written two ways by two release lines: one counted the markers through count() so the number and noun always agree, the other wrapped the line in the shared note() helper. The line now does both, so its grammar and its note: prefix come from the output layer rather than from hand-built text.
## [2026-09-25T21:13:56.047Z]
The rule-log subcommands now refuse an unknown rule id with the shared aspect-not-found error, in text and in the JSON error document. An unknown rule id used to be refused in about six different wordings under two codes depending on the command, so an agent could not recognise the one situation or learn one remedy.
