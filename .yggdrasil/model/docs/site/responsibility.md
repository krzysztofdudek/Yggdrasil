# Docs Site Config — Responsibility

VitePress infrastructure — build system, navigation structure, theme, and static assets. Distinguishes from sibling content nodes (concept, guides) by owning HOW documentation is built and presented, not WHAT it says. A broken VitePress config prevents the entire docs site from building. Theme changes affect every page's appearance. Navigation structure determines discoverability.
