## [2026-09-23T20:09:45.974Z]
The node claimed every rule script this graph defines, but its mapping and its type's classifying predicate reached only one directory level, so the rule scripts of grouped aspect ids (the portal rules and the reference-catalogue rules, twenty-one of fifty-seven) were outside every rule this node carries, among them the ones guarding secrets and loopback binding. The mapping now names each nesting depth an aspect id can have. A single recursive glob would also have swept in the fake rule scripts that drill cases keep under their own nested graph, and a mapping entry cannot subtract, so the depths are listed instead and a repository test fails the moment a rule script appears that none of them reaches, or a drill fixture that one of them does.
