## [2026-09-23T20:10:29.220Z]
The list of modules that decide the build outcome was a literal file list, and the check engine had since split into stages the list did not name — progressive scoping among them, which re-codes out-of-scope findings and so shapes the exit code. The gating set is now derived from file-name globs (every check stage in core and cli, every progressive-scope module, the issue grouping), so a stage split out later is fenced the moment it exists, and a unit test plants a forbidden import in every real file those globs reach. The description, which named six modules and called the log command read-only although it writes, was rewritten from the same globs and says the presentation commands never serialize YAML rather than that they never write.
