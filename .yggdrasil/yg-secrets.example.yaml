# Copy this file to yg-secrets.yaml and fill in your values.
# yg-secrets.yaml is gitignored — never commit secrets.

llm:
  # api_key: "your-api-key-here"
  # provider: ollama          # ollama | openai | anthropic
  # model: "qwen3.5:9b"
  # endpoint: "http://localhost:11434"
