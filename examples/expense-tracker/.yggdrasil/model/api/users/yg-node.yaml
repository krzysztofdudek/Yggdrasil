name: Users
type: service
description: "User profile and account operations: profile fetch and password change"

relations:
  - target: api/db
    type: uses
  - target: api/auth
    type: uses

mapping:
  - paths:
      - apps/api/src/routes/users.ts
      - apps/api/src/services/users.ts
