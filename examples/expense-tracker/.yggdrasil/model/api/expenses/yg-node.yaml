name: Expenses
type: service
description: "Expense CRUD with subscription limit enforcement and budget overage detection"

relations:
  - target: api/db
    type: uses
    consumes: [db.prepare, db.run]
  - target: api/auth
    type: uses
    consumes: [requireAuth]

mapping:
  - paths:
      - apps/api/src/routes/expenses.ts
      - apps/api/src/services/expenses.ts
