name: Budgets
type: service
description: "Monthly budget limits per category with spent-amount computation"

relations:
  - target: api/db
    type: uses
  - target: api/auth
    type: uses

mapping:
  - paths:
      - apps/api/src/routes/budgets.ts
      - apps/api/src/services/budgets.ts
