name: Reports
type: service
description: "Read-only spending aggregation: monthly summary and per-category breakdown"

relations:
  - target: api/db
    type: uses
    consumes: [db.prepare]
  - target: api/auth
    type: uses
    consumes: [requireAuth]

mapping:
  - paths:
      - apps/api/src/routes/reports.ts
      - apps/api/src/services/reports.ts
