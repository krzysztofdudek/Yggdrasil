name: Reports Page
type: module
description: "Monthly spending breakdown by category with percentages"

relations:
  - target: api/reports
    type: calls
    consumes: [GET /reports]

mapping:
  - paths:
      - apps/web/src/pages/Reports.tsx
