name: Categories Page
type: module
description: "Category list and custom category creation with limit counter"

relations:
  - target: api/categories
    type: calls

mapping:
  - paths:
      - apps/web/src/pages/Categories.tsx
