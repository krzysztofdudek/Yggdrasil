version: "5.2.0"

# Partial-dir: one explicit node owns one file in a directory; its sibling is
# left type-covered, matching the same architecture type but claimed by no
# node. Both satisfy coverage, via different mechanisms, in the same
# directory — the fixture this proves.
coverage:
  required:
    - src/
  excluded: []
  type_level: true
