quality:
  max_direct_relations: 10

reviewer:
  tiers:
    standard:
      provider: ollama
      consensus: 1
      config:
        model: test
        # Reserved port 1 — guaranteed unreachable; this fixture is only driven
        # read-only by the offline family miner, so no reviewer is ever called.
        endpoint: http://127.0.0.1:1
