$ yg check
exit: 1
--- stdout
yg check: FAIL  1 error · 2 warnings   3 nodes · 3/3 files covered · 1 pair verified (script) · 1 obligation outside your changes vs main (1 changed input)

error[unverified] 1 pair whose inputs changed since the verdict
  at:   no-todo-comments @ alpha
  why:  A verdict was recorded, but its inputs changed since (a source edit, an aspect edit, or a changed reference), so it no longer counts. It is re-judged over the code as it stands now.
  fix:  yg check --approve  (1 script pair · free)

warning[rules-digest-stale] Committed agent-rules digest is out of sync: AGENTS.md digest block is missing; .clinerules/yggdrasil.md is missing; CLAUDE.md @AGENTS.md import is missing
  why:  Agents read the committed digest before running yg prime; a stale, hand-edited, or missing digest means agents may follow outdated rules or none at all.
  fix:  yg init --upgrade

warning[refused-outside] no-todo-comments — 1 violation in beta — outside your changes
  at:   beta  src/beta/beta.ts:1  TODO comment found — remove it or track the work in the issue tracker.
  why:  Source files must not contain TODO comments — track work in the issue tracker, not the code.

note: 1 enforced finding held outside it stands on code this change did not touch — that is the baseline this repository already had, not a result of your change.
--- stderr
