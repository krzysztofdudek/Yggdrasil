$ yg check
exit: 1
--- stdout
yg check: FAIL  4 nodes · 3/8 files (38%) · 1 aspects · 0 flows

Errors (5) in 2 groups:

  unverified (deterministic check not run on this checkout — free)  3 pairs  3 nodes
            Deterministic results live in the gitignored local cache (.yggdrasil/.yg-lock.deterministic.json), so a fresh clone, a new rule or a cleared cache holds none until the check runs on this checkout. Running it is free: no reviewer call, and the committed lock is not touched.
            Fix: yg check --approve --only-deterministic
            - app/svc-01  aspect 'no-todo'
            - app/svc-02  aspect 'no-todo'
            - app/svc-03  aspect 'no-todo'

  config-invalid
            yg-config.yaml failed to parse.
            Why: Flow sequence in block collection must be sufficiently indented and end with a ] at line 15, column 1:

quality: [

^

            Fix: Fix the syntax error in .yggdrasil/yg-config.yaml.

  unmapped (5)
            .clinerules/yggdrasil.md
            .gitattributes
            AGENTS.md
            CLAUDE.md
            src/tools/gen.ts
            Why: No node maps these files, so no rule checks them, and yg check stays red until each one has an owner (or is moved out of coverage.required).
            Fix: Add each file to a node's mapping, or create a new node for it — yg context --file <path> lists candidate owners.

Next: yg check --approve --only-deterministic  (fills 3 unverified; 2 errors remain — need code/graph fixes)
--- stderr
