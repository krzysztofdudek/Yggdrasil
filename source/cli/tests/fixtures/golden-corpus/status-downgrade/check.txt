$ yg check
exit: 1
--- stdout
yg check: FAIL  5 errors in 2 blocks · 5 warnings in 2 blocks   5 nodes · 4/8 files covered

error[aspect-status-downgrade] Aspect 'header-comment': node type 'service' in yg-architecture.yaml declares status 'advisory', but the aspect already reaches the 4 nodes it attaches to there (app/svc-01, app/svc-02, app/svc-03, …) as 'enforced' from the aspect default (enforced, because the aspect's yg-aspect.yaml sets no status:)
  why:  An explicit attach-site status cannot relax (downgrade) what already cascades — that would silently weaken enforcement. An attach site can only raise the status above the aspect default and the other sites, never lower it.
  fix:  Remove the explicit status from node type 'service' in yg-architecture.yaml (let the cascade win), or, to weaken the rule everywhere, set a lower status: in the aspect's yg-aspect.yaml. See: yg knowledge read aspect-status.

error[unverified] 4 pairs whose script check has not run on this checkout — free to run
  at:   no-todo  4 pairs · 4 nodes · script
  why:  Script-rule results live in the gitignored local cache (.yggdrasil/.yg-lock.deterministic.json), so a fresh clone, a new rule or a cleared cache holds none until the check runs on this checkout. Running it is free: no reviewer call, and the committed lock is not touched.
  fix:  yg check --approve --only-deterministic  (4 script pairs · free)

warning[unverified] 4 pairs whose script check has not run on this checkout — free to run
  at:   header-comment  4 pairs · 4 nodes · script
  why:  Script-rule results live in the gitignored local cache (.yggdrasil/.yg-lock.deterministic.json), so a fresh clone, a new rule or a cleared cache holds none until the check runs on this checkout. Running it is free: no reviewer call, and the committed lock is not touched.
  fix:  yg check --approve --only-deterministic  (4 script pairs · free)

warning[uncovered] 4 files belong to no node — not under coverage.required, so they never block
  at:   .clinerules/yggdrasil.md
        .gitattributes
        AGENTS.md
        CLAUDE.md
  why:  Not under a coverage.required root — shown, but it never blocks.
  fix:  Map these files to a node, or add their root to coverage.required to make this an error.

next: edit .yggdrasil/yg-architecture.yaml  (aspect-status-downgrade)
then: yg check --approve --only-deterministic  (4 script pairs · free)
--- stderr
