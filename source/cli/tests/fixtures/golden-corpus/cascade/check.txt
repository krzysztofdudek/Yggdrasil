$ yg check
exit: 1
--- stdout
yg check: FAIL  5 errors · 1 warning   3 nodes · 2/7 files covered

partial: 1 component file did not parse (app/svc-02), so that component was left out — the findings below were computed without it and may be symptoms of it; fix it first.

error[yaml-invalid] yg-node.yaml in app/svc-02 does not parse: Flow sequence in block collection must be sufficiently indented and end with a ] at line 7, column 1
  at:   app/svc-02
          bad: [unclosed
          ^
  why:  The file is not valid YAML, so component 'app/svc-02' was not loaded: a flow or relation naming it reads it as missing, and the files it maps read as unmapped, until it parses.
  fix:  Fix the YAML in .yggdrasil/model/app/svc-02/yg-node.yaml.

error[flow-node-broken] Flow 'Checkout' names node 'app/svc-02', whose yg-node.yaml did not parse
  why:  The component was not loaded, so the flow cannot resolve it — a symptom of the yaml-invalid error on 'app/svc-02', not a missing node.
  fix:  Fix the YAML in .yggdrasil/model/app/svc-02/yg-node.yaml; the flow resolves once the component loads.

error[unmapped] 1 file belongs to no node
  at:   src/svc-02/index.ts
  why:  No node maps these files, so no rule checks them, and yg check stays red until each one has an owner (or is moved out of coverage.required).
  fix:  Add each file to a node's mapping, or create a new node for it — yg context --file src/svc-02/index.ts lists candidate owners.

error[unverified] 2 pairs whose script check has not run on this checkout — free to run
  at:   no-todo  2 pairs · 2 nodes · script
  why:  Script-rule results live in the gitignored local cache (.yggdrasil/.yg-lock.deterministic.json), so a fresh clone, a new rule or a cleared cache holds none until the check runs on this checkout. Running it is free: no reviewer call, and the committed lock is not touched.
  fix:  yg check --approve --only-deterministic  (2 script pairs · free)

warning[uncovered] 4 files belong to no node — not under coverage.required, so they never block
  at:   .clinerules/yggdrasil.md
        .gitattributes
        AGENTS.md
        CLAUDE.md
  why:  Not under a coverage.required root — shown, but it never blocks.
  fix:  Map these files to a node, or add their root to coverage.required to make this an error.

next: edit .yggdrasil/model/app/svc-02/yg-node.yaml  (yaml-invalid — 3 errors need a code or graph fix)
then: yg check --approve --only-deterministic  (2 script pairs · free)
--- stderr
