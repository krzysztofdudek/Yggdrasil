$ yg check --details
exit: 1
--- stdout
yg check: FAIL  34 errors in 4 blocks · 1 warning   25 nodes · 24/29 files covered · 16 pairs verified (script)   view: details

error[refused] no-todo — 8 violations in 8 nodes
  at:   app/svc-03  src/svc-03/index.ts:2  TODO marker left in shipped code — move it to the tracker
        app/svc-06  src/svc-06/index.ts:2  TODO marker left in shipped code — move it to the tracker
        app/svc-09  src/svc-09/index.ts:2  TODO marker left in shipped code — move it to the tracker
        app/svc-12  src/svc-12/index.ts:2  TODO marker left in shipped code — move it to the tracker
        app/svc-15  src/svc-15/index.ts:2  TODO marker left in shipped code — move it to the tracker
        app/svc-18  src/svc-18/index.ts:2  TODO marker left in shipped code — move it to the tracker
        app/svc-21  src/svc-21/index.ts:2  TODO marker left in shipped code — move it to the tracker
        app/svc-24  src/svc-24/index.ts:2  TODO marker left in shipped code — move it to the tracker
  why:  Unfinished work belongs in the tracker, not in shipped code.
  fix:  Change the code at these lines, then run yg check --approve --only-deterministic (free) to record the new verdict.

error[relation-broken] Relation target 'app/svc-99' does not exist
  at:   app/svc-05
  why:  This node declares a dependency on a node the graph does not contain, so the relation cannot be checked. Nodes under app: svc-01, svc-02, svc-03, svc-04, svc-05, svc-06, svc-07, svc-08, svc-09, svc-10, svc-11, svc-12, … (24 in all).
  fix:  Correct the target in .yggdrasil/model/app/svc-05/yg-node.yaml relations, or remove the relation.

error[unmapped] 1 file belongs to no node
  at:   src/tools/gen.ts
  why:  No node maps these files, so no rule checks them, and yg check stays red until each one has an owner (or is moved out of coverage.required).
  fix:  Add each file to a node's mapping, or create a new node for it — yg context --file src/tools/gen.ts lists candidate owners.

error[unverified] 24 pairs with no verdict yet
  at:   readable-names @ app/svc-01
        readable-names @ app/svc-02
        readable-names @ app/svc-03
        readable-names @ app/svc-04
        readable-names @ app/svc-05
        readable-names @ app/svc-06
        readable-names @ app/svc-07
        readable-names @ app/svc-08
        readable-names @ app/svc-09
        readable-names @ app/svc-10
        readable-names @ app/svc-11
        readable-names @ app/svc-12
        readable-names @ app/svc-13
        readable-names @ app/svc-14
        readable-names @ app/svc-15
        readable-names @ app/svc-16
        readable-names @ app/svc-17
        readable-names @ app/svc-18
        readable-names @ app/svc-19
        readable-names @ app/svc-20
        readable-names @ app/svc-21
        readable-names @ app/svc-22
        readable-names @ app/svc-23
        readable-names @ app/svc-24
  why:  The lock holds no entry for this pair: it is new (a new rule, component or mapped file), or the fill that would have judged it did not complete.
  fix:  yg check --approve  (24 reviewer pairs · paid — ask the user to approve it first)

warning[uncovered] 4 files belong to no node — not under coverage.required, so they never block
  at:   .clinerules/yggdrasil.md
        .gitattributes
        AGENTS.md
        CLAUDE.md
  why:  Not under a coverage.required root — shown, but it never blocks.
  fix:  Map these files to a node, or add their root to coverage.required to make this an error.

next: edit src/svc-03/index.ts:2  (refused — 10 errors need a code or graph fix)
then: yg check --approve  (24 reviewer pairs · paid — ask the user to approve it first)
--- stderr
