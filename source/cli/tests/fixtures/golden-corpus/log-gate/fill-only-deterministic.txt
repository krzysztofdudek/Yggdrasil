$ yg check --approve --only-deterministic
exit: 1
--- stdout
--- stderr
No fresh log entry for node 'app/svc-01' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-01 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
No fresh log entry for node 'app/svc-02' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-02 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
No fresh log entry for node 'app/svc-03' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-03 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
No fresh log entry for node 'app/svc-04' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-04 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
No fresh log entry for node 'app/svc-05' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-05 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
No fresh log entry for node 'app/svc-06' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-06 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
No fresh log entry for node 'app/svc-07' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-07 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
No fresh log entry for node 'app/svc-08' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-08 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
No fresh log entry for node 'app/svc-09' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-09 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
No fresh log entry for node 'app/svc-10' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-10 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
No fresh log entry for node 'app/svc-11' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-11 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
No fresh log entry for node 'app/svc-12' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-12 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
No fresh log entry for node 'app/svc-13' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-13 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
No fresh log entry for node 'app/svc-14' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-14 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
No fresh log entry for node 'app/svc-15' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-15 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
No fresh log entry for node 'app/svc-16' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-16 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
No fresh log entry for node 'app/svc-17' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-17 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
No fresh log entry for node 'app/svc-18' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-18 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
No fresh log entry for node 'app/svc-19' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-19 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
No fresh log entry for node 'app/svc-20' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-20 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
No fresh log entry for node 'app/svc-21' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-21 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
No fresh log entry for node 'app/svc-22' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-22 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
No fresh log entry for node 'app/svc-23' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-23 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
No fresh log entry for node 'app/svc-24' — mandatory before recording verdicts when its source drifted.
Node type 'service' has log_required: true — every source change needs a justification entry capturing WHY. This component's source has drifted from the state its recorded verdicts were written over, which earlier commits can be as much the cause of as anything in progress now. Recording answers for the code as it stands, so it stops here and approves nothing this run until a fresh entry exists.
yg log add --node app/svc-24 --reason '<why this change was made>', then re-run: yg check --approve --only-deterministic — if you did not make this change, ask the user for the reason; never invent one
