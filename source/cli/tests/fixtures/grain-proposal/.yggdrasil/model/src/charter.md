# src

Proposed node for `src`. Written by grain from evidence in this repository; nothing here is verified.

## Certified conventions

- `no-todo-comments` — every file under `src/` is free of TODO markers (1 site breaks it today).

## Sub-gate candidates — evidence, not yet law

- `named-export` — every file under `src/` declares at least one named export.
