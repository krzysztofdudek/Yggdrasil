# Proposed graph

Written by grain from evidence in this repository. Nothing here is verified.

- **architecture**: 1 node type · 1 node · 0 dependency cycles
- **enforced**: 1 of 2 aspects earned `status: enforced` from a real drill
- **candidates**: 1 advisory (sub-gate origin, same drill bar as enforced but below grain's own certification bound)

Per-element evidence: `proposal.json`. Move `.yggdrasil/` to the repository root and run `yg check`,
or accept the whole proposal with `yg adopt`.
