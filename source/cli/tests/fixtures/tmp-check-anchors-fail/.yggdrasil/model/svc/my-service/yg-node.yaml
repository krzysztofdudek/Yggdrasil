name: MyService
type: service
description: test
aspects:
  - logging
mapping:
- paths:
- src/svc/
    aspects:
- aspect: logging
        anchors:
          audit-entry:
            regex: "NONEXISTENT_PATTERN"
            rationale: "Check for pattern"
