name: Check
type: engine
description: >-
  The fixture's engine core. Owns the runCheck seam the rule derives its option
  set from: two issue-gating options written as the ternary the derivation
  matches, one whole-list rewrite written as the shape the second derivation
  matches, a near miss of that rewrite whose branches are different lists, one
  byproduct switch written as an if-statement, and a same-file helper carrying
  its own gating ternary and its own rewrite, neither of which may leak into
  the derived key set.
aspects: []
relations: []
mapping:
  - src/core/check.ts
