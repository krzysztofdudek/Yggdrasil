// yg-suppress-disable(no-todo) legacy file, whole-file waiver — tracked in TICKET-401
export function whole(): number {
  // TODO: migrate this module off the legacy pipeline once TICKET-401 lands
  return 1;
}
