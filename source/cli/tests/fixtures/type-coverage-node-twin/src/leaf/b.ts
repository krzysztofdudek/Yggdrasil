// Plain sibling file — type-covered only, no owning node. Removed in the
// "without" copy of this fixture to compare 'owned's own context/aspect-test
// output with and without it present.
export const b = true;
