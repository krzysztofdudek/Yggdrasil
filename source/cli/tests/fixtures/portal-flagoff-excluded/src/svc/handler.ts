// A second genuinely unmapped file, in its own directory, so the unguarded residue
// names more than one path — proof that flipping an excluded root out of the residue
// does not also sweep up files that were never excluded.
export function handle(): string {
  return 'handled';
}
