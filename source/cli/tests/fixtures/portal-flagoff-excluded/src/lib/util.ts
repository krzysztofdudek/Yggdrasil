// No node maps this file and the type-level tier is off in this fixture, so nothing
// classifies it either — it is a plain, genuinely unmapped file: present in the
// unguarded residue, unlike the excluded file under vendor/.
export function helper(): string {
  return 'helper';
}
