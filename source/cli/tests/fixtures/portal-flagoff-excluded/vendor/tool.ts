// Sits under vendor/, a coverage.excluded root — skipped before classification
// regardless of whether the type-level tier is on. Counts toward the "excluded"
// term, never toward the unguarded residue, with or without the tier.
export const tool = true;
