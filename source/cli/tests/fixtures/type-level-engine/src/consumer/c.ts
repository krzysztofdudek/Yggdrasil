// Matched by 'consumer' (parents: [classifying-parent]). Its effective
// aspects depend on the derived relations a TypedEdgeIndex supplies for this
// file's outgoing imports — the tests supply a hand-built index, not a real
// import graph, since the shape of that index is this module's documented
// input contract.
export const c = 1;
