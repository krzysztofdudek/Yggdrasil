// Owned by the explicit 'forbidden' node (type: mid), which declares its own
// aspect (component-only-rule) — never reachable from any type-covered
// file's cascade (channel 2 is structurally unreachable from a transient
// subject view).
export const x = 1;
