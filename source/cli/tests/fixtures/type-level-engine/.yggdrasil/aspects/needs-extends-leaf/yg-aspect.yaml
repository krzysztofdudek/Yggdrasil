name: Needs Extends Leaf
description: "Attached to consumer with when { relations: { extends: { target_type: leaf } } } — a resolved import is type-blind evidence among structural kinds, so it must answer 'extends' exactly as it answers 'uses'."
reviewer:
  type: deterministic
scope:
  per: file
