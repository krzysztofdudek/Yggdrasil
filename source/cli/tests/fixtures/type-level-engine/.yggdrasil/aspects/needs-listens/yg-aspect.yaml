name: Needs Listens
description: "Attached to consumer with when { relations: { listens: { target_type: leaf } } } — an import is never evidence of an event; a derived relation never carries type 'listens', so this must always read false."
reviewer:
  type: deterministic
scope:
  per: file
