name: Vendor Owner
type: leaf
description: "An explicit component whose mapping names a file under vendor/ — a root this variant's config excludes from coverage. Pins that a coverage exclusion cuts an explicit mapping entry too: the file is never a review subject despite the node naming it directly."
mapping:
  - vendor/mapped.ts
