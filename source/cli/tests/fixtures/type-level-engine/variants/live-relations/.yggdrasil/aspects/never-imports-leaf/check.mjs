// Trivial deterministic check — the applicability predicate is what this
// variant exercises, not the check body — so this file exists only to make
// the aspect a real, loadable deterministic rule rather than a content-less
// stub.
export function check(_ctx) {
  return [];
}
