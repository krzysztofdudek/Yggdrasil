# LLM Leaf Rule

An LLM-reviewed rule attached to the `leaf` type, present so a test using this
variant can exercise a type-covered file going through the LLM review path
rather than only the deterministic one.

Approve any file that exists.
