# variants/zero-enforcement

Overlays a `yg-architecture.yaml` where `emptyparents` carries only a
whole-unit-scoped aspect, plus a second file matching it. Pins that a file can
be covered by its type while having nothing applicable at the per-file
granularity.
