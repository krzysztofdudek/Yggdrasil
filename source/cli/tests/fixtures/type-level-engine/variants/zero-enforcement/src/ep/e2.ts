// A second file matched by 'emptyparents', alongside the base fixture's e.ts.
// This variant's architecture overlay gives the type only a whole-unit-scoped
// aspect, so neither file has anything applicable at the per-file granularity.
export const e2 = 1;
