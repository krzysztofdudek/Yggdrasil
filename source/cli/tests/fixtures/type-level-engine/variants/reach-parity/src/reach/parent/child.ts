// Genuinely owned by reach-parent/child (child-wins: a child mapping one
// specific file inside a directory its parent globs owns that file, not the
// parent) — reach-leaf's architecture-derived read allowance must NOT admit
// it, even though the parent's directory mapping textually covers it.
export const child = 1;
