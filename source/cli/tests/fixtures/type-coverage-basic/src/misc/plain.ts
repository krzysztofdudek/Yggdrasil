// Lattice row: UNMATCHED. Matches none of svc/util/special/big — this
// architecture has no type for this file at all, distinct from a file that
// matches a type but simply has no owning node.
export const plain = true;
