/**
 * Utilities for resolving graph file paths.
 *
 * NOTE: this module is currently empty. Its former contents — the per-node
 * drift-identity engine (collectTrackedFiles, buildLayerResolver, emptyIdentity,
 * TrackedFile, TrackedContext) and the yggPrefixOf helper — were retired when the
 * verdict-lock redesign replaced the approve pipeline that consumed them. Kept as
 * the canonical home for future graph-file-path helpers.
 */

export {};
